SESSION HANDOFF — 20 Aug

qa HEAD: 3885b34ebccd0176497f42572a1272576424db30. Re-verify fresh at session start.

This session, in order:

Session-start protocol run: confirmed prior handoff's qa HEAD (8158f97), reconciled Feedback table. Caught one real discrepancy: prior handoff said "GEN-2608-073 (Artist pages) fully shipped" — the actual live GEN-2608-073 row is a separate, still-open item (Artist directory cards read as too basic/ordinary on real-photo cards, status NEW). The shipped item was GEN-2608-072 (the /for-artists landing page). Flagged rather than silently carried forward. GEN-2608-073 is STILL OPEN, unresolved, carry forward again.

Placement/fidelity chain on the venue detail page header (BUG-2608-072→076), all Hitesh-screenshot-driven, each one real and source-verified before shipping:
- BUG-2608-074: illustration panel used --afa-surface-page (page bg) instead of --afa-surface-raised (elevated-panel token) — panel had zero visual distinction, tier-label badge looked like a stray floating label.
- BUG-2608-075: Follow/Get Directions were stacked left-aligned instead of a flex row with actions right, level with the title block.
- BUG-2608-076: Get Directions was a bare text link, no border/padding — rebuilt to match Follow's button chrome.
- BUG-2608-077: the actual illustration artwork (Intimate/MidHall/LargeArena marks) was a hand-drawn "interpretation," not a real port — different viewBox, different scene entirely, no procedural per-venue variation. Wholesale-ported real path data + the export's seed-based variation (pendant lamp vs spotlight, chair/row/tier counts) from VenueMedia.tsx. This was the actual root cause behind three earlier "fixed but still doesn't look right" rounds — property patches on a redrawn component don't converge no matter how many individually-correct values you throw at it.
- BUG-2608-078: detail-hero box was 16:9 live, export specifies 16:7. Single-value fix.
All confirmed via real quoted export lines (not screenshots-only) once BUG-2608-072's original brief was shown to have missed things too. Figma MCP could not pull raw source for either Figma Make export this session (Venues, Events, Artists all Make-mode files) — every real value came from targeted Claude Code reads of the local export folders, not the Figma connector.

GEN-2608-075: full exhaustive Venues fidelity audit (Claude Code), 14 previously-uncaught gaps beyond the original BUG-2608-072 brief — hero H1 half the spec'd size, detail-page name wrongly italic/fixed-32px, card tier badge missing on real-photo cards, gallery only rendering one photo, native <select> city filter, directory <main> bg colliding with card fill token, sharp-corner holdouts, missing table headers, dead tab-hover state. Honest gap flagged by Claude Code and passed through, not silently checked off: sandbox DATABASE_URL was a placeholder, so DB-backed views couldn't be screenshot-diffed locally — only hero/toggle/empty-state got a real visual check.

GEN-2608-076: no-photo panel was missing a grid/graph-paper texture (real export value: two 1px cream linear-gradients, 22px cells, 4% opacity) and the card icon was shrunk (82%/220px cap) vs the export's full-bleed treatment. Fixed both, scoped to card only since hero wasn't verified at the time (closed out later — see BUG-2608-077 above).

GEN-2608-077: Events Directory + Event Detail rebuilt wholesale from the approved "Event management app" Figma Make export (zip supplied directly by Hitesh, reviewed live — real bug fix, not just visual: posterImage was never rendered anywhere on either page, pages relied entirely on emoji badges). Field-mapped against real schema.prisma, not the export's mock data (facilities come from event.venue.facilities, hype score is computeHypeScore() server-side, seats are totalSeats/availableSeats via existing getAvailabilityStatus()). Old Overview/Lineup/Venue tab switcher replaced with one flowing scroll per the export — verified terms/refund policy/plus-ones/reviews/competition-voting all still present, just relocated, not dropped. One real bug found and NOT ported: the export's own .duotone CSS (mix-blend-mode: lighten against amber) produces a salmon/coral cast on real photos — confirmed by cross-referencing which mock events had posterImage set vs null. Reused the codebase's already-verified Photo.tsx duotone instead.

BUG-2608-079: cross-cutting bug, not a one-off. Photo.tsx had zero error handling — a DB-stored image URL that 404s/times out (confirmed live: qa-general-event-04's Unsplash posterImage) rendered a broken, semi-transparent card instead of falling back to the surface's illustrated placeholder. Affected all 7 real call sites (Events, Venues x2, Artists x2, homepage bento, PhotoCrossfadeBackdrop) — fixed in all 7, each swapping to its own already-existing fallback. Real sub-bug found mid-fix: React's synthetic onError on <img> never fires in this project's Next.js/React setup (confirmed against AGENTS.md's own "this is NOT the Next.js you know" warning, not a Photo.tsx-specific bug) — worked around via a native DOM listener attached through ref.

Artists directory hero: Hitesh flagged it's the odd one out vs Events/Venues (centered, rounded corners, vs their left-aligned sharp-corner convention). Checked docs/design.md — GEN-2608-073's original approval was screenshot-only, raw export never pulled, unlike Venues/Events. Hitesh supplied the real export zip ("Discover Artists Directory Page"). Confirmed real gap by reading the actual export (left-aligned eyebrow/headline/subtitle grid + separate bordered stat+search row below, sharp corners) vs current live code (centered, maxWidth 800px, rounded-10px search box). Claude Code prompt sent, scoped to the hero only, explicitly told to preserve BrowseSearchDropdown's real functionality and add real i18n across all 11 locales, not just restyle by eye. NOT YET BUILT — prompt was the last thing sent this session, no response received yet. Pick this up first next session.

PRs merged this session: #506–#515 (10 PRs). qa HEAD progressed 8158f97 → 3885b34.

Not done, carry forward:

Artists directory hero left-align — Claude Code prompt sent, response not yet received. First thing to check next session.
GEN-2608-073 — Artist directory cards (real-photo ones) read as too basic/ordinary. Still NEW, no design direction agreed yet. Needs a real conversation before building, not another guess.
The empty-state design gap surfaced mid-session (queried, never resolved): 1,140 approved venues in QA, only 22 have facilities data, only 1 has a seat map. The "sparse" view is the default 99% of real venues will show, and it was never designed or Figma-audited — the whole Venues fidelity round matched Prithvi Theatre's fully-populated mock data. Hitesh hasn't picked a direction yet: (a) design a real empty/sparse state as its own small round, or (b) treat it as a data problem and push on owner-side facility/seatmap entry. Raise again at session start.
Owner card city field — export shows a city row with no backing data anywhere (VenueOwner has no city field in schema or API). Needs Hitesh's call: add the real field, or confirm permanently out of scope. Carried from prior session, still untouched.
Figma MCP could not pull raw source for any Make-mode file this session (Venues, Events, Artists all failed identically — connector returns a manifest of resource links, not fetchable content in this environment). Every real fix this session came from Claude Code's local file access instead. Worth flagging to Anthropic/checking if this is fixable, since it's now a load-bearing gap in the chat-side workflow.
Whether the Shegaon Mic Night Unsplash posterImage URL is genuinely dead (vs just blocked in these sandboxes) was flagged but not checked — worth a real look now that BUG-2608-079's fallback makes it non-urgent either way.
Fresh GitHub PAT will not persist — request new one next session. (This session's worked; the first one pasted was rejected as bad credentials — worth double-checking the copy next time to save a round trip.)
Razorpay / Google Maps billing dashboards — flagged repeatedly across many sessions now, still unconfirmed. Raising again, bold, at next session start per protocol.
Standing untouched items carried from before this session, no new info: seat-map narrower gaps, TWA/mobile redesign, K2 payout ledger, Admin Dashboard UI, BUG-2608-055, CA/TDS gate, language click-testing, GEN-2608-040, Artist Reputation epic click-testing, QST-2607-009 Google Sign-In, seed59-prod-tls-risk.

The recurring lesson from last session held again this session and got reinforced further: three of the eight fixes this session (BUG-2608-074/075/076) were all on the same single component (VenueDetailClient header) and each one was "fixed" in isolation without widening the check to the rest of the element already open in front of me. The actual break came from routing repeat-miss patterns to Claude Code for a real source-verified pass (BUG-2608-077's wholesale artwork port, GEN-2608-075's full audit) rather than continuing to patch one property at a time from a screenshot. When something looks "off but you can't say exactly why" after two rounds of targeted fixes, that's the signal to stop guessing and get real source access — not to try a third targeted guess.
