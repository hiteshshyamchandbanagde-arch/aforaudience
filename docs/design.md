# AforAudience — Master Design Document v3.5
### The World's First Live Art Universe — Consolidated Product, Engineering & Delivery Plan
**Status:** Living document | **Supersedes:** onboarding sections of the original Web & Mobile design docs
**Working model:** Solo founder-developer + Claude acting as Architect / Senior Developer / QA collaborator

---

## 0. Document Purpose & What Changed

This document merges four prior artifacts into one source of truth:
- `AforAudience_Design_Document.md` (web product & business)
- `AforAudience_Mobile_App_Design_Document.md` (React Native app)
- `AforAudience_Revised_Onboarding_Flow.md` (browse-first access model)
- `AforAudience_Web_Login_Test_Plan.md` (QA gaps on auth)

**Ruling made this chapter:** Browse-first / guest-mode / login-at-commitment (from the Revised Onboarding doc) is now the **single standard for web and mobile**, replacing the old "Role Selection → Register → OTP → Profile Setup" flow everywhere it previously appeared, including the Mobile doc's §4/§5.3 and the Web doc's Register flow. Anywhere the two conflict, this document wins.

Since delivery is one person working with Claude as collaborator (not a multi-role team), the Agile plan in §10 is deliberately built for a **solo-developer + AI-pair cadence**, not a staffed sprint team — story points here are for prioritization and scope-sizing, not velocity commitments against a hiring plan.

**Amendment (this revision):** Added §4.5 — Venue Owner sets rental rates (Hourly/Daily/Flexible) and ticket pricing is now per-section, controlled by the Organiser, who also sets performer compensation (paid/free), performer count caps, and an approval-gate toggle for Artist applications. This pulled some Venue OS stories forward into Release 1 (see §8.2) since publishing an event now depends on a confirmed venue booking.

**Second amendment (this revision):** Ruled on the three open money decisions flagged in §4.5 — (1) auto-approval requires `isVerified` only, (2) venue rental is taxed via a **flat platform booking fee**, not a percentage, and (3) **both `PAID` and `BUY_IN` performer slots are commissioned**, which means `PAID` fees now settle as a real platform payout at T+2 rather than an off-platform promise — a materially bigger build than first scoped. A new open question follows directly from that: whether an Organiser needs a revenue check or guarantee deposit to cover promised `PAID` fees (§4.5, suggestion 9) — not yet decided.

**Third amendment — Implementation session, consolidated:** Release 0 shipped and is live in production, verified end-to-end with real accounts (not just code review): registration → login → password reset/lockout → contextual auth sheet → role upgrade (Organiser/Venue Owner) → Admin approval → venue creation, all walked through on the real deployed site. Also shipped from Release 1: the full economics schema foundation (applied live), per-section ticket pricing on event creation (E6), venue rental rate with Hourly/Daily/Flexible + day-of-week overrides (F4), the fix preventing an event from going live without venue booking confirmation, and showing/calculating the venue's rate at booking time for Hourly/Daily venues (Flexible intentionally left as a proposed-amount placeholder pending real negotiation UI — see §9.1). Three real bugs were found through actual testing (not caught in review) and fixed same-session — see §9 for the full list of what's shipped vs. still open, replacing the informal §9 notes from the design phase.

**Infra note, worth keeping close at hand:** `DATABASE_URL` must be Supabase's **Session Pooler** connection string (port `5432` on the `pooler.supabase.com` host), set for **both Production and Preview** environments in Vercel — Transaction pooler (port `6543`) doesn't reliably support the queries Prisma's migration tooling runs, and Direct connection is IPv6-only so Vercel can't reach it at all. Schema migrations are applied directly against Supabase via the Supabase MCP connector as their own explicit step, not through the Vercel build (the build script is plain `next build` — an earlier attempt to wire `prisma migrate deploy` into it caused a cascade of failed deploys and was reverted). Both the Vercel and Supabase MCP connectors are connected and used for verifying real deployments and querying the live database directly, rather than assuming from code alone.

**Fourth amendment — Follow-up session, consolidated:** Shipped Follow (G1) and Review (G2) — both genuinely from-scratch, not schema-only gaps like the performer economics cluster was. Shipped F3 (Venue Owner revenue summary + a lightweight custom month calendar). Ran a real security audit: found and fixed six routes that checked ownership but never checked `isApproved` (event edit, venue edit, venue booking request/confirm, both sides of Flexible negotiation, application approval) — only two routes had ever been checked before this pass. Also narrowed the TLS bypass in `src/lib/prisma.ts` — removed a **global** `NODE_TLS_REJECT_UNAUTHORIZED=0` that disabled certificate verification for every outbound connection the app makes (not just the database), leaving only the Postgres-specific bypass in place since it couldn't be safely verified from the sandbox. Confirmed via real production traffic (login, a real booking write, event pages) that the database connection survived the change.

Also fixed a real, previously-undiagnosed mobile bug: **no viewport meta tag existed anywhere in the app.** This was the actual root cause of the mobile layout appearing cropped rather than reflowing — a separate, deeper issue than the SiteNav hamburger-menu fix that shipped alongside it, and the reason that fix couldn't have worked on a real phone until this landed too. Once fixed, a systematic search for the same "fixed-width flex/grid" overflow pattern found and fixed three more real bugs: the homepage's stats row, Navarasa grid, and footer; and the event detail (booking) and artist profile pages' fixed-pixel sidebar columns (`1fr 380px` / `1fr 320px`) — the event detail one is very likely what was reported as "booking has issues" on mobile.

**Process note, worth reading if you're picking this up in a new conversation:** during this session, two pieces of work were built twice — once now-forgotten from earlier in this same long conversation, and rediscovered independently later without realizing it already existed (a role-upgrade/admin-approval flow, and a venue revenue/calendar view). Both times it looked like "another parallel collaborator" had done the work, but the user confirmed only one session was ever running — it was actually this same conversation's own earlier turns, no longer visible due to context-window limits. Cleanup: ~27 stale branches (mostly already-merged, never deleted after merging via API instead of GitHub's UI) were removed from the repo; only `main` remains. **If you're a fresh Claude picking this up:** check `main` and this doc's §9 for what's actually built before assuming a story needs starting from scratch — the doc is the reliable source of truth, not any single conversation's memory of itself.

**Fifth amendment — Economics model rewritten to match the published About page.** The About page shipped to `qa.aforaudience.com/about` commits the platform, in writing, to a specific promise: **"We will never tax the scene."** Not the venue owner. Not the organiser. Not the artist. Not the rental between a venue and an organiser. Not the fee an organiser pays a performer. That promise is the moat, and it now supersedes every prior ruling in this doc about how to commission which flow. §4.5 has been rewritten from the ground up to reflect this; §3's open-decisions table has retired the two "commission on X" rulings; the affected EPIC stories (E7, E11, E14, H4) have been reworded; and §9 has been updated to reflect what's dead, what stays meaningful in a new framing, and what's newly on the roadmap. The two prior commission rulings (second amendment) are formally reversed. Implementation-side confirmation added in sixth amendment: `PlatformSettings.ticketCommissionRate`, `performerSlotCommissionRate`, `flatVenueBookingFee` still exist as columns, are marked `@deprecated` in `schema.prisma` with JSDoc annotations, and are never read by application code. `audienceBookingFee` was added as a new column on the same table (not a rename of `flatVenueBookingFee`), preserving audit trail. **The About page is now the pricing constitution; §4.5 implements it. If they disagree, the About page wins.**

**Sixth amendment — QA shipping session, consolidated (13 Jul 2026).** Four PRs merged to `qa` in a single working day, taking Release 1 from ~20% shipped to mostly shipped. Checkpoint 2 (Razorpay payment integration) went end-to-end with a real card in QA: SDK wrapper, POST `/api/bookings` creates a PENDING booking in a Prisma transaction then a Razorpay order outside it (dodges the 5-second transaction timeout), redirects to `/checkout/[bookingId]`, verifies HMAC signature via confirm endpoint, transitions to CONFIRMED. Webhook at POST `/api/payments/webhook` handles browser-death race conditions. Free events auto-confirm immediately. Payment amounts stored in paise as integers, 15-minute PENDING TTL. Checkpoint 3 (ticket delivery) shipped PDF generation via pdf-lib (A4 editorial-tone), QR code encoding the booking ID (physically scan-verified), Resend email delivery with PDF attachment from `tickets@mail.aforaudience.com`, atomic single-flight delivery, on-demand ticket download endpoint, Download buttons on checkout success page and `/tickets` list. Checkpoint 4 (audience booking fee, EPIC K1) shipped end-to-end: fee configurable at `/dashboard/admin/settings` (ceiling ₹500, ships at ₹0 default), captured to `Booking.bookingFeeAmount`, visible as a separate line item at checkout. Fix B (User Display Name, new retrospective EPIC L) shipped: `User.displayName` separate from `User.name` (username), register form persists Full Name as displayName, Profile page lets existing users set/edit, tickets/emails/greetings use displayName with fallback to name. QA env: separate Supabase project (`aforaudience-qa`), qa branch auto-deploys to `qa.aforaudience.com` via a second Vercel project, prod stays on main → `www.aforaudience.com`. Env-label badge (`NEXT_PUBLIC_ENV_LABEL`) distinguishes Beta v1 (prod) from QA (preview). Transactional email working in QA via Resend with `mail.aforaudience.com` as the dedicated sending subdomain, DMARC monitoring in place. Real bug caught only through live testing and not code review: **free events auto-confirm but never receive PDF/email** — recorded as EPIC M for the next session (now shipped, see seventh amendment).

**Seventh amendment — This session (14 Jul 2026).** Two things shipped, both small: (1) EPIC M1 — the free-event ticket-delivery gap from the sixth amendment. Root cause: the atomic single-flight claim in `deliverTicket()` ran on `Payment.updateMany WHERE deliveredAt IS NULL`; free events have no Payment row, so the claim always affected 0 rows and delivery silently no-op'd. Additionally the free-event auto-confirm path in POST `/api/bookings` never called `deliverTicket()` in the first place. Fix: moved the claim from `Payment.deliveredAt` to `Booking.deliveredAt` so it works for both flows, wired the free-event auto-confirm path to fire `deliverTicket()` background-style (same pattern as the paid confirm route). `Payment.deliveredAt` / `Payment.deliveryError` are kept in the schema marked `@deprecated` to preserve historical audit trail; existing paid bookings' delivered state was backfilled onto Booking at migration time so any future admin-retry pathway doesn't re-fire them. Migration `20260714000000_ticket_delivery_on_booking` applied to `aforaudience-qa` before the code deploy (forward-compatible ordering). (2) This design document was consolidated into the repo as `docs/design.md` — previously it lived in project files and drifted between chat sessions. Going forward, doc edits go through PRs like any other code change; the doc is versioned with the code that implements it. Also this session: established the discipline that handoffs should be ≤60 lines (previous 300+ line format was burning 30–40% of every subsequent session's context budget for no useful gain), and that the design doc lives in the repo rather than in project files (the old habit was loading the entire 445-line doc into every message regardless of relevance).

**Eighth amendment — Same 14 Jul 2026 session, continued.** With Checkpoints 2–4 all shipped and the M1 free-event fix landed, the session had headroom for smaller loop-closers and mobile groundwork. Six PRs merged in this session total (#39 free-event delivery, #40 design doc consolidation, #41 PWA installable, #42 PWA score improvements, #43 admin retry endpoint, #44 displayName nudge). Notable landings: (1) **PWA — the app is installable on Android.** `src/app/manifest.ts` completed with maskable icons + shortcuts (Events/Tickets/Venues) + `lang`/`dir`; hand-written `public/sw.js` (~150 lines, no next-pwa/workbox dependency) implements network-first-with-offline-fallback for navigations, cache-first for `_next/static/`, stale-while-revalidate for `_next/image/`, and never-cache for `/api/`; a brand-consistent `public/offline.html`; inline `<script>` in the layout `<head>` registers the SW (moved from React client component after learning that PWA validators are static scanners that don't wait for hydration); a `beforeinstallprompt` interceptor renders a subtle install CTA with a 14-day dismissal window; brand icons at 192/512/maskable/apple/favicon sizes generated programmatically by `scripts/gen-pwa-icons.py` so future rebrands are reproducible in one command. Vercel Deployment Protection turned off across the project so PWABuilder.com could validate the manifest. **PWABuilder generated a working Android APK** (package ID `com.aforaudience.qa.twa`, throwaway signing key) — under the hood it uses Google's Bubblewrap tool. The APK was downloaded to Hitesh's phone but not yet installed; assetlinks.json setup is the next step to hide the URL bar in the TWA. (2) **EPIC M1's follow-up — admin retry endpoint.** `POST /api/admin/redeliver-ticket/[bookingId]` (ADMIN role required) atomically resets `Booking.deliveredAt`/`deliveryError` under a two-branch WHERE (either "previous attempt failed" or "delivered >30s ago so force-resend is safe"), then fires `deliverTicket()` background-style. 30-second cooldown protects against admin double-tap and against races with in-flight deliveries from the confirm/webhook path. Closes the loop `Booking.deliveryError` opened. No UI yet — Hitesh calls it via curl until an admin bookings list exists. (3) **EPIC L5 — displayName backfill nudge.** Subtle sticky top banner in brand-warm palette appears when a logged-in user has null `displayName`, links to `/profile`, dismissible for 14 days, self-hides on `/auth`, `/checkout`, `/api`, `/admin`. Deliberately no soft-gate on booking — respects the "browse-first, never block" line from §2. Prod path for the mobile app is now clearly documented in §11: same PWABuilder workflow with package ID `com.aforaudience.app` (reserved, not yet used) and a permanent signing key — Hitesh will run that separately when Razorpay live keys land and real Play Store submission is ready.

**Ninth amendment — Late 14 Jul 2026 session, evening/night continuation.** Four more PRs merged after the eighth amendment (#46 login/username fixes, #47–#49 support widget cluster, #50 assetlinks), taking the same day's total to ten. **(1) Login bug caught through real usage** and DB-verified before fixing: bare 10-digit phone numbers failed silently on login because the auth path did exact-string matching against phone values that were stored `+91`-prefixed. All 14 QA users were affected. Bundled username case-insensitivity in the same PR since it was a one-line touch of the same call site. **(2) Support widget cluster — a bounded FAQ chatbot + a feedback form + an admin review page — shipped as three sibling PRs.** The chatbot at `POST /api/chat` uses `claude-haiku-4-5-20251001`, is aggressively bounded: it answers **only** from `src/lib/site-knowledge.ts` (a hand-curated knowledge base of AforAudience-specific facts), is instructed to refuse anything off-topic, and specifically never invents refund or fee policy — those are still open business decisions (§9.5), not something a chatbot should hallucinate. The chat surface is admin-configurable via a new `PlatformSettings.chatMaxMessagesPerSession` (default 15, `0` = kill-switch so admin can disable the feature without a deploy if it misbehaves), enforced client-side and re-validated server-side. Chat access stays open to everyone (guest + authenticated) — an earlier suggestion to gate it to paid bookers was pushed back on, the admin-configurable session cap being the safer control surface than a paywall. `POST /api/feedback` is guest-accessible with an optional screenshot attachment stored as a base64 data URL directly in `Feedback.attachmentData` for MVP simplicity (client resizes to well under 1MB before encoding; migrate to Vercel Blob if attachments become heavily used — see 9.5-adjacent housekeeping). The admin review page at `/dashboard/admin/feedback` is a real page, not just an API — lists feedback, filters by status, marks resolved. **(3) Prompt-caching reverted mid-ship:** the installed `@anthropic-ai/sdk` types don't expose `cache_control` on the plain `messages.create()` shape used in this codebase, even though the API supports it. Shipped uncached (~₹0.30/turn vs. ~₹0.11 cached — still negligible against the platform's economics) with a source-code note to revisit if the SDK types catch up. **(4) `public/.well-known/assetlinks.json`** — the QA APK from the eighth amendment was showing an address bar on launch, and that bug was caught **through the newly-shipped feedback widget itself**, submitted by Hitesh. Fingerprint recorded, file served at the well-known path, browser Digital Asset Links verification now passes. Note that Android caches this verification aggressively — a full uninstall/reinstall is needed to re-verify on the same device. Prod path in §11 remains valid: add the prod fingerprint as a **second** entry in `assetlinks.json` when the prod APK is built, do not replace QA's.

**Tenth amendment — 15 Jul 2026 morning session.** Two PRs merged (#52 F6, #53 RLS), plus this doc update. **(1) F6 — VenueBooking rate snapshot fully wired.** The schema comment on `VenueBooking` (§4.5) had claimed since the fifth amendment that `agreedRateType`/`durationHours`/`durationDays`/`platformFeeAmount` were "snapshotted at confirmation time so later changes to the Venue's published rates don't retroactively alter a confirmed booking." Reading the actual confirm code path (`PATCH /api/venue-bookings/[id]`) turned up two real gaps: (a) the **actual rate values** (`Venue.hourlyRate`, `Venue.dailyRate`) were never snapshotted onto the booking, only the derived shape — so a Venue Owner editing their published rate today would retroactively shift the effective rate of every already-confirmed HOURLY/DAILY booking against that venue; and (b) even the four fields that *were* meant to be snapshotted were being silently left `null` on the PENDING → CONFIRMED transition, because the confirm handler was writing `{ status }` only. The QA data made the bug visible in real terms: three CONFIRMED bookings existed against real venues, and their `amount` fields didn't cleanly divide by the *current* venue rates (e.g. The Vintage Club at hourlyRate 1000 had a 2500 booking → 2.5 hrs, non-integer, meaning the venue rate has changed since confirmation and we now have no way to know what it was). Migration `20260715000000_venue_booking_rate_snapshot` added `snapshotHourlyRate` and `snapshotDailyRate` (both `Float?`, nullable and additive, no backfill — writing today's Venue rates onto historical bookings would fabricate history worse than leaving null). Confirm handler now snapshots rate + duration + rate type on the PENDING → CONFIRMED transition for HOURLY/DAILY venues, with an idempotence guard (`agreedRateType === null`) so flipping status back and forth doesn't overwrite the original capture. FLEXIBLE flow untouched — it goes through the `VenueBookingRequest` negotiation loop and already snapshots what applies there; there is no published rate for FLEXIBLE, `amount` IS the negotiated deal. §9.3 updated: F6 moves off the "backend ready, waiting on frontend" list — the frontend didn't need a change, the backend just wasn't finished. **(2) RLS on `Payment` and `Feedback` — deny-by-default.** Supabase's security advisor flagged both tables as RLS-disabled, meaning anyone holding the anon key could construct a Supabase client and read/modify every row (Razorpay IDs and amounts on `Payment`; guest bug reports and attached screenshots on `Feedback`). Access-pattern audit before deciding the shape: **zero uses of `@supabase/supabase-js` anywhere in `src/`** (verified via grep across `.ts` and `.tsx`), all reads/writes go through Prisma against `DATABASE_URL` → Session Pooler as the `postgres` role, which has `BYPASSRLS`. Given that: `ENABLE ROW LEVEL SECURITY` on both tables with **no policies** is the correct default — zero blast radius on the app (Prisma continues unchanged), while the entire anon/authenticated JWT read path against these tables is killed. `FORCE ROW LEVEL SECURITY` intentionally not applied (`FORCE` would subject the postgres role to RLS too, breaking Prisma). If a future feature ever needs client-side reads on these tables, a targeted `CREATE POLICY` scoped to the specific authenticated user should be added in that PR — not by loosening this deny-by-default. Both migrations applied to QA Supabase before the code deploys, verified via `information_schema` and `pg_class`. Prod Supabase is now two migrations behind QA; that gap closes on the next `qa → main` promotion.

**Eleventh amendment — 17 Jul 2026 session.** Three bug-fix PRs merged (#58 homepage marquee loop, #59 mobile events-filter/sort-dropdown misalignment, #60 server-side email format validation), plus doc cleanup. **(1) Marquee loop (#58):** homepage ticker array was 7 unique items followed by only the first 3 repeated (10 total, not a clean duplicate) — the `translateX(-50%)` keyframe only loops seamlessly when the two rendered halves are identical, so the mismatch caused a visible jump each cycle. Fixed by rendering the 7 unique items duplicated exactly once. A residual jank concern (raised, not confirmed) was parked as low-impact/no clear repro rather than chased further. **(2) Mobile filter-bar misalignment (#59):** the events-page filters row (type pills, city select, price pills, view toggle) had no responsive layout at all, unlike `SiteNav` which already has a 780px breakpoint — on narrow viewports the row wrapped unpredictably and a fixed-height divider floated misaligned against whatever wrapped next to it. Fixed with an explicit mobile layout at the same 780px breakpoint: column stack, divider hidden, select full-width. **(3) Server-side email validation (#60):** registration only checked email was non-empty and unique; format was client-side only (`<input type="email">`, bypassable via autofill/disabled-JS/direct API call). Two real QA users had malformed emails (`amolgokhe893gmail.com`, `amolgokhe.com`) live in the database, confirmed via direct query before fixing — not hypothetical. New `src/lib/validation.ts` with `isValidEmailFormat()`, wired into `POST /api/auth/register`. Scope-checked first: register is the only endpoint that writes a new email (`users/me` only reads it, `forgot-password` only looks one up, both harmless as-is). Already-malformed DB rows deliberately left untouched — a data decision, not folded into the fix. **(4) Phone-verification gap found, not fixed this session:** investigating #60 surfaced that neither password login nor either booking route checks `User.isVerified` — an account can skip phone-OTP entirely and still book/pay for a real ticket. A booking-gate fix was drafted and then deliberately reverted before shipping, because no UI exists for a user to complete phone verification after the immediate post-registration screen — shipping the gate alone would strand legitimate unverified users at checkout with no way out. Logged properly as backlog (§9.1 #1, §9.2) rather than rushed. Confirmed as a non-issue in the same investigation: email verification is already correctly non-blocking by design (`verify-email/route.ts` deliberately never touches `isVerified`), so no work needed there. **(5) Doc hygiene:** found and fixed stale status markers from the tenth amendment that never got updated when F6 shipped — the epics table (§6) still said "Not started", the backlog-order snapshot (§8.2) still listed F6 (and L5, also shipped in the eighth amendment) as remaining work, and §9.1's "next up" list still carried F6 and `assetlinks.json` as unblocked work despite both having shipped in the tenth and ninth amendments respectively.



## 1. Vision, Brand, Personas (carried forward, unchanged)

**Vision:** The first unified live-performance-art ecosystem — comedians, poets, musicians, dancers, organisers, venues, and audiences on one platform.
**Tagline:** "Where Art Finds Its Crowd"
**Differentiators:** Navarasa search (9 emotions of Indian classical art as an event filter — no competitor has this), a portable global Artist reputation layer (Hype Score, reviews, history), and a Venue OS.

| Role | Core Job To Be Done |
|---|---|
| Audience | Discover → attend → rate/tip |
| Artist | Get discovered, get booked, build reputation |
| Organiser | Run events end-to-end |
| Venue Owner | Monetize space, manage calendar |
| Admin | Approvals, trust, platform health |

Brand colors, typography, and design principles are unchanged from the original Design Document (§2) and apply identically to web and mobile.

---

## 2. The Access Model — Browse-First (New Standard, All Platforms)

### 2.1 Principle
**Discovery is the product. Login is a checkpoint, not a gate.** Anyone — logged in or not — browses events, artists, and venues immediately. Login is requested only at the moment an action requires identity.

### 2.2 Unified Flow (Web + Mobile)
```
Splash / Landing (brand only, <2s)
  → City Detection/Selection (no account)
  → Home — Guest Mode, full browsing
  → [Guest browses Events / Artists / Venues / Search / Navarasa filters freely]
  → Guest attempts a gated action
  → Contextual login prompt (bottom sheet on mobile / modal on web)
  → Phone OTP or Google/Apple one-tap (web: email/password + same social options)
  → Resumes exact action — no redirect to Home
```

No role picker at signup. **Audience is the default identity for everyone.** Artist / Organiser / Venue Owner become **opt-in upgrades** from Profile → "Become an Organiser / Apply as Artist / List Your Venue," each routed through its own lightweight application + admin approval flow (unchanged from Design Doc §4.5).

### 2.3 Gated Actions (applies to both platforms)
Book ticket · Follow artist · Tip artist · Rate/review · Save/wishlist · View My Tickets · Post an event · Apply as Artist/Organiser/Venue.

### 2.4 What This Replaces
- **Mobile Design Doc:** delete "Role Selection Screen" (§5.3) and the Role Selection step in the Onboarding Flow (§4) and Figma checklist. Auth screens (`login.tsx`, `register.tsx`, `otp.tsx`) remain but are invoked contextually, not as a forced first-run sequence.
- **Web Design Doc:** `/register` no longer collects a role at signup. Role selection UI and its associated test cases (2.1–2.4, 2.12 in the QA plan) are retired in favor of the opt-in upgrade flow.

---

## 3. Open Product Decisions — Resolved This Chapter

The QA test plan flagged five undocumented decisions. Ruling on each so stories/estimates aren't blocked:

| Decision | Ruling |
|---|---|
| Forgot/reset password | **In scope for MVP.** Email-based reset link, 1-hour token expiry. New epic (see EPIC-B). |
| Email verification (`isVerified`) | **Soft-gate.** Unverified users can browse and book (guest-equivalent), but cannot post events, apply as Artist, or receive payouts until verified. |
| Pending Organiser/Venue Owner login | **Login succeeds**, dashboard shows a "Pending Approval" banner/state instead of full functionality (matches QA case 5.4's better-UX branch). |
| Rate limiting / lockout | **In scope.** 5 failed attempts → 15-minute lockout per account, generic error message (no enumeration). |
| Session/token policy | **Web:** NextAuth JWT, 7-day expiry, refresh on activity. **Mobile:** SecureStore JWT, 30-day expiry, biometric re-auth optional (matches Mobile doc §12 already). |
| Auto-approval floor for performer applications | **Require `isVerified`** on the applying Artist account. No Hype Score threshold for now. |
| Commission on venue rental | **None. Ever.** Venue rental is entirely between Venue Owner and Organiser; platform takes zero from it. Reversed from second amendment per fifth amendment. |
| Commission on performer slots (`PAID` / `BUY_IN`) | **None. Ever.** Performer compensation is entirely between Organiser and Artist; platform takes zero from either direction. Reversed from second amendment per fifth amendment. |
| Commission on ticket sales | **None.** Platform does not take a percentage of the ticket price. See "Platform revenue model" below. |
| Platform revenue model | **Audience-side booking fee only for MVP** — a small flat fee (e.g., ₹10–15) added to each ticket at checkout, framed transparently as "supports the artist ecosystem." No supply-side charges. Post-MVP revenue arrives in phases per §4.5: optional Pro tiers for Venues/Organisers, featured placements, then livestream / digital merch cuts on features the platform uniquely provides. The core artist ↔ organiser ↔ venue ↔ audience transaction remains untaxed forever. |

---

## 4. Consolidated Feature Set by Phase

Reconciling the Design Doc roadmap (§15) with the Mobile doc's phases into one backlog view:

| Phase | Web | Mobile | Notes |
|---|---|---|---|
| **MVP (P1)** | Guest browse, contextual auth, booking, Razorpay, PDF tickets, notifications | Guest browse, contextual auth, booking, Razorpay, My Tickets | Mobile launches with web feature parity first — no exclusive features yet |
| **P2 — Growth** | Venue OS, Admin panel, reviews, Navarasa search, Art Passport | Same + push notifications, dashboards | |
| **P3 — Intelligence** | AI Matchmaker, mood search, livestream, tip wallet, payouts | Tip Artist, Reels tab, Backstage Pass | |
| **P4 — Scale / Exclusive** | NFT tickets, digital merch, multi-city | Shake to Discover, AR Venue Preview, QR check-in, Live Reactions, Pre-show Chat, offline tickets, Wallet integration | App-exclusive features deliberately land *after* core parity, matching Mobile doc §13 phasing |

---

## 4.5 Economics Model — Rewritten Per the About Page Promise

**Governing principle (from the About page, verbatim):** *"We will never tax the scene. Not the venue owner. Not the organiser. Not the artist. Not the rental between a venue and an organiser. Not the fee an organiser pays a performer. None of it."*

Every design decision in this section flows from that. Where the old §4.5 asked "how do we tax which flow," this §4.5 asks "how do we sustain a real business without taxing any of them."

### The three money flows on the platform (who pays whom)

There are three distinct money flows around a live event, and each stays entirely between its participants — the platform is inside exactly one of them, on the audience side.

**Flow 1 — Venue rental (Venue Owner ↔ Organiser).** The Venue Owner sets how their space is rented (Hourly, Daily, or Flexible-quote). The Organiser pays that amount directly to the Venue Owner. **Platform takes zero.** The rate model, the negotiation flow, the confirmed-booking-required-before-publish rule — all preserved from the old §4.5. Only what's removed: any "platform fee added on top."

**Flow 2 — Performer compensation (Organiser ↔ Artist).** For each lineup slot, the Organiser marks it `PAID` (Organiser owes the Artist a fee), `FREE` (exposure/open-mic, no money either way), or `BUY_IN` (Artist pays Organiser a spot fee — common pay-to-play open-mic model). **Platform takes zero from either direction.** This compensation is settled between Organiser and Artist off-platform for MVP (the way it already is today in the scene — WhatsApp/UPI/cash). The platform records the *promise* (compensation type and amount, so both sides have a clear record), but does not process the payment or take a cut. A future release may offer optional in-app payment facilitation as a convenience — and even then, still with zero platform fee.

**Flow 3 — Ticket sales (Audience → Organiser).** The audience buys tickets; the money goes to the Organiser (minus the small audience-side booking fee described below). **Platform takes zero commission on the ticket price itself.** The Organiser sets per-section ticket pricing exactly as before (via `TicketTier`). This is the only flow the platform is in the middle of, and the only one that has any platform charge — and that charge is on the *audience*, not the Organiser, framed transparently.

### Where platform revenue comes from — phased introduction

Aligned to the About page's promise "we will earn our right to charge before we charge." Rough sequencing keyed to platform maturity and runway (not calendar months):

**Phase A — MVP (from Day One):**
- **Audience booking fee** — a small flat fee (e.g., ₹10–15) added to each ticket at checkout. Shown as a separate line item; framed as *"supports the artist ecosystem — keeps the platform free for artists and venues."* Uses the ticket-processing pipeline (Razorpay Route or similar) so the fee is captured before the rest of the ticket amount is passed through to the Organiser. **Checkpoint 4 is complete (sixth amendment).** Ships at ₹0 by default; admin flips it on at `/dashboard/admin/settings`. Ceiling ₹500 (50000 paise) enforced in setter as a typo-guard.
- **Optional "support the platform" tip at checkout** — a small opt-in "buy the platform a coffee" widget with a couple of preset amounts. Zero pressure, but historically works better than founders expect.
- Everything else free.

**Phase B — After real supply-side traction (once venues/organisers are actively using the platform and asking for more):**
- **Venue Pro** — monthly subscription for advanced features Venue Owners have actually asked for (calendar sync, analytics, priority search, verified badge, etc.). Free tier stays fully functional; Pro is genuinely more, not free-with-crippling.
- **Featured event/venue/artist placements** — small pay-to-promote slots with clear guardrails around search integrity.

**Phase C — After Venue Pro is proven:**
- **Organiser Pro** — subscription for real-time sales dashboards, lineup builder, audience insights, AI matchmaker access.
- **Artist Premium** — small subscription for Rising Star rotation, verified badge, application boost. Free artist profiles remain first-class.
- **Verified badges, priority approval, white-label event branding** — small one-time value-added services.

**Phase D — After the platform has built genuinely new capability:**
- **Livestream revenue share (~15%)** on the pay-per-view livestream feature — platform built the streaming infrastructure, so a share of the revenue *from that specific feature* is earned. This does not violate the "never tax the scene" promise because the platform is providing entirely new value the scene didn't have before, not taxing a transaction that already existed.
- **Digital merch marketplace cut (~10%)** — same reasoning; platform-built marketplace, platform earns a cut on merch it uniquely enables.
- **Small ticket platform fee** may be considered here (2–3%), transparently framed, only after the platform is delivering enough scale-level value to defend it. **This is a "maybe later" — the default is to never introduce it and stay funded by Phases A–C indefinitely.**

The whole point of the phased plan: every revenue stream is either (a) on the audience side (booking fee, tip), (b) optional Pro subscriptions the supply side actively wants and pays for voluntarily, or (c) tied to features the platform *uniquely provides* (livestream, merch). No stream ever taxes the core artist ↔ organiser ↔ venue ↔ audience transaction. That promise is permanent.

### Data model — what stays, what dies, what's new

**Stays (from prior §4.5, still fully valid):**
- `Venue.rateType` (`HOURLY` | `DAILY` | `FLEXIBLE`), `hourlyRate`, `dailyRate`, `minDurationHours` — Venue Owner rate model unchanged.
- `VenueBooking.agreedRateType`, `agreedAmount`, `durationHours` / `durationDays` — negotiated rental terms snapshotted on each confirmed booking.
- `VenueBookingRequest` and `VenueBookingOffer` — the Flexible negotiation loop with counter-offers.
- `TicketTier` — per-section ticket pricing on events.
- `Event.maxSeatsPerBooking` (default 4, platform-bounded 1–10) — anti-abuse cap, not economics.
- `Event.maxPerformers` and `applicationApprovalMode` (`MANUAL` | `AUTO`) — Organiser's lineup controls.
- `Performance.compensationType` (`PAID` | `FREE` | `BUY_IN`), `feeAmount`, `buyInAmount` — reinterpreted as **records of the promise between Organiser and Artist**, not as amounts the platform will process. Kept because they're still meaningful UX and coordination signals: the Artist can see up front whether a slot pays, the Organiser has a record of what was agreed, and both sides can dispute against a shared record.

**Dies (dead columns; marked `@deprecated` in schema, do not read from application code):**
- `VenueBooking.platformFeeAmount` — no more flat fee added on top of venue rental. Column stays for existing rows' historical accuracy; new bookings write `0` or `null`.
- `PlatformSettings.ticketCommissionRate` — no more ticket commission. Dead column.
- `PlatformSettings.performerSlotCommissionRate` — no more slot commission. Dead column.
- `PlatformSettings.flatVenueBookingFee` — retired name. `audienceBookingFee` was added as a **new** column (not a rename), per sixth amendment implementation choice.
- `Payment.deliveredAt`, `Payment.deliveryError` — source of truth moved to `Booking.deliveredAt`/`Booking.deliveryError` per seventh amendment. Historical rows preserved; new writes go to Booking.

**New (from Checkpoints 2–4 and seventh amendment):**
- `PlatformSettings.audienceBookingFee` — Int paise, used by K1. Consistency with `Payment.amount` (also Int paise).
- `Booking.subtotalAmount`, `Booking.bookingFeeAmount`, `Booking.totalAmount` — all Float rupees, for compatibility with pre-existing `Booking.totalAmount`. Snapshotted at reservation.
- `Booking.deliveredAt`, `Booking.deliveryError` — atomic single-flight claim for ticket delivery. Works for both paid and free events (Payment-based claim didn't).
- `Payment` (from Checkpoint 2) — one per Booking, `amount` in paise as Int, tracks `razorpayOrderId`/`razorpayPaymentId`, `status` enum (`CREATED` → `VERIFIED` / `WEBHOOK_CONFIRMED` / `FAILED`).
- `User.displayName` (from Fix B / EPIC L) — separate from `User.name` (which is the login username). Tickets, emails, greetings use `displayName ?? name`.
- (Optional, Phase B onward) `Subscription` model for Venue Pro / Organiser Pro / Artist Premium — `{ userId, tier, status, currentPeriodEnd, ... }`. Not needed for MVP.

### Fee snapshotting (sixth amendment implementation detail)

The audience booking fee is captured onto Booking at reservation time. If admin changes the fee while a PENDING booking is open, that booking pays the original fee, not the new one. This is intentional — prevents fee-changes mid-flight from surprising an audience member.

### The old collaborator suggestions — which are still live, which are moot

1. **"Don't let an event go public without a confirmed venue booking."** — Still live, still the rule. Already shipped.
2. **"`FLEXIBLE` needs a real negotiation flow."** — Still live. Already shipped (F5/F7/E10/E12).
3. **Auto-approval requires `isVerified`.** — Still live. Already shipped.
4. ~~**"Commission math needs to be explicit."**~~ — Moot. No commission.
5. ~~**"Performer fee payout mechanics need to be settled through platform."**~~ — Moot. Platform doesn't process performer fees at MVP.
6. **"`BUY_IN` needs a collection point and cancellation rule."** — Still live *if* the platform ever offers optional payment facilitation (Phase D or later). Not urgent for MVP; performer pays Organiser directly.
7. **"Counter-offer loop needs a cap and expiry."** — Still live, still the right call. Already shipped.
8. **"Max-seats-per-booking should be Organiser-configurable but platform-bounded."** — Still live. Already shipped (E13).
9. ~~**"Organiser guarantee deposit for promised `PAID` fees."**~~ — Moot. Platform isn't in that money flow, so there's nothing to guarantee against. If the Organiser doesn't pay their performer, that's between them; platform doesn't owe anyone anything.

### Note on Razorpay dependency

The audience booking fee (Phase A) requires the ticket-processing pipeline to be live. **Test-mode keys unblocked the entire build** — Checkpoints 2–4 all shipped against them. When the company clears KYC and live keys are swapped in on production, the audience booking fee begins collecting real money immediately.

Split-payment mechanics (audience booking fee to platform, remainder to Organiser) can be handled through Razorpay Route on the platform's own account. This is a marketplace pattern Razorpay explicitly supports and keeps the platform out of Payment Aggregator license territory — the platform is the merchant of record only for the booking-fee slice, and Organisers are the merchants of record for their ticket revenue. **K2 (Razorpay Route split) is deferred until real Organiser onboarding lands as a real feature** — until Organisers have Razorpay-linked accounts of their own, all Checkpoint 4 revenue currently settles into the platform's Razorpay account, with the "remainder to Organiser" mechanic pending.

---

## 5. Architecture Summary (unchanged, restated for reference)

- **Shared backend:** Next.js API routes on Vercel, Prisma v7 → Supabase Postgres, NextAuth.js, Razorpay, Resend (email), MSG91 (SMS). Both web and mobile hit the same API.
- **Web:** Next.js 16, TypeScript, Tailwind, shadcn/ui, Zustand.
- **Mobile:** React Native 0.74+ / Expo SDK 51+, Expo Router, Zustand + React Query, NativeWind, Expo SecureStore for tokens. *(Release 3 target — not started as of seventh amendment.)*
- **Realtime:** Supabase Realtime for seat locking, live reactions, pre-show chat.

Data model, enums, and API routes are as specified in Design Doc §9–10 and Mobile Doc Appendix A, with three sets of changes layered on:
- Browse-first: registration no longer requires `role` at submit time (defaults to `AUDIENCE` server-side).
- Auth hardening: new `PasswordResetToken` model (§3's reset-password decision).
- Venue & event economics (§4.5): `Venue.rateType`/`hourlyRate`/`dailyRate`, `VenueBooking.agreedRateType`/`agreedAmount`/duration, new `TicketTier` model replacing flat `Event.ticketPrice`, and `Performance.compensationType`/`feeAmount` plus `Event.maxPerformers`/`applicationApprovalMode`.
- Payment & delivery (sixth/seventh amendments): `Payment` model, `Booking.deliveredAt`/`deliveryError`, `Booking.subtotalAmount`/`bookingFeeAmount`.

---

## 6. Epics & User Stories

Story points use Fibonacci (1, 2, 3, 5, 8, 13). Estimates assume a solo developer pairing with Claude for design/code/review — they size relative effort, not calendar time.

### EPIC A — Guest Browsing & Discovery
| ID | Story | Acceptance Criteria | Pts | Status |
|---|---|---|---|---|
| A1 | As a guest, I can see Home (Tonight/Trending) without logging in | Home loads with no auth check; "For You" degrades to city+trending | 3 | ✅ Verified |
| A2 | As a guest, I can browse Events with full filters | City/date/price/type/Navarasa filters all functional logged-out | 3 | ✅ Verified |
| A3 | As a guest, I can view full Event detail (lineup, reviews, venue) | No content is hidden or blurred for guests | 2 | ✅ Verified |
| A4 | As a guest, I can view Artist and Venue profiles fully | Same as A3 | 2 | ✅ Verified — no auth check anywhere in code |
| A5 | As a guest, I can search | Search works with no session | 2 | ⬜ N/A — no standalone search feature exists yet, only in-page filters on Events/Artists listings (already confirmed open) |

### EPIC B — Contextual Auth
| ID | Story | Acceptance Criteria | Pts | Status |
|---|---|---|---|---|
| B1 | As a guest, tapping a gated action shows a contextual bottom sheet/modal, not a redirect | Sheet copy references the specific action just taken | 5 | ✅ Shipped |
| B2 | As a user, after completing login I resume exactly where I was | Booking resumes at seat selection, not Home | 8 | ✅ Shipped — resumes the exact seat selection after sign-in, no lost context |
| B3 | As a user, I can register via phone OTP (mobile) or email/password (web) with no role picker | Account created with role=AUDIENCE by default | 5 | ✅ Shipped |
| B4 | As a user, I can sign in with Google/Apple one-tap | Social auth creates/links account correctly | 5 | ⬜ Not started — needs OAuth app registration in Google/Apple consoles first |
| B5 | As a user, I can request a password reset (web) | Reset email sent, link expires in 1hr, old password invalidated on use | 5 | ✅ Shipped |
| B6 | As the system, I lock an account after 5 failed logins for 15 minutes | Generic error shown; no user enumeration (ties to QA 3.2/3.3) | 3 | ✅ Shipped |
| B7 | As a pending Organiser/Venue Owner, I can log in and see a Pending Approval state | Dashboard shows banner, not full features (resolves QA 5.4) | 3 | ✅ Shipped — verified live with a real account |

### EPIC C — Booking & Payments
| ID | Story | Pts | Status |
|---|---|---|---|
| C1 | As audience, I can select seats on an interactive map with live lock (no double-booking), limited to the event's configured max-seats-per-booking across all sections combined | 8 | ✅ Shipped (sixth amendment) |
| C2 | As audience, I can pay via Razorpay (UPI/card/wallet) | 5 | ✅ Shipped (Checkpoint 2 / sixth amendment) — HMAC-verified confirm + server-to-server webhook, browser-death race handled |
| C3 | As audience, I receive PDF ticket + SMS + email instantly on confirm | 5 | ✅ Shipped (Checkpoint 3 / sixth amendment) — PDF via pdf-lib with QR encoding booking ID, Resend delivery from `tickets@mail.aforaudience.com`; **SMS still deferred** on MSG91 DLT registration. Free events now included (M1, seventh amendment). |
| C4 | As audience, I can view My Tickets (gated — triggers login if guest) | 3 | ✅ Shipped — `/tickets`, honest about reservation vs. confirmed status |
| C5 | As audience, I can cancel a booking and see refund status | 5 | ⚠️ Partial — cancel works (releases seats immediately); refund half is Checkpoint 5, not started (needs business decisions before build — see §9.5) |

### EPIC D — Artist Growth
| ID | Story | Pts | Status |
|---|---|---|---|
| D1 | As an artist, I can apply to upgrade from Audience via Profile | 3 | ✅ Shipped — no approval gate, unlike Organiser/Venue Owner; homepage's "I'm an Artist" CTAs now actually lead here instead of plain registration |
| D2 | As an artist, I can build a profile (bio, style tags, reel, socials) | 5 | ⬜ Not started — Artist dashboard exists (pre-existing) but the full profile-editing surface (reel, socials) isn't confirmed complete |
| D3 | As an artist, I can apply for open event slots and see application status | 5 | ✅ Shipped (pre-existing, predates this doc) |
| D4 | As an artist, I can view my Hype Score and post-show report | 5 | ⬜ Not started — Hype Score field exists (defaults 0), no growth/report UI |

### EPIC E — Organiser Tools
| ID | Story | Pts | Status |
|---|---|---|---|
| E1 | As a user, I can apply to become an Organiser via Profile | 3 | ✅ Shipped — verified live |
| E2 | As an approved Organiser, I can create an event via a guided wizard | 8 | ✅ Shipped (pre-existing, predates this doc) |
| E3 | As an Organiser, I can build a lineup (drag/drop, time blocks) | 8 | ✅ Shipped (eighteenth amendment) — `/dashboard/organiser/events/[id]/lineup`. Touch-compatible drag reorder via `@dnd-kit` (new dependency — no drag library existed, native HTML5 DnD doesn't work on touch), per-slot duration input, computed start/end time blocks (derived from `event.startTime` + cumulative duration, not stored). |
| E4 | As an Organiser, I can approve/reject Artist applications | 3 | ✅ Shipped — confirmed directly in code; was mislabeled not-started in earlier passes of this doc |
| E5 | As an Organiser, I can view real-time ticket sales | 5 | ✅ Shipped (fifteenth amendment — per-event dashboard; sixteenth — organiser overview + range filters; seventeenth — venue owner overview + organiser breakdown). Per-event: polling dashboard (20s), per-tier sold counts, revenue split, sales timeline, recent bookings. Organiser overview (`/dashboard/organiser/sales`) and Venue Owner overview (`/dashboard/venue/sales`) both have Week/Month/Quarter/Year/All Time range filters and drill-down into their respective per-entity pages. |
| E6 | As an Organiser, I can set/edit ticket price independently per seat section, matching the venue's seat map zones | 5 | ✅ Shipped |
| E7 | As an Organiser, I can mark each lineup slot as Paid (Organiser owes the Artist a fee, settled between them off-platform for MVP), Free (no money either way), or Buy-in (the Artist pays the Organiser a spot fee, e.g. ₹200 — also settled between them off-platform for MVP). Platform records the promise for both sides' clarity but does not process the payment or take a cut. | 8 | ✅ Shipped — compensation choice happens at application-approval time. The stored `feeAmount` and `buyInAmount` fields are records of the between-parties promise. |
| E8 | As an Organiser, I can set a maximum number of performers for an event/slot | 2 | ✅ Shipped — enforced on both auto- and manual-approval paths |
| E9 | As an Organiser, I can toggle whether Artist applications require my manual approval or auto-accept up to the performer cap (auto-accept requires the applying Artist to be verified) | 5 | ✅ Shipped — Auto mode can only default to Free compensation (no per-applicant input at apply time); Organiser can adjust after the fact |
| E10 | As an Organiser, I can request a venue booking under Hourly, Daily, or Flexible terms, and — for Flexible — send a duration/date request and receive a quote back from the Venue Owner before confirming | 8 | ✅ Shipped |
| E11 | *(Retired per fifth amendment — no longer in MVP.)* Buy-in is now settled between Organiser and Artist off-platform. | — | ⬜ Retired |
| E12 | As an Organiser, when a Venue Owner quotes me on a Flexible request, I can accept, decline, or send back a counter-offer, up to 3 rounds before the request auto-expires | 5 | ✅ Shipped |
| E13 | As an Organiser, I can set a max-seats-per-booking limit for my event (default 4, platform-bounded 1–10), enforced across all sections combined in a single booking | 3 | ✅ Shipped |
| E14 | *(Retired per fifth amendment.)* Platform fee on venue bookings removed. E14 cleanup ended up moot: the old `platformFeeAmount` UI on `VenueBooking` was never displayed anywhere in the shipped codebase (verified sixth amendment), so no cleanup was actually needed. | — | ⬜ Retired |

### EPIC F — Venue Owner Tools
| ID | Story | Pts | Status |
|---|---|---|---|
| F1 | As a Venue Owner, I can create a venue profile with seat map | 8 | ✅ Shipped (pre-existing, predates this doc) |
| F2 | As a Venue Owner, I can set availability and pricing per zone | 5 | ✅ Shipped (pre-existing) |
| F3 | As a Venue Owner, I can view booking calendar and revenue | 5 | ✅ Shipped — revenue cards (this month/total/pending) + a lightweight custom month calendar, built into the existing bookings page |
| F4 | As a Venue Owner, I can choose a rental rate type — Hourly, Daily, or Flexible — for my venue and set the base rate(s) for it | 5 | ✅ Shipped — including optional day-of-week overrides, verified live |
| F5 | As a Venue Owner, I can review an Organiser's Flexible booking request and respond by accepting, declining, or sending a counter-offer amount | 5 | ✅ Shipped |
| F6 | As a Venue Owner, I can see the agreed rate/duration snapshotted on each confirmed VenueBooking, unaffected by later changes to my published rates | 3 | ✅ Shipped (tenth amendment) |
| F7 | As a Venue Owner, when an Organiser counters my quote, I see their new number alongside the full offer history and can accept, decline, or counter again | 5 | ✅ Shipped |

### EPIC G — Reviews, Tips, Follow
| ID | Story | Pts | Status |
|---|---|---|---|
| G1 | As audience, I can follow an artist (gated) | 2 | ✅ Shipped. Nineteenth amendment: follower list + count now visible on the Artist dashboard (was previously invisible even though the data existed — the new-follower push linked to a page with nothing on it), and a push notification fires on new follow (not unfollow). |
| G2 | As audience, I can rate/review a performer post-show (gated) | 3 | ✅ Shipped. Nineteenth amendment: now actually gated on attendance — requires a `CONFIRMED` booking with `checkedInAt` set (EPIC N's check-in system made this buildable; the original "not restricted to attendees" limitation is closed). Server-side only — the review widget's UI still renders unconditionally, see EPIC J follow-ups. |
| G3 | As audience, I can tip an artist directly, 100% passthrough (gated) | 5 | ⬜ Not started — Razorpay is now live in QA (sixth amendment), so the technical blocker is gone; ship when it fits priority. Feedback also asks for the reverse view (artist seeing who tipped them) — same story. |

### EPIC H — Admin & Trust
| ID | Story | Pts | Status |
|---|---|---|---|
| H1 | As Admin, I can approve/reject Organiser and Venue Owner applications | 5 | ✅ Shipped — verified live |
| H2 | As Admin, I can view a dashboard of pending approvals | 3 | ✅ Shipped — verified live |
| H3 | As Admin, I can flag/suspend suspicious accounts | 5 | ⬜ Not started |
| H4 | As Admin, I can configure the audience-side booking fee from a settings screen. | 2 | ✅ Shipped alongside Checkpoint 4 (K1). `/dashboard/admin/settings`. Dead columns from fifth amendment are correctly not surfaced. |

### EPIC I — Mobile-Exclusive (Phase 4)
Shake to Discover (5), QR Check-in (5), Offline tickets (5), AR Venue Preview (13), Live Reaction Feed (8), Pre-Show Chat (5), Add to Wallet (5).

### EPIC J — Notifications (rewritten nineteenth amendment — shipped this session)

Web Push (not SMS/email) is now the primary channel. VAPID-based, works on any installed PWA or browser tab with permission granted — no native app required. SMS remains blocked on MSG91 DLT registration; email stays ticket-delivery-only, unchanged.

**Infrastructure:**
| Piece | Detail |
|---|---|
| `PushSubscription` model | Keyed to `userId`, unique on `endpoint`. Upsert-safe — re-subscribing (new device, or an already-granted device switching to a different logged-in account) reassigns the row rather than erroring or duplicating. |
| `src/lib/push.ts` | `sendPushToUser` / `sendPushToRole`, no-ops with a warning if VAPID env vars aren't set. `notifyAfterResponse(fn, label)` wraps every call site — see the `after()` note below, this is not optional plumbing. |
| `/api/push/subscribe` | POST upsert, DELETE scoped to the caller's own endpoint only. |
| `sw.js` | `push` + `notificationclick` handlers — focuses an existing tab if one's open, else opens a new one at the notification's target URL. |
| `NotificationOptIn.tsx` | Dismissible opt-in banner (any logged-in role, not gated to one). Also handles the "device already granted permission under a different account" case silently — re-links or creates a subscription for whoever's currently logged in without a re-prompt, since the browser already decided. |

**Two engineering bugs found live-testing, both fixed — worth remembering for any future background-work pattern in this codebase:**
1. **Vercel can freeze a serverless function's runtime the instant its response is sent**, silently killing any bare un-awaited `promise.catch(...)` fire-and-forget call before it completes — no error, no log, it just never finishes. Confirmed happening: a push worked twice, then silently dropped a third time with zero trace anywhere. Fix: Next.js `after()` (15.1+) wraps every push send and every `deliverTicket()` call site now — runs after the response, but guarantees completion. `sendPushToRole`/`sendPushToUser` calls must go through `notifyAfterResponse()`, never called bare with `.catch()`.
2. **Default push urgency ('normal') lets Android's Doze/battery optimization defer delivery** by minutes on an idle device — confirmed live (a sales-milestone push arrived several minutes late, not dropped). Fixed via `options.urgency: 'high'` in `sendPushToUser` — note `@types/web-push`'s `RequestOptions` doesn't declare this field even though the runtime supports it (checked `node_modules/web-push/src` directly), hence a local type augmentation rather than fighting the incomplete types package. TTL also set to 1 hour (was the library default of 4 weeks) — a stale approval/negotiation notification surfacing hours later isn't useful for this app's notification types.

**Notification matrix (all confirmed working live, in this order of build):**
| Notify | Trigger |
|---|---|
| Admin | New Organiser/VenueOwner application |
| Organiser/VenueOwner applicant | Admin's approve/reject decision |
| Organiser | New artist application (manual-review events only — auto-approved events notify the Artist instead, since there's nothing for the Organiser to act on) |
| Artist | Application approved/rejected, or auto-approved |
| VenueOwner | New venue booking request (both the Flexible-negotiation and direct-booking paths) |
| Organiser/VenueOwner | Other side accepted/declined/countered in a negotiation (turn-based — whoever didn't just act gets pushed) |
| Artist | New performer-specific review |
| Organiser | New general event review (no `Review`→`Venue` link exists in the schema, so VenueOwners aren't reachable from a review yet) |
| Organiser | Ticket sale milestone — first sale / 50% / sold out (never per-ticket, would spam a popular event; only the single highest threshold a given booking crosses fires) |
| Artist | New follower |

**Deliberately not built (flagged, not silently skipped):**
- 48h negotiation-expiry notification — needs a cron job; no scheduled-task infra exists in this codebase yet (no `vercel.json`, no cron routes). Separate scope/decision.
- Audience "booking confirmed" push — redundant with the existing ticket email/PDF.
- Review widget's client-side gating — ~~the check-in requirement (see EPIC N amendment below) is enforced server-side only; the review UI on the event detail page still renders the rating widget unconditionally~~ ✅ **Fixed (PR #111, 19 Jul).** `events/[id]/page.tsx` (server component, already `force-dynamic`) now looks up the session and queries for a checked-in `CONFIRMED` booking, passing `canReview: boolean` down to the client component, which gates the widget on it (in addition to the existing "already reviewed" check). Anonymous viewers always get `canReview: false`; they see the widget on next load if they sign in and are actually checked in.
- Android `.apk`/TWA-specific notification checks — `POST_NOTIFICATIONS` runtime permission (Android 13+) and notification delegation (`enableNotificationDelegation`) live in the PWABuilder/Bubblewrap-generated Android project, not this repo. Confirmed conceptually works the same way (same Chrome engine, same service worker) but not verified against the actual QA `.apk`.

### EPIC K — Platform Revenue (per fifth amendment / About page)
| ID | Story | Pts | Status |
|---|---|---|---|
| K1 | As audience, when I check out for a ticket, I see a small booking fee as a separate line item on the order, framed as "supports the artist ecosystem." | 3 | ✅ Shipped (Checkpoint 4 / sixth amendment) |
| K2 | As the platform, at Razorpay settlement time, the audience booking fee is captured by the platform's own merchant account and the remainder is passed through to the Organiser — via Razorpay Route split. | 5 | ⚠️ Partial — fee captured to platform's Razorpay account; Route split deferred until real Organiser onboarding lands (Organisers need their own Razorpay-linked accounts first) |
| K3 | As audience, at checkout I optionally see a small "support the platform" widget with preset tip amounts — zero pressure, easy to skip. | 3 | ⬜ Not started |
| K4 | As Admin, I can view total booking fees collected, per day / month, from a simple revenue dashboard. | 3 | ⬜ Not started (though `/dashboard/admin/settings` exists for the config surface) |
| K5 | *(Phase B)* Venue Pro subscription. | 8 | ⬜ Not started — deliberately Phase B |
| K6 | *(Phase C)* Organiser Pro subscription. | 8 | ⬜ Not started — deliberately Phase C |
| K7 | *(Phase C)* Artist Premium subscription. | 5 | ⬜ Not started — deliberately Phase C |
| K8 | *(Phase B)* Promoted placement slots. | 5 | ⬜ Not started — deliberately Phase B |

### EPIC L — User Display Name (retrospective, sixth amendment)
Retrospective epic to record Fix B work.

| ID | Story | Status |
|---|---|---|
| L1 | User has separate `displayName` from login `name` (username) | ✅ Shipped |
| L2 | Register form's Full Name persists as displayName | ✅ Shipped |
| L3 | Profile page lets existing users set/edit displayName | ✅ Shipped |
| L4 | Tickets/emails/greetings use displayName-with-fallback-to-name | ✅ Shipped |
| L5 | Backfill hint / one-time nudge for null-displayName users | ✅ Shipped (eighth amendment, PR #44) — sticky top banner, dismissible for 14 days, self-hides on auth/checkout/api/admin |

### EPIC M — Free-Event Ticket Delivery (retrospective, sixth/seventh amendments)

| ID | Story | Pts | Status |
|---|---|---|---|
| M1 | Free events get PDF + email on auto-confirmation (delivery claim moved from Payment to Booking) | 3 | ✅ Shipped (seventh amendment) |

### EPIC N — Check-In / Scan Flow (twelfth amendment)

Real gap found in review, not hypothetical: the ticket PDF's QR has always encoded the raw `booking.id` (see comment in `src/lib/ticket-pdf.ts`), but until now nothing validated it or marked a ticket used - any booking, confirmed or not, could be "let in" indefinitely and repeatedly by anyone holding the QR image or just the printed Booking ID text.

| ID | Story | Status |
|---|---|---|
| N1 | `Booking.checkedInAt` / `checkedInByUserId` - once-only used-state, audit trail of who scanned | ✅ Shipped |
| N2 | `POST /api/events/[id]/checkin` - validates event match, `CONFIRMED` status, not-already-used; Organiser (owner) or Admin only | ✅ Shipped |
| N3 | `GET /api/events/[id]/checkin` - live checked-in/total counts for the scanner header | ✅ Shipped |
| N4 | Scanner UI (`/dashboard/organiser/events/[id]/checkin`) - camera QR scan (`html5-qrcode`) + manual booking-ID entry fallback, pass/fail card with attendee name + section | ✅ Shipped |

**Deliberately deferred:** the QR payload itself is still unsigned (just the raw booking ID) - anti-forgery lives entirely at scan time via the DB lookup/single-use check above, not at PDF-generation time. Signing/tokenizing the QR payload is a smaller follow-up, not blocking, since a forged code still has to guess a real unused `cuid` to do anything.

---

## 7. Key Use Cases (Detailed Flows)

**UC-1: Guest discovers and books a ticket (the flagship flow)**
1. Guest opens app/site → Home loads instantly, no auth.
2. Selects city, browses events, filters by Navarasa/mood, opens Event → sees lineup, venue, reviews.
3. Taps **Book Now** → contextual sheet: "Sign in to save your seat" → phone OTP.
4. Returns to seat selection → picks seat → Razorpay → PDF/SMS/email confirmation.

**UC-2: Audience upgrades to Organiser**
1. From Profile → "Become an Organiser" → application form (org name, city, phone).
2. Submits → application enters Admin queue.
3. Account flagged `isApproved: false`; user sees "Pending Approval" state if they visit a dashboard.
4. Admin reviews in Admin panel → approves.
5. User notified → dashboard unlocks full Organiser functionality.

**UC-3: Organiser creates an event, Artist applies, Audience books**
1. Approved Organiser → Create Event wizard → sets theme, type, date, searches Venue OS for a space.
2. Organiser requests the venue: if it's listed Hourly/Daily, books directly at the published rate; if Flexible, sends a duration/date request.
3. (Flexible only) Venue Owner responds with a quote → Organiser accepts → `VenueBooking` moves to `CONFIRMED` with the agreed rate/duration snapshotted.
4. Organiser sets ticket prices per section (matching the venue's seat map zones), marks each lineup slot Paid (with fee) or Free, sets max performer count, and picks Manual or Auto approval for applications.
5. Event goes to `PENDING_APPROVAL` — **cannot advance without a `CONFIRMED` VenueBooking** — Admin approves → status `APPROVED`, now publicly visible.
6. Artist (browsing Events tab) sees an open lineup slot with its fee/free status → applies.
7. If Manual: Organiser reviews and approves. If Auto: the application auto-accepts as soon as the Artist's account is verified and a slot is open.
8. Audience discovers event (as in UC-1), sees per-section pricing, and books.

**UC-4: Forgotten password (web)**
1. User on `/login` taps "Forgot password."
2. Enters email → reset link sent (1hr expiry) via Resend.
3. Clicks link → sets new password → old sessions invalidated → redirected to login.

**UC-5: Rate-limited login attempt**
1. User enters wrong password 5 times.
2. 6th attempt shows "Too many attempts, try again in 15 minutes" — same message regardless of whether the email exists.
3. After 15 minutes (or successful OTP/social login), lockout clears.

---

## 8. Agile Delivery Plan (Solo Developer + Claude)

Since this is one person collaborating with Claude as architect/dev/QA rather than a staffed team, the plan below is intentionally lightweight — a Kanban-flavored backlog with story points for sizing/prioritization, not committed sprint velocity.

### 8.1 Working Cadence
- **Cycle length:** 1-week working blocks ("sprints" in name only) — short enough to re-prioritize based on what's actually shippable, since there's no fixed team velocity to protect.
- **Ceremonies (self-run, ~15 min each):**
  - *Monday planning:* pick the next 8–15 points of stories from the top of the backlog (§6), confirm with Claude any design ambiguity before coding starts.
  - *Friday review:* demo to self, mark Definition of Done items, log what's actually done vs. carried over.
- **Claude's role per story:** (1) clarify acceptance criteria if ambiguous, (2) propose architecture/approach, (3) write/review code via Claude Code, (4) generate test cases (extending the existing QA test plan pattern), (5) flag regressions against prior decisions in this doc.

### 8.2 Backlog Order (maps to §4 phasing)

**Status snapshot as of tenth amendment:** Release 0 is complete and live in prod. Release 1 (MVP Core) is **mostly shipped** — Checkpoints 2, 3, 4 all live in QA and stable. What remains from the originally-scoped ~135 points:

- Checkpoint 5 (refunds) — needs business decisions before build (§9.5)
- E3 (lineup drag-and-drop) — not started, standalone
- E5 (real-time ticket sales dashboard) — not started
- K2 completion (Route split) — deferred to post-Organiser-onboarding
- SMS via MSG91 — blocked on DLT template registration

The original release ordering:

1. **Release 0 — Foundation:** B1–B7 (contextual auth + reset + lockout), A1–A5 (guest browsing) — ✅ Shipped
2. **Release 1 — MVP Core:** C1–C5 (booking/payment/tickets), D1, E1–E2, E6–E10, E12, E13, F1, F2, F4, F5, F7, plus K1–K2 (audience booking fee). ✅ Mostly shipped per snapshot above.
3. **Release 2 — Growth:** F3, F6 (remaining Venue OS), H1–H4 (Admin), G1–G3 (reviews/follow/tip), remaining D-stories, K3–K4 (checkout tip widget, revenue dashboard). Partly shipped (F3, H1/H2/H4, G1/G2).
4. **Release 3 — Mobile Parity:** Same epics rebuilt/ported to React Native, no exclusive features yet. **Not started.**
5. **Release 4 — Exclusive/Scale:** EPIC I (mobile-exclusive), AI Matchmaker, Navarasa search, Art Passport, livestream.

### 8.3 Estimation Notes
- Original Release 0 + 1 estimate: ~135 points (post-fifth-amendment). With Checkpoints 2/3/4 + Fix B + M1 all shipped, the remaining Release 1 backlog is small enough that timeline estimation now depends more on external unblocks (Razorpay live keys, MSG91 DLT, refund decisions) than on engineering velocity.
- Re-estimate after Release 0 shipped: informed the sixth-amendment shipping pace (four checkpoints in one working day was materially faster than the original ~10–15 points/week baseline). Session-length management is the actual current constraint, not story-point throughput.

### 8.4 Definition of Done (every story)
- [ ] Acceptance criteria met and manually verified
- [ ] Corresponding case(s) added to the QA test plan pattern (extend `AforAudience_Web_Login_Test_Plan.md`-style tables per epic)
- [ ] No plaintext secrets, passwords hashed, sessions behave per §3 policy
- [ ] Works on the guest→auth transition path if the story touches a gated action (no dead-ends, no lost context)
- [ ] Deployed to staging (`qa.aforaudience.com`) and smoke-tested before merging to main

### 8.5 Testing Strategy
- Auth/session/role stories reuse and extend the existing QA Test Plan structure (§1–9 of that doc) as the template for all future feature test plans — one table per epic, same columns (Test Case / Steps / Expected Result).
- Every gated-action story needs an explicit "guest attempts action → login prompt → resumes correctly" test case, since that's the whole point of this model.
- Payment and booking stories need a concurrency test (two users booking the last seat simultaneously) given Supabase Realtime seat locking is core to correctness.

### 8.6 Vercel Deployment Discipline (established this session, after PR #185 rate-limit block)

**Root cause investigated and confirmed:** Vercel Hobby allows 100 deployments per rolling 24h; hitting it blocks *all* new deployments account-wide (not just the offending branch) until the window resets. This is what blocked PR #185. Checked live deployment history: ~20 deployments fired in a single ~9-hour session window, driven by push volume — not by the Playwright e2e workflow, which runs entirely on GitHub Actions infra and creates zero Vercel deployments.

**What actually eats the quota:**
- Every push to any branch (feature, PR, or `qa`) creates its own full Vercel deployment
- Pushing each intermediate commit separately multiplies this — e.g. PR #187 alone generated 4 separate deployments across its 4 commits before merge
- Doc-only commits to `qa` (e.g. handoff/design.md updates) also each burn a full deployment with zero code change

**Dead end investigated — do not retry:** `ignoreCommand` / "Ignored Build Step" in `vercel.json` does **not** reduce deployment count. Vercel creates the deployment record and counts it toward the 100/day quota *before* the ignore command runs; a canceled build still counts as a full deployment. Confirmed via Vercel's own docs and community reports. Not worth implementing — it would only save build minutes, not quota.

**Actual policy going forward (free, no infra change needed):**
- **Squash/batch commits locally before pushing** feature branches — push once when a logical unit of work is ready for Vercel-verification, not after every small commit.
- **Batch doc-only updates** into the same push as adjacent code changes where practical, rather than a standalone push purely for a handoff/design.md note, when a code push is already imminent.
- This is a workflow discipline change, not a technical fix — no vercel.json changes made.

**Vercel Pro (6,000 deployments/day, no queue) was quoted and explicitly declined by Hitesh this session** — not pursued further unless he raises it again.

---

## 9. Known Gaps & Backlog (Live Tracking)

Replaces the design-phase "What's Next" notes with what's actually true after real implementation and live testing. Organized by priority, not by epic.

### 9.0 Standing Rules

Canonical, human-readable copy of Claude's standing behavioral rules - the actual enforcement mechanism is Claude's memory system (not this doc), but memory isn't visible to Hitesh directly or part of the repo, so this is where a person (or a future session double-checking) can read them without asking. Update this section whenever a rule is added/changed in memory - it should never drift from what memory actually says.

1. **Collaborator standing (added 26 Jul).** Claude is a collaborator, not a passive assistant - has full right to push back, argue in case of conflict, and flag it directly if Hitesh appears to be diverting from the defined goal. Check in on goal-alignment roughly every 15 minutes during a session. If the goal itself is unclear or ambiguous, ask Hitesh rather than guessing and proceeding.
2. **Feedback-table registration cadence (added 26 Jul).** Register every reported issue/incident/bug/suggestion/idea into the Feedback table on a ~30-minute cadence during a session, not just at natural pause points. Ask Hitesh for clarifying detail before logging if a report is ambiguous. Backlog items/decisions discussed in-chat (e.g. this doc's §9 entries) get registered into the Feedback table too (category=FEATURE_IDEA), not only this doc.
3. **2-hour handoff checkpoint (added 26 Jul).** Every 2 hours during a session, proactively ask Hitesh for a handoff checkpoint rather than letting the thread run indefinitely and risking context rotation/detail loss.
4. **Commercial/billing reminder (added 26 Jul).** At the start of every session, remind Hitesh to check commercial/billing indexes for paid third-party services currently in use - Razorpay and Google Maps/Places API - so usage/cost doesn't go unnoticed. Delivered in bold (and red, where the interface supports color) so it stands out.

**Pre-existing rules, still active (predate this section, folded in for one place to read all of them):**
- Session-start protocol: query the Feedback table (`status IN ('NEW','REVIEWED')`, `category='BUG'`) and cross-check each against this document before treating anything as an open bug or acting on a prior handoff's assumptions.
- Log observations/findings continuously as found during a session, not batched at the end.
- Even after Hitesh states a decision, proactively offer a better/alternative approach if one exists - not passive agreement.
- If an automated test scenario fails 2+ times in CI, stop auto-retrying and flag for manual verification instead.
- Never enter passwords into a live browser, even for a throwaway test account Claude creates itself, even if explicitly asked.
- Hard freeze: no `qa` → `main` merges, no prod Supabase changes, no prod env vars, no Razorpay live keys/KYC until Hitesh's explicit "company registered" signal.

### 9.1 Next up

**Advanced Admin Dashboard — full scope (25 Jul, organiser-role testing session, Hitesh's stated top priority).** Evolves the existing `/dashboard/admin/feedback` page (ninth amendment - lists feedback, filters by status, marks resolved) into a real feature/bug/idea tracker with trends, not a from-scratch build. Both trend charts and board/list/detail view are in v1 together (Hitesh's call, not phased); self-serve status/severity editing is in v1 too, scoped as below. Must work fully on mobile, not responsive-as-an-afterthought (Hitesh's explicit requirement).

**Schema changes needed (currently missing from `Feedback`):**
- `severity` enum (e.g. P0-P3, or Low/Medium/High/Critical - needs a quick decision, not yet made) - today "important" only lives in Claude's head and in `design.md` prose, not as queryable data.
- `resolvedAt` timestamp - only `createdAt` exists today, so time-to-resolution trend metrics aren't computable without it.
- `status` becomes a real enum (`FeedbackStatus`: NEW/REVIEWED/RESOLVED) instead of a free-text string - low-risk migration (only 3 values exist today), worth locking down before a dashboard starts depending on exact string matches.
- `title` (short, separate from the full `message`) - needed for clean list/board rows; could be auto-generated from the first N chars of `message` at creation time rather than requiring manual entry, to keep the existing feedback-submission flow (chatbot widget, guest-accessible) untouched.
- New `FeedbackChangeLog` model (or a simpler JSON audit trail column) - every self-serve status/severity change gets a timestamped record of who changed what, from what, to what. This is the one thing worth protecting: right now, every status change that goes through Claude gets a reasoned note in this doc explaining *why* - self-serve editing must not lose that trail. A simple changelog satisfies this without slowing Hitesh down.

**Self-serve editing scope, deliberately narrow:** status and severity only, via inline dropdowns/controls. The original `message` (raw filed report) stays immutable - it's the evidence, not something to edit after the fact. Every change auto-logs to the changelog above.

**Page structure:**
- **Board/list view** - columns by status (New/Reviewed/Resolved) on desktop; a single filterable/sortable stacked list with a status-change dropdown per card on mobile (true drag-and-drop Kanban doesn't work well on touch - same reasoning already applied to other mobile-vs-desktop layout decisions in this codebase). Filterable by category, severity, page, keyword search.
- **Detail panel** - full message, screenshot (if attached), page URL, timestamps, changelog history, status/severity controls.
- **Trend charts** - opened vs. resolved per week (line), category breakdown (bar/pie), open-item age distribution. Needs `resolvedAt` and `severity` to be meaningful rather than decorative - this is why the schema changes above aren't optional even for a "just ship the charts" ask.

**Both previously-open decisions now settled (26 Jul, session 33) — see §9.5.** Severity scale: **Low/Medium/High/Critical**. Resolved-item visibility: default board query excludes `RESOLVED`; a "Show Resolved" toggle lazy-fetches them on demand rather than always loading them or requiring a separate archive page. Both now unblocked to build.

**Schema shipped (26 Jul, session 33, PR #191, merge `3e35857`).** `status` locked down to a real `FeedbackStatus` enum (only NEW/REVIEWED/RESOLVED ever existed - safe conversion), `FeedbackSeverity` enum added (LOW/MEDIUM/HIGH/CRITICAL), `resolvedAt`/`title` columns added (nullable, additive, no backfill), `FeedbackChangeLog` model added (audit trail for self-serve edits). Applied to QA Supabase via Supabase MCP before the code PR, matching standing practice. **Along the way, fixed a real build break the schema change surfaced:** the pre-existing `/api/admin/feedback` PATCH handler (ninth amendment) passed a raw string into what's now an enum column - fixed with a validated cast, and while touching that route, wired in the `FeedbackChangeLog` write + `resolvedAt` tracking on every status change (the schema's actual purpose), plus added `severity`/`title`/`resolvedAt` to the GET response.

**UI shipped (26 Jul, session 34, PR #192, merge `d484eca`).** Board view (desktop, columns by status) / filterable-sortable stacked list with per-card status+severity dropdowns (mobile - no drag-and-drop, same reasoning already applied elsewhere in this codebase). Filters: category, severity, page-contains, keyword search - all client-side, same MVP-volume reasoning as the original page. "Show Resolved" toggle is a genuine lazy fetch (`GET /api/admin/feedback?status=RESOLVED`), not loaded by default, matching Hitesh's settled call. Trend charts (opened vs. resolved/week, category breakdown, open-item age distribution) - hand-rolled inline SVG, no new chart library dependency, fetched once via `?status=ALL` regardless of the toggle state so trends always reflect the full picture. Detail panel: full message, screenshot, page URL, timestamps, changelog history, status/severity controls. `PATCH /api/admin/feedback` now accepts `severity` alongside `status`, writes a changelog entry per changed field, no-ops cleanly when nothing actually changed. `POST /api/feedback` now auto-derives `title` at creation time (first ~60 chars, word-boundary cut) - the schema-noted gap above is closed. **Admin Dashboard v1 is now feature-complete per the full scope in this section** - not yet exercised with live testing; next step is Hitesh clicking through it on QA.

**Shipped this session (session 26, 23 Jul) - from a broader brand/vision conversation with Hitesh, not code-driven feedback:**
- **Chatbot idle pulse** ✅ Subtle breathing animation (SVG SMIL, opacity+radius) on the existing spotlight glow behind the mic icon, active only while the support widget is closed - stops once opened, where it'd be distracting. Confirmed in the same pass that `icon.svg` (favicon/app icons) already uses the exact same three-bar brand mark as the intro splash sequence - already consistent, nothing to fix there.
- **BrandLoader component** ✅ Reusable animated loading indicator reusing the three-bar motif (cream/gold/ember from the icon), styled as a small looping "equalizer" rather than the splash's one-shot full-screen play - reads as a soundwave, fitting for a live-performance platform. Swapped in for the plain "Loading..." text on the five highest-traffic pages (all four role dashboards + `/profile`) - confirmed all five used the identical `<div>Loading...</div>` pattern before editing, so this was a safe, consistent swap. **~27 other lower-traffic "Loading..." spots elsewhere in the app were deliberately left alone** in this pass - a natural, low-risk follow-on whenever it's worth doing, not something to blind-swap across dozens of files with different contexts in one go.


**Shipped this session (session 25, 23 Jul) - two new requests, no prior backlog entry:**
- **"Artist Background" expanded profile** — Hitesh's clarification of an earlier vague "Artist background" feedback item: profile picture, complete biography, journey, thanks/acknowledgments, and goals/ambitions. Collaborator additions: a one-line tagline (shown prominently in the hero) and an influences section. New Artist fields (`tagline`/`fullBiography`/`journey`/`influences`/`acknowledgments`/`goals`), all optional/nullable - no artist is forced to fill any of it in. **Profile picture is URL-paste, not real file upload** - confirmed via code read that `User.avatar` was a schema field literally commented as a "future" field, never wired up anywhere, and no storage provider (Vercel Blob/S3/Cloudinary) exists in this app yet. Wired it up honestly as a pasted-link field, same trust pattern already used for social links and venue Maps URLs, rather than overclaiming a real upload flow that would need new infrastructure. `PATCH /api/users/me` extended to support it (its own comment already named avatar as the next field to add); `PATCH /api/artists/me` extended for the six background fields, each length-capped consistent with other free-text fields in this codebase. Shown on the public Artist profile: tagline under the name, real avatar image if set (previously always showed a letter-circle placeholder), and each filled-in background section rendered under the existing short bio.
- **Mood-based theme** — Hitesh's extra feature ask: let the audience set a different theme based on their mood. **Scoped deliberately, not built as a full site re-theme at the time:** this codebase used hardcoded hex colors inline in every component, not CSS variables, so a genuine global re-theme would have meant auditing and changing every page - a bigger, riskier undertaking not worth doing blind in one pass. Shipped instead: the homepage's existing Navarasa grid (9 emotions of Indian classical art, already styled `cursor: pointer` but with zero click behavior before this) is now a real mood picker - tapping a rasa themes the CTA section directly below it with a mood-appropriate accent color, and the choice persists in `localStorage` (works for anonymous visitors, remembered on return visits). Extracted into a new `MoodThemeSection` client component (the rest of the homepage stays a server component, same "isolated island" pattern used elsewhere in this codebase). **Update (session 27, 23 Jul): the "hardcoded hex, not CSS variables" blocker above is now resolved — see Theme Phase 0 below.** Phase 2 (retrofitting the mood picker to a real full-site reskin) is unblocked whenever Hitesh wants it, no longer needing its own from-scratch color audit first.

- **Theme Phase 1 — opt-in "Indigo" theme** ✅ **Shipped (session 27, 23 Jul, PR #184).** Delegated decision (Hitesh: "take best decision" on both what the theme should be and where the toggle lives). **Chose an accent-swap theme, not a light/dark inversion, after auditing usage first:** ink and cream are both used in dual roles across the 71 Phase-0 files (ink as body text on a cream page, but also as a solid button/badge *fill* with white/cream text on top - ~38 such spots). Inverting the raw values directly would flip those solid buttons to a light fill with the same light text on top, illegible - a correct dark mode needs a real semantic text-vs-background split across those files, which is genuinely Phase 2-sized work, not a same-day Phase 1. Shipped instead: `[data-theme="indigo"]` overrides only 3 vars (`--afa-terracotta`, `--afa-terracotta-tint`, `--afa-maroon-black`) to a deep indigo + the existing gold - every text/background contrast pair stays intact, ~226+ usages update for free via Phase 0's centralization. Pre-paint inline script in `layout.tsx` `<head>` applies a saved `localStorage('afa-theme')` preference before first paint (no flash); toggle button in `SiteNav.tsx` (desktop nav + mobile panel), site-wide and available to everyone, per the same delegation. **Not yet done:** no live click-through test of the toggle itself this session (no browser access) - worth trying live on QA. Sets up Phase 2 (mood-picker full reskin) with a proven, working `data-theme` cascade pattern.

  **Follow-up found from Hitesh's own screenshot (session 27):** the wordmark's "A" was using `--afa-terracotta` like every other accent, so it shifted color under Indigo too - correctly reproducing the coupling, but the wrong coupling. A brand mark should stay one fixed, recognizable color everywhere (nav, hero, all 5 auth pages, and matching the non-theme-aware static app icon/splash/email brand color), independent of accent theme. Fix: new `--afa-brand-mark` var, fixed to the same `#C8441A`, never overridden by any theme block; all 8 wordmark occurrences repointed to it. **Merged and live on `qa`** — PR #185, `HEAD` commit `88a5fd59fa3e560674078df3d23f657cc4375d17`. Initially blocked on a Vercel Hobby-plan deployment rate limit hit earlier this session; resolved by confirming (via direct Vercel deployment history) that the account-wide rate-limit window had cleared, forcing a fresh build with an empty commit on the PR branch to get a live green check (stale "failure" status was never going to re-check itself), then squash-merging once verified. Merge commit parent and 8-file diff spot-checked against this description — clean.

- **Theme Phase 0 — CSS custom properties for every hardcoded hex color** ✅ **Shipped (session 27, 23 Jul, PR #182).** Zero visual change: 1,512 hex-color occurrences across 71 component files replaced with `var(--afa-*)` references, 54 distinct colors defined once in `globals.css` `:root` (byte-identical hex values, pure refactor). Deliberately excluded two files where CSS variables would actually break real output: `src/lib/ticket-pdf.ts` (pdf-lib's float-RGB drawing API - no DOM, not CSS at all) and `src/lib/email.ts` (HTML email templates - CSS custom properties have poor support in email clients, especially Outlook desktop). Verified structurally (quote/brace counts identical pre/post-migration across all changed files) and spot-checked on `origin/qa` post-merge. **Known gap, out of scope for this phase:** several pages (notably the seat-map builder) also use `rgba(14,12,10, alpha)` - the same ink color with transparency - which wasn't touched since the ask was specifically "every hardcoded hex color," not rgba. A handful of alpha-borders remain literal; worth a fast follow-up pass if full single-source-of-truth on the ink color specifically matters later. **Unblocks:** Phase 1 (one opt-in second theme) and Phase 2 (mood-picker full site reskin) can now both just swap `:root` values instead of hunting through files - Hitesh has not yet said go on either.

**External blockers:**
- Razorpay webhook secret setup (2-min dashboard task, no code needed)
- Razorpay Route activation (K2 completion) — deferred until real Organiser onboarding is a real feature
- MSG91 DLT registration (SMS via EPIC J)
- Google/Apple OAuth app registration (B4)
- Refund policy decisions (unblocks Checkpoint 5 / C5 refund half)

**Best-unblocked next work (session-friendly, no external unblocks needed):**
1. ✅ Gate ticket booking on phone-verified users only — shipped. `/verify-phone` is a standalone completion flow (reuses the existing SIGNUP_VERIFY OTP endpoints), reachable any time via a sticky nudge banner or a direct redirect when `/api/bookings`/`/api/venue-bookings` reject an unverified user. Non-dismissible nudge (unlike the displayName one) since booking is now actually gated on it.
2. ✅ E3 — lineup drag-and-drop builder — shipped (eighteenth amendment).
3. ✅ E5 — real-time ticket sales dashboard — shipped: per-event dashboard (fifteenth amendment) + overview with Week/Month/Quarter/Year/All Time range filters (sixteenth amendment). Venue Owner revenue view shipped same session as a same-shape extension.
4. ✅ **Venue Owner sales overview** — shipped (seventeenth amendment). `/dashboard/venue/sales`: same pattern as the Organiser overview — range filter, per-venue drill-down, plus an organiser-breakdown table (which organisers rent the most / spend the most). Table on the overview, no dedicated per-organiser page, per the phasing decision.
5. ✅ **EPIC J — full push notification system** — shipped nineteenth amendment. See EPIC J for the complete matrix.
6. ✅ **Postgres connection pool exhaustion** — shipped nineteenth amendment. See §9.2.
7. ✅ **Verification gated at commitment points** (venue publish, event publish, spot application, offer accept) — shipped (PR #97, 18 Jul). See §9.2 for the settled principle.
8. ✅ **Backdated events + invalid time formats rejected server-side** — shipped (PR #97, 18 Jul, second commit). `POST /api/events` now combines date+startTime into an actual instant and rejects if already passed.
9. ✅ **Overnight events show "(next day)"** — shipped (PR #97, 18 Jul, third commit). `formatEventTimeRange()` in `src/lib/eventTime.ts`, applied on public event page, checkout, organiser event management page.
10. ✅ **Toast notification system, full rollout** — pilot shipped (PR #98, 18 Jul); full rollout to the remaining real candidates completed 19 Jul (PRs #107–#110). Audited all 22 dashboard pages first — of the ~16 pages assumed to need it, 7 turned out to be false positives (load-only errors with no user action to wrap, or already handled via a differently-named state) and were correctly left alone rather than converted. The 9 genuine candidates (Artist edit/events, Organiser event edit/lineup/detail, Venue-requests/edit/bookings, Admin settings) now use `useToast()`. Two recurring shapes handled differently: pages that gate full-page rendering on a load error (`if (error && !X) return <errorpage>`) kept that gate untouched and only moved the *action*-handler errors to toast; pages with no such gate got a new dedicated `loadError` state split out from the action-toast path, so a genuine load failure doesn't look identical to an empty list once a toast auto-dismisses.
11. ✅ **Required-field errors name what's missing** — shipped (PR #98, 18 Jul). Venue/event create forms now say e.g. "Please fill in the required fields: Venue Name, Address, City." instead of a generic message, and check those before other validation so the most fundamental gap surfaces first.
12. ✅ **Contextual verification messages** — shipped (PR #99, 18 Jul). `requireVerifiedPhone(user, action)` takes an action string; each of the four gated points now names what it's protecting instead of one generic string.
13. ✅ **Input bounds for seats/price/rating + Maps URL validation** — shipped (PR #100, 18 Jul). Live QA found a venue created with a 13-digit seat count/price (2.2e90 total seats, 2.2e136 per-seat pricing) — the number inputs had `min` but no `max`, and neither form runs native HTML5 validation (custom submit handler). Server now enforces seats 1-100,000, price ₹0-1cr, acoustic rating 0-5, and Maps link must parse as a real URL, on both `/api/venues` and `/api/events`.
14. PWA screenshots in the manifest — needs 2-3 real screenshots of the app on a phone; ~15 min from Claude once the images exist
15. Prod Play Store package — repeat PWABuilder against `https://www.aforaudience.com` with package ID `com.aforaudience.app` (reserved for this) and a **permanent signing key** (never lose). Only when Razorpay live keys are in and real Play Store submission is desired (weeks out).
16. Legal pages — ✅ Drafted and live on QA (`/privacy`, `/terms`) with a visible "Draft — pending legal review" banner and bracketed placeholders (exact booking fee, refund policy, legal entity name/address, Grievance Officer, payout mechanism). Deliberately not final: needs a CA/lawyer review pass once the company is registered, then placeholders filled and the banner removed before promoting to prod.

**Newly unblocked, not yet started (identified session 5, 18 Jul; updated 19 Jul):**
- ~~Roll the toast `fail()` pattern out to the ~16 remaining dashboard pages~~ ✅ **Done, see item 10 above.**
- ~~Hide/disable the review-rating widget client-side when the viewer isn't checked in~~ ✅ **Done, see deliberately-not-built list above (PR #111).**
- ~~Header not reflecting a just-corrected displayName without a full reload~~ ✅ **Fixed (PR #104, 19 Jul).** Root cause was exactly as guessed: `useSession()` caches client-side and `profile/page.tsx` never told NextAuth to refetch after a successful save. Fix calls the session `update()` function post-save; the server-side `session` callback (`auth.ts`) already re-reads `displayName` from the DB on every check, so this alone was enough.
- ~~Two stacked `position: sticky; top: 0` banners (`PhoneVerifyNudge`, `NotificationOptIn`) probably need a z-index/offset reconciliation~~ ✅ **Fixed (PR #102, 18 Jul, via `NudgeStack`)** — single sticky wrapper replacing the independent banners, see §9.2 below.
- Android `.apk`/TWA push notification checks (`POST_NOTIFICATIONS` permission, notification delegation) — needs the PWABuilder/Bubblewrap-generated Android project's files, not just this repo.
- ~~"Wall of Fame" — Artist of the Month / Event of the Month~~ ✅ **Shipped (PR #114, 19 Jul).** See §9.3 entry below for full detail; entry left there marked done rather than deleted, for history.
- ~~Persona value-prop blocks (Audience / Organiser / Venue / Artist) on the homepage~~ ✅ **Shipped (PR #113, 19 Jul).** Copy + layout only as scoped, no backend touched.



### 9.2 Real gaps found through live testing (not hypothetical)

| Item | Detail |
|---|---|
| **Artist Payment Terms "Paid" allows blank Fee per artist at Publish** | ✅ **Fixed (session 32, 26 Jul, PR #189, merge `c924b87`).** Confirmed live (organiser-role testing, 25 Jul): selected Paid, left "Fee per artist" empty, event published successfully (`DIM MIC -201 MIC OPEN`). Fix: shared `src/lib/money-validation.ts` `parseAmount()` util now requires Fee per artist (bounded ₹1-₹1,00,00,000) whenever Payment Terms = Paid, at Publish, both client-side (create + edit pages) and server-side (`POST /api/events`, `PATCH /api/events/[id]` - the real enforcement point). `DIM MIC -201 MIC OPEN` itself left untouched, same treatment as `NIKI 4700`. |
| **Offer Amount (negotiated-pricing Manual Canvas venue) not validated at Publish - inverts the negotiation flow** | ✅ **Fixed (session 32, 26 Jul, PR #189, merge `c924b87`).** Confirmed live (organiser-role testing, 25 Jul): blank Offer Amount does not block Publish, unlike per-section pricing on Numbered venues which correctly blocks with "Please set a price for every section." Published `DIM MIC - 200 LINEUP` against `NiKi 2600 MANUAL Canvas` with Offer Amount blank. Fix: Offer Amount now required (client + server, via the same shared `parseAmount()` util) whenever a venue is attached at Publish - applied to both `POST /api/events` (create) and `PATCH /api/events/[id]` (edit), plus the separate `POST /api/venue-bookings` route (edit-page's direct-booking path), which turned out to have **no** amount validation at all before this fix. `DIM MIC - 200 LINEUP`'s existing blank-offer state left untouched. |
| **Create Event validation banner doesn't re-validate after fields are corrected** | ✅ **Fixed (session 36, 26 Jul, PR #201, merge `07d0433`).** Confirmed live (organiser-role testing, 25 Jul, mobile): empty-form Publish attempt correctly shows a banner listing missing required fields (Title, Description, Date, Start Time, End Time, Total seats) - this part is working as designed, and has a dismiss (×) button so it's meant to persist until user action, not auto-vanish like a toast (this is the right call - a toast would risk the user losing track of what's still wrong). The actual bug: after filling in Title, Description, Date, Start Time, and End Time, the banner still displayed the original unchanged message, still listing those now-valid fields as missing. It wasn't re-checking form state after the initial failed submit. Fix: a `useEffect` (skipping the mount render) clears the banner as soon as any relevant field changes - title/description/date/times/seats, pricing, venue selection, tier prices, performer/booking settings - so editing a flagged field itself counts as the "user action" that dismisses it, rather than requiring a resubmit. (Separately flagged mobile's "LINEUP" Event Type vs. laptop's "OPEN MIC" as a possible mismatch - Hitesh confirmed LINEUP was a deliberate manual selection on that device, not a bug. No further action.) | **Not yet click-tested live** - needs a real Create Event attempt on QA: trigger the banner, correct the fields, confirm it clears without resubmitting. |
| ~~Login "CSRF never fires" concern (carried in session handoffs, never actually in this doc)~~ | ✅ **Closed as a non-issue (session 27/28, 23 Jul), not a real bug.** Live-verified via Claude in Chrome + Hitesh: a fresh network trace on `/login` page-load shows only `/api/auth/session` firing, no `/api/auth/csrf` - this is genuinely correct NextAuth v4 behavior (`getCsrfToken()` is called lazily inside `signIn()` at submit time, not on page load) rather than a bug. Code review of `(auth)/login/page.tsx` confirms a completely standard `signIn("credentials", {..., redirect:false})` call - nothing app-side bypasses NextAuth's built-in CSRF handling. Confirmed conclusively by a real login: Hitesh signed in for real and was redirected to `/` with a valid authenticated session (nav showed `Hi, E2E` / Dashboard / Sign out) - the credentials provider cannot succeed at all without a valid CSRF handshake completing first, so a successful login is itself proof the flow works. Likely origin of the original concern: an earlier session checked only page-load network traffic (same thing done here first) and mistook normal lazy-fetch behavior for "never fires." Hitesh also confirms he has never personally hit a login failure. No code changes needed - closing this out rather than deferring it a fourth time. |
| **`e2e.yml`'s first real push-triggered run (qa HEAD `27981f6`, 23 Jul) failed — 4/12 tests red, not flaky** | Investigated (session 17/28) rather than just re-run blind. Two distinct failures, both reproducible on chromium-desktop AND mobile-chrome, original + retry: (1) `numbered-seat-booking.spec.ts:71` — "Continue to Checkout" click never navigates to `/checkout/`, stays on `/events/<id>/`. Traced to `POST /api/bookings`: the frontend only redirects when the response includes both `payment` and `booking` (`EventDetailClientPage.tsx`); confirmed via `/api/debug/razorpay-check` that Razorpay QA creds ARE present and reachable on this deployment (`keyMode: test`, `canReachRazorpay: true`), so the fallback isn't a missing-credentials issue — most likely an exception earlier in `bookings/route.ts`'s NUMBERED-path tier lookup (`No pricing configured for section: ...`), which would line up with the still-open "seat-map level-aware pricing bug" below. Not fully confirmed — need the live `bookingError` banner text or a Playwright trace, and both require either a live authenticated click-through (blocked here — no credential entry in a live browser, ever) or downloading the run's trace.zip artifact (blocked here — Azure blob storage host not in this sandbox's network allowlist). **Update (session 30, 25 Jul):** live click-through testing did happen this session (Hitesh, real browser) and confirmed a P0 seat-map race condition causing zone/price data to silently fail to persist (see Session 30 block above) — a strong candidate root cause for the "No pricing configured for section" exception theorized here. Recommend fixing the P0 race condition first, then re-testing this e2e spec, before assuming a separate fix is needed. **Update (session 31, 25 Jul):** the originally-theorized "NUMBERED-path tier lookup / no pricing configured" cause has been independently narrowed - per-tier pricing UI was re-tested end-to-end this session and confirmed working correctly once a venue with a real seat map is attached (live-confirmed on NiKi 2600, Back/Front/Middle priced independently, saved, published). So the pricing-gap theory for this e2e failure is likely NOT the cause after all. Recommend simply rerunning `numbered-seat-booking.spec.ts` against current `qa` before further investigation - it may already be passing, or the real cause is something else entirely (e.g. still the P0 race condition, or something unrelated). (2) `waitlist-wallet-credit.spec.ts` — Artist A's register+apply step times out at 60s with a `[next-auth][error][CLIENT_FETCH_ERROR] Failed to fetch` console error before the browser context can close; happened on all 4 attempts (2 browsers × 2 tries). Could be CI-environment flakiness (concurrent headless contexts hitting the Vercel preview) or a genuine issue in the fixture/registration flow — not yet distinguished. **Next session: get a live click-through (with Hitesh present) on both flows, or pull the trace.zip via a machine with network access to blob storage, before writing any fix.** |
| Dashboard "1fr 1fr" form grids not fully verified on mobile | Systematic search found many `1fr 1fr` grids across Organiser/Venue/Artist dashboard forms. Lower risk than the fixed-pixel-column bugs already fixed — spot-checked, not exhaustively tested. |
| Flexible negotiation has no notifications | ~~Real gap~~ ✅ **Fixed nineteenth amendment.** Both sides now get pushed on every state change (new request, accept/decline/counter) — see EPIC J. |
| **Postgres connection pool exhaustion — live bug, real users affected** | ✅ **Fixed nineteenth amendment.** `EMAXCONNSESSION: max clients reached in session mode - max clients are limited to pool_size: 15` was hitting many routes (`/artists/[id]`, `/api/artists/me`, `/api/auth/[...nextauth]`, `/api/reviews`, more) — first occurrence July 9, so pre-existing, but this session's heavier concurrent testing pushed it into visibly broken pages. Root cause: `prismaPgConfig` in `src/lib/prisma.ts` had no `max` set, so node-postgres defaulted to 10 connections **per Pool instance** — and Vercel spins up a separate instance (own module scope, own Pool) per concurrent invocation, so even 2 concurrent instances could open up to 20 connections against a 15-client cap. Fixed: `max: 1` + `idleTimeoutMillis: 10_000`. Worth remembering for any future serverless-DB config in this codebase. |
| PENDING bookings expire on payment path but not on prod | 15-minute TTL is live on the Razorpay-integrated payment path (sixth amendment), but prod has no Razorpay yet, so an abandoned reservation there still holds capacity indefinitely. Fixes itself when prod gets Razorpay. |
| Postgres-specific TLS bypass still in place | `ssl: { rejectUnauthorized: false }` in `src/lib/prisma.ts` is deliberately still there. Worth testing removal against a real deploy; Supabase's certificate is normally CA-signed, so this may not be necessary at all. |
| Existing users' `displayName` is null | Fix B added the Profile-page edit surface; users must opt in. No backfill was done for existing users. L5 is the follow-up story. |
| **Header doesn't reflect a just-corrected displayName** | ~~From live feedback (`/profile`, 18 Jul): editing displayName updates the Profile page immediately but the header nav still shows the old value until a full navigation/re-render.~~ ✅ **Fixed (PR #104, 19 Jul).** `profile/page.tsx` now calls the session `update()` function after a successful save, forcing NextAuth to refetch immediately instead of waiting for the next window-focus/interval refetch. |
| **Venue Owner application doesn't require phone verification** | ~~Real gap~~ ✅ **Fixed (PR #97, 18 Jul).** Turned out broader than first scoped — the actual gap was every point where an unverified identity becomes something someone else commits to, not just Venue Owner apply. **Settled principle: verify at first external commitment, not at signup or browsing.** Gated: publishing a venue listing (`POST /api/venues`), publishing an event (`POST /api/events`), submitting a spot application (`POST /api/applications` — previously `isVerified` only affected auto-approve eligibility, an unverified artist could still submit into the Organiser's queue), and accepting a venue-negotiation offer (`PATCH /api/venue-booking-requests/[id]`, accept action). Left open on purpose: role-apply itself, profile edits, browsing — same browse-first reasoning as the chatbot's open-access decision. Guard: `requireVerifiedPhone()` in `src/lib/verification.ts`. |
| **Phone-verify nudge banner not sticky on scroll-down** | ~~From the same feedback report — the banner is visible when scrolled to top but not when scrolled further down the page.~~ ✅ **Fixed (PR #102, 18 Jul).** Root cause was a z-index/stacking conflict, confirmed: `PhoneVerifyNudge` and `NotificationOptIn` both used independent `position: sticky; top: 0`, and `SiteNav`'s own sticky positioning covered whichever nudge was showing once nav engaged. Fixed with a new `NudgeStack` wrapper — single sticky container measuring its own height via `ResizeObserver`, publishing a `--nudge-stack-height` CSS var that `SiteNav` now offsets by instead of a hardcoded `0`. |
| **Unbounded numeric inputs (seats/price/rating) + unvalidated Maps URL** | ~~Real gap~~ ✅ **Fixed (PR #100, 18 Jul).** Live QA created a venue with a 13-digit seat count and price (2.2e90 total seats, 2.2e136/seat). Number inputs had `min` but no `max`; both create forms use a custom submit handler so native HTML5 validation never ran regardless. Server now enforces seats 1-100,000, price ₹0-1cr, rating 0-5, Maps link must be a real URL - on both `/api/venues` and `/api/events`. **Same bug family resurfaced 19 Jul on a field this pass missed:** `maxPerformers` on event create had no cap at all (not even client-side `max`) and crashed `prisma.event.create()` with a raw Postgres integer-overflow error when a user typed an enormous number — found by correlating a live feedback report against Vercel's runtime error log rather than by re-auditing the form. ✅ **Fixed (PR #112, 19 Jul)** — capped at 500, client clamp + server validation, matching this row's pattern exactly. Worth a deliberate sweep for any other numeric field that was missed the same way, rather than waiting for the next crash to surface one at a time. |
| ~~Dashboard nav link~~ | ✅ **Already fixed, status corrected (found session 17, 23 Jul).** Was: "Role-based users can only reach Dashboard via Profile, not from the header nav... Still not fixed" (live-confirmed as recently as 21 Jul, session 12). Checked current `SiteNav.tsx` directly - `accountLinks` (which includes a role-based Dashboard link whenever `getDashboardLink()` returns non-null) is rendered inline in both the desktop header row and the mobile menu, not hidden behind Profile or anything else. Must have been fixed as a side effect of some other UI pass without this note being updated - no code changed here, this is a status-only correction found while auditing this table for staleness after finding the same thing with the chatbot icon entry above.
| ~~**Stale session role blocked Venue Owner from responding to a Flexible offer**~~ | ✅ **Fixed (PR #155, 21 Jul, session 12).** Live-tested: Vijay (venue0001, DB role correctly `VENUE_OWNER`) opened Flexible Requests on Mahesh's ₹2,999 offer and saw no Accept/Counter/Decline controls - just "Waiting on the other side to respond," the organiser-perspective copy. Root cause: NextAuth's session callback already refreshes `displayName`/`isVerified` fresh from the DB on every session check (fix for the exact same class of bug, PR #104) but never extended that to `role` - so a same-session Audience→Venue Owner elevation left the client-side session role stale until a full re-login, which is exactly what resolved it when tried live. Added `role` to that same per-check DB refresh. Also hardened `venue-requests/page.tsx`'s subtitle/back-link so an unrecognized `callerSide` doesn't silently fall into organiser-phrased copy (was `!== 'VENUE_OWNER' ? organiser-copy`, now explicit three-way check). Confirmed via direct DB query the underlying data (single offer, `proposedBy: ORGANISER`, correctly scoped to Vijay's venue) was clean throughout - this was purely a client-session-freshness bug, not a data or authorization bug. |
| ~~**Venue detail page always showed "No seating sections defined yet" for Numbered venues**~~ | ✅ **Fixed (PR #156, 21 Jul, session 12).** Live-confirmed on the published "Jaipur Ja Mic 100" (341 real seats, 4 zones: General/Back/Zone 3/Zone 4) - `dashboard/venue/[id]/page.tsx` only ever read `venue.seatMap.sections` (GA-only field); Numbered venues' real zones live in `Seat`/`VenueZonePrice` instead (same data event-creation pricing already reads correctly per PR #151). `GET /api/venues/[id]/owner` now includes `seats` (tierLabel) and `zonePrices`, grouped by level+zoneName on the page. `Venue.capacity` itself was already syncing correctly (300→341 after the seat map save) - only the zone list was stale. |
| ~~**Event detail page showed a bare dash for Ticket Price on multi-tier events**~~ | ✅ **Fixed (PR #156, 21 Jul, session 12).** Live-confirmed on "Jaipur Mic Gala 100" (Back ₹600/General ₹1000/Zone 3 ₹600/Zone 4 ₹500) - the organiser event detail page only read the flat `event.ticketPrice` field, null for tiered events. `GET /api/events/[id]/owner` now includes `ticketTiers`; page shows a price range (`₹500 – ₹1000`) or single price when all tiers match, instead of `—`. |
| **Color-legend swap on seat-map summary (General/Back colors flipped between two views of the same venue)** | Live-observed (21 Jul, session 12), not yet investigated. Same venue ("Jaipur Ja Mic 100"), same seat counts, but the summary legend showed 🔴 General/🟢 Back in one screenshot and 🔴 Back/🟢 General in another. Not yet determined whether this is a non-deterministic legend render order (cosmetic only) or an actual mismatch between which color maps to which zone in the underlying data (seats possibly mis-tagged) - needs a closer look before fixing either way. |
| **"This page couldn't load" (browser-level error, not an app error state) on clicking "Check approval status"** | Live-reported (22 Jul, session 13) on event "Jaipur Mic Gala 100" (PENDING). Hitesh's instruction: log only, no action needed. Investigated to the extent Vercel logs allow: `PATCH /api/events/[id]/` for this event returned `200` in the runtime log at the same timestamp as the click, followed by a successful `GET /api/events/[id]/owner/` `200` - no 4xx/5xx, no error-level log entries, no runtime-error cluster on this route in the surrounding window. Button itself is a plain `<button onClick={togglePublish}>` (`organiser/events/[id]/page.tsx`) - no navigation, no form submit - so the code path doesn't explain a full browser-level connection failure the way it would explain an in-app error state. Best available read: transient client-side/network blip (the browser's native "couldn't load" screen, not a React error boundary or a styled in-app error), not a reproduced server or app bug. Not investigated further per instruction; re-open with a fresh repro if it recurs. |
| **Silent admin action failures — found while scoping the toast rollout, not from a feedback report** | ✅ **Fixed (PR #106, 19 Jul).** Auditing all 22 dashboard pages for the toast rollout surfaced 2 pages with no error handling at all, not just an unstyled banner: `dashboard/admin/page.tsx`'s approve/reject on pending Organiser/Venue Owner applications, and `dashboard/admin/feedback/page.tsx`'s status-update action. Neither checked `res.ok` — a failed PATCH (403, 404, network error) looked identical to a successful one, the row just silently didn't move. Both now check `res.ok` and surface the server's real error via toast, plus a success toast on confirmed state changes (previously none). |
| Homepage hero right-column void | ~~Noted, not addressed.~~ ✅ **Fixed (PR #85, this session)** — see hero section history in recent PR commits. |
| Free events not getting PDF/email | ~~Root cause identified~~ ✅ **Fixed seventh amendment (EPIC M1).** |
| Ticket booking has no phone-verification gate | ~~Real gap~~ ✅ **Fixed (thirteenth amendment).** `/api/bookings` and `/api/venue-bookings` now reject unverified users (`reason: PHONE_NOT_VERIFIED`); frontend redirects to the new `/verify-phone` completion flow. Email verification, by contrast, is already correctly non-blocking by design — confirmed via code review, no gap there (see `verify-email/route.ts` comment). |
| ~~**Venue create form loses all data on the phone-verify detour**~~ | ✅ **Fixed (PR #155, 21 Jul, session 12).** Chose option (a) from the two candidates below: `dashboard/venue/create/page.tsx` now snapshots all form state to `sessionStorage` on every change, restores it on mount if present, and clears it on successful submit or explicit Cancel. Live-confirmed the verify-phone round trip no longer wipes the form. (Option (b) - verify-before-filling - was ruled out as it would've fought the browse-first/gate-at-commitment principle and directly contradicted the Draft-gating fix shipped alongside it, below.) |
| ~~**Phone-verification gate fires on Draft saves, not just Publish (should only gate real commitment)**~~ | ✅ **Fixed (PR #155, 21 Jul, session 12).** Both `POST /api/venues` and `POST /api/events` now parse `req.json()` first and only call `requireVerifiedPhone()` when `publish === true` - Draft saves never gate, matching `verification.ts`'s own documented principle. Live-confirmed the fix resolves the original report. |
| ~~**No way back to Guided-Setup-vs-Draw-It-Myself once seats exist ("no back navigation after save")**~~ | ✅ **Fixed (PR #157, 22 Jul, session 13).** Dropped the stale `seats.length === 0 &&` guard on the "← Back to setup options" link (`backToChoice()` only flips local view state, never touched `seats`, so the guard wasn't protecting any real data loss) - link now always shows, matching the wizard's own unconditional "Change approach" equivalent. |
| ~~**Seat-picker overlap + illegible labels (audience-facing `SeatPicker.tsx`)**~~ | ✅ **Fixed (PR #157, 22 Jul, session 13).** Hitesh live-reported overlapping/unreadable seats on the "Jaipur Mic Gala 100" checkout sidebar. Root cause: `x`/`y` position was percentage-based (scales with the container) but `SEAT_SIZE` was a fixed 22px, so seat squares stayed constant size while the spacing between them shrank at narrower render widths. Fixed by expressing width/height/margin as percentages of the canvas on their own axis (proven equivalent to a true square regardless of the container's aspect ratio), plus a CSS container query (`cqw` units) so the row/seat label font-size scales with the container too, instead of staying a fixed unreadable 9px. |
| ~~**Stale "Select at least one seat first" error message survives seat selection**~~ | ✅ **Fixed (PR #157, 22 Jul, session 13).** `EventDetailClientPage.tsx`'s `bookingError` was only cleared inside `reserveSeats()`, never on a seat-selection change - so clicking "Continue to Checkout" with zero seats, then selecting seats afterward, left the stale error on screen even as the seat count/price updated correctly. Now cleared in both `updateSeat()` (GA path) and the `SeatPicker` `onChange` (Numbered path). |
| ~~**Checkout page shows no seat/zone breakdown for Numbered bookings**~~ | ✅ **Fixed (PR #157, 22 Jul, session 13).** Numbered-venue bookings store `seats: {}` by design - the real per-seat picks live in the `BookingSeat` join table instead (see §9.4 seat-map entry) - but `GET /api/bookings/[id]` never fetched that relation, and the checkout page rendered its line items straight from `booking.seats`, so a Numbered booking showed the event, the booking fee, and the total with nothing in between. Route now includes `bookingSeats` + `seat`, resolves price the same way `GET /api/events/[id]/seats` already does (`Seat.tierLabel` matched to `TicketTier.sectionName`); checkout page groups seats by tier and shows seat labels + a computed per-tier amount, in both the pre-payment summary and the post-payment "You're in!" confirmation. GA path (`booking.seats` qty map) is completely untouched. |
| **Vague error when a seating section is missing its name** | From live testing (20 Jul) — confirmed via code review, not yet fixed. Both `dashboard/venue/create/page.tsx` and the `[id]/edit` page filter sections with `sections.filter(s => s.name.trim() && s.seats > 0)` before submit - a section with seats and price filled in but no name is silently dropped, not flagged individually. If that's the only section, the user sees the generic "Add at least one seating section with a name and seat count," which reads as "you added nothing" when they feel they did. Fix should validate per-row and point at the specific incomplete section (e.g. inline red border + "Name required" under the empty field) rather than a single aggregate message after the fact. |
| Free event checkout shows "₹0" instead of "Free" | ~~From live feedback (15 Jul, category BUG) — "paid event in free amount to replace 0 rupees." Cosmetic, not investigated this session.~~ ✅ **Fixed (PR #104, 19 Jul).** Confirmed cosmetic-only, not a payment bypass — free events (`subtotalAmount === 0`) already auto-confirm server-side and never see a fee. Label fixed in 3 spots on the checkout page: pre-payment total, Pay button text, post-confirmation "Amount paid" summary. |
| Artist username auto-suggestion not working | ~~From live feedback (15 Jul) — register form's username suggestion feature reportedly broken. Not investigated this session.~~ Partially investigated (19 Jul): confirmed via git history that the core feature (live availability check + case-insensitive matching) works correctly, shipped 14 Jul. One concrete bug found by code review and fixed (PR #105) — `suggestAvailableUsername()` had an off-by-one always skipping the first numbered fallback (e.g. suggesting "hitesh2" when "hitesh1" was actually free). Could not reproduce further — no live browser access in this environment, Vercel log retention doesn't reach back to 15 Jul. Not treated as full closure of the original report; re-open if it resurfaces with a specific repro. |
| ~~Admin/Organiser dashboard "arrow-connected sections" UX~~ | ✅ **Fixed (PR #132, 19 Jul).** Recurring feedback (15 Jul and 18 Jul, two separate reports) - the admin dashboard's top nav rendered each link as text suffixed with " →" (e.g. "Feedback →"), which in a row read as a connected sequence rather than five independent destinations. Replaced with distinct bordered pill buttons, no arrow suffix. Confirmed the pattern was specific to this one admin nav row - organiser/venue/artist dashboards don't have it, no further scope needed. |
| ~~Pre-existing type error blocks `next build`'s typecheck step~~ | ✅ **Fixed (PR #115, 19 Jul).** `otp/request/route.ts:58` — `targetPhone` was provably non-null on every real code path but TS couldn't narrow it through the if/else reassignment chain; added an explicit guard. §9.2 sweep (19 Jul, session 3): the suspected follow-on error at `api/events/[id]/attendees/route.ts:41` doesn't actually exist — `bookings.map((b) => ...)` infers `b`'s type from Prisma's `findMany` return shape, never `any`. Confirmed against the live Vercel build log for the current qa HEAD (c578a85): "Finished TypeScript in 12.9s", zero errors, build green. Misdiagnosed at the time under `next build`'s early-abort typecheck; no dedicated sweep needed, the queue was empty. |


### Session 30 / 25 Jul 2026 — Extensive manual QA pass (Hitesh, web + mobile, ~125 findings)

Entire session was live manual testing across both new-account role-approval flows and venue/seat-map creation (Section, Numbered, Manual Canvas, multi-level). Full findings list (37 items) filed to the `Feedback` table (category=BUG, status=NEW) this session for session-start reconciliation; the structurally important ones are summarized here.

| Item | Detail |
|---|---|
| **P0 — Seat Map Builder race condition, confirmed real data loss** | ✅ **Root cause fixed (session 32, 25 Jul, PR #188, merge `20d7eed`).** Generated/manually-placed seats vanish (variable timing, even while the tab is backgrounded); a subsequent Save reports "Saved 0 seats" after seats were genuinely placed. **Root cause confirmed via code inspection, not just hypothesis:** both the seat-map builder and the Venue Booking Requests list had `useEffect(() => { if (session?.user) fetchX() }, [session])`. next-auth's `useSession()` hands back a new session object reference on its own schedule (window-focus revalidation, internal polling) unrelated to anything actually changing - on the seat-map builder this silently re-ran the fetch mid-edit and overwrote unsaved local seats/zone prices; on the requests list it caused the opposite symptom (stale, since that unreliable trigger was the only refetch signal). **Fix:** both effects now key off `session?.user?.id` (only changes on a real login/logout) instead of the `session` object; the requests list additionally got an explicit 20s poll (matching the Revenue page's existing pattern) since it's read-only display with no local edits to protect. Confirmed via code read that the Revenue page's own poll is a separate, intentional `setInterval` mechanism, not part of this root cause - left untouched. One clean save *did* succeed previously when a manual placement happened to beat the race ("Saved 4 seats across 1 level") - confirms the save/persistence layer itself was always fine; the bug was specifically in the pre-save window, now closed. `NIKI 4700 Duplex Canvas` (QA) is still live with only 4 seats/1 level as the historical consequence - **left deliberately untouched, not re-tested/re-saved as part of this fix**, per the standing sequencing-risk note; `DIMPAL 101 MIC Hold` stays `PENDING` until Hitesh gives the go-ahead to touch that venue. **Next step:** re-run `numbered-seat-booking.spec.ts` against current `qa` now that this is fixed - may resolve the still-open e2e failure below too, since the "no pricing configured for section" theory traced back to this same race condition. |
| **P0/P1 — Venue Booking negotiation has no upper-bound price validation, one CONFIRMED record now holds a nonsensical amount** | ✅ **Root cause fixed (session 32, 26 Jul, PR #189, merge `c924b87`).** Confirmed live (organiser-role testing, 25 Jul) - overflow-scale garbage entered as counter-offers on `DIM MIC - 200 LINEUP` (against `NiKi 2600 MANUAL Canvas`) reached `CONFIRMED`. Verified via direct DB query: `VenueBooking` id `cms0oc890000504jigj4zfkq0`, `status=CONFIRMED`, `amount=3.33333333333333e+90`. Root cause: `PATCH /api/venue-booking-requests/[id]` (action `counter`) only checked `Number(amount) <= 0` - no upper bound, and no `Number.isFinite` guard (a non-numeric string would have produced `NaN` and slipped through the same check). Fix: now uses the shared `parseAmount()` util (₹1,00,00,000 cap, `Number.isFinite` guard, required). **`cms0oc890000504jigj4zfkq0` itself is left untouched, same treatment as `NIKI 4700` - not deleted/corrected without Hitesh's explicit go-ahead; this fix only prevents new bad data going forward.** |
| **Publish-gating for Numbered venues did not block an incomplete venue from publishing** | ✅ **Fixed (26 Jul, session 34, PR #193).** Old check was `seatCount > 0` only. New check requires every distinct `(level, tierLabel)` zone with real seats to carry a `VenueZonePrice` row with a real positive `suggestedPrice`. Verified against live QA data before shipping: 3 currently-published venues (NIKI 4700 Duplex Canvas, Niki 1100 Numbered, Venue023-001) had unpriced zones - confirmed this was a real, live gap. Gate only applies going forward. **Evidence cleaned up (26 Jul, same session, Hitesh's call):** those 3 venues were kept unpriced deliberately as evidence while this fix was being built; now that it's shipped and documented, placeholder `suggestedPrice` rows were added for their zones (General/Back/Zone 3/Zone 4, ₹300-800 range - a prefill default only, organisers override at event creation same as always) via Supabase MCP, so they're no longer sitting in a broken state that could confuse future testing or organiser event creation against them. |
| **No snapshot/lock on venue name, address, or seat map once bookings exist** | Confirmed via schema: `Event.venueId` and `Venue.seatMap` are live mutable references, no snapshotting. Editing these today silently changes what existing ticket holders are relying on. Precedent already exists for pricing (`VenueBooking.snapshotHourlyRate`/`snapshotDailyRate`) — not yet extended to identity/structure fields. |
| **Price Range displays "—" on 3 of 4 test venues** | ✅ **Fixed (26 Jul, session 34, PR #194).** Same field-mismatch bug family as PR #151/#156 - three separate call sites (owner venue list `dashboard/venue/page.tsx`, public listing `src/app/venues/page.tsx`, owner venue detail stat box `dashboard/venue/[id]/page.tsx`) each only ever read the GA-only `seatMap.sections` field, never `VenueZonePrice`. The venue-detail page had already fixed its zone *list* to resolve real prices (PR #156-era `numberedZones`) but the Price Range stat box above it was never updated to use it - same file, two different gaps. All three now branch on `seatingMode` and read `VenueZonePrice` for NUMBERED venues. Verified against live QA data before shipping: 8 NUMBERED venues with real priced zones (₹300–₹10,000 range) now compute correctly; the 2 zero-seat draft venues correctly still show "—". |
| **Free-text fields confirmed producing garbage public-facing content today** | Venue Address/City/Facilities and Artist `genre` are all unvalidated free text. Directly observed: a venue's public page header rendered literal placeholder test copy as its subtitle; an artist has their own name ("Aman") as a public genre filter chip (confirmed via direct DB query); casing variants ("standup comic" vs "Standup comedy") create duplicate filter chips instead of merging. **Facilities partially fixed (session 36, 26 Jul, PR #203, merge `84a3c33`)** — replaced with a shared `FacilitiesPicker` component: preset chips (Parking, WiFi, AC, Sound System, Stage Lighting, Green Room, Bar/Refreshments, Wheelchair Accessible, Restrooms, Power Backup, Projector/Screen) + an "Other" free-text fallback capped at 200 chars, outputs a plain `string[]` so no schema change was needed. Existing venues with old comma-separated free text round-trip correctly (anything not matching a preset shows up under "Other"). **City fixed (session 36, 26 Jul, PR #210, merge `e489e56`)** — Hitesh provisioned the Google Maps API key (`GOOGLE_PLACES_API_KEY`, Vercel Preview env var, server-only); real Places API (New) autocomplete now replaces the plain City textbox on both venue create and edit, with `Venue.state`/`Venue.country` (new, nullable/additive columns) populated automatically when a suggestion is picked. Server-side proxy (`src/lib/places.ts` + 2 routes) so the key never reaches the browser, session tokens used throughout so a full search-and-pick bills as one Google session, not per-keystroke. Manually typing a city with no dropdown match still works (doesn't hard-block on Google being down) - state/country just stay null in that case. **Not yet click-tested live** - needs a real venue registration on QA confirming suggestions appear and state/country populate correctly. Address and Artist `genre` remain open — `genre` is **confirmed still plain comma-separated free text** (`src/app/dashboard/artist/edit/page.tsx`) despite being settled in §9.5 (searchable multi-select + admin-approval flow) — third instance this session of a §9.5 decision that was never actually built (see the flagged pattern note below `9.5`). |
| **Header nav wraps to a second line when logged in** | ✅ **Fixed (26 Jul, session 34, PR #195).** Root cause: the "home" variant's extra flourish (32px gaps/22px logo/18px padding vs. 24px/20px/16px on every other page) plus a full logged-in account row (Hi, name + Dashboard + My Tickets + Profile + Sign out) demanded more width than a common laptop screen (~1280-1366px) has - `.sitenav-desktop`'s existing `flexWrap: wrap` then kicked in, producing a visibly broken two-line nav instead of true overflow. Fixed with a media query at exactly the affected width band (781-1300px, i.e. desktop-but-not-huge) that collapses home's spacing down to the same values every other page already uses there, plus a slightly narrower search input for extra margin. Purely additive CSS - untouched below 780px (mobile hamburger) and above 1300px (full hero flourish intact). |
| **Both e2e test failures already documented in more depth elsewhere in this doc** | See the pre-existing entry above (dated before this session) covering `numbered-seat-booking.spec.ts` and `waitlist-wallet-credit.spec.ts` with real code-level tracing. **Session 30 update:** that entry's working theory ("exception in `bookings/route.ts`'s NUMBERED-path tier lookup, `No pricing configured for section`") lines up strongly with the P0 seat-map race condition confirmed this session — if zone/price data doesn't reliably persist (proven above), a later booking attempt against that venue would plausibly hit exactly this "no pricing configured" exception. Worth treating the P0 fix as the likely fix for this too, rather than investigating separately. |

| **NUMBERED-venue seat count/availability badge stale, never reflected real bookings** | ✅ **Fixed (session 33, 26 Jul, PR #190, merge `f81f996`).** Confirmed live: "Jaipur Mic Gala 100" had 3 CONFIRMED bookings holding 11 real seats (verified via `BookingSeat` join), yet `Event.availableSeats` still read 341/341, unchanged from `totalSeats`. Root cause: those two `Event` columns are only ever kept accurate on the flat/GA booking path — NUMBERED occupancy lives entirely in `BookingSeat` and was never wired back onto them. **`POST /api/bookings` itself is unaffected and correct** — it already computes live occupancy via `BookingSeat` inside its own transaction, so double-booking was never actually possible; this was purely a stale audience-facing display bug (seat-count text + `getAvailabilityStatus`'s Filling Fast/Sold Out badge, itself unchanged — just fed wrong inputs). Fix: both the event detail page and `GET /api/events` now compute live `totalSeats`/`availableSeats` for NUMBERED venues only (seat count for the venue minus held `BookingSeat` rows for CONFIRMED/unexpired-PENDING bookings against that event — same rule already proven correct in the bookings route), batched via `groupBy` on the listing endpoint to avoid N+1 across events. GA-seating events untouched. |
| **Your Venues page: horizontal scrollbar + Register Venue unreachable on mobile** | ✅ **Fixed (session 36, 26 Jul, PR #202, merge `264f03c`).** Two Feedback reports, same root cause (`8b0897ae`/`be447196`): header action-button row (Revenue Overview / Booking Requests / Flexible Requests / + Register Venue) had `display: flex` with no `flexWrap` - on mobile that's ~550-600px of buttons in one unwrapped row, forcing the whole page wider than the viewport, with Register Venue (last in the row) ending up past the right edge. Fixed with `flexWrap: 'wrap'`. Card grid below had the same latent risk on very narrow screens (`minmax(320px, 1fr)` with no upper clamp) - fixed with `minmax(min(320px, 100%), 1fr)` so the column never exceeds available width. **Not yet click-tested live** - needs a real mobile check on QA with venues present, confirming no horizontal scroll and Register Venue reachable without scrolling. |
| **Event card hover-lift implies whole card is clickable, only the button navigates** | ✅ **Fixed (session 36, 26 Jul, PR #203, merge `84a3c33`).** Feedback `4b91d901`. Hitesh's call: make the whole card clickable rather than remove the hover hint. Outer card `div` is now the `Link` itself; the inner "View Event" button became a visual-only `span` (can't nest an anchor inside another anchor - would break HTML validity/hydration). **Artist listing page already had this right** - its grid cards already wrap the whole tile in a `Link`, code-verified, no change needed there. **Not yet click-tested live.** |
| **Acoustic Rating (0-5) accepts non-numeric input, silently dropped rather than rejected** | ✅ **Fixed (session 36, 26 Jul, PR #203, merge `84a3c33`).** Feedback `1f75f3b4`. `handleAcousticRatingChange` on both Create and Edit Venue already guarded `!Number.isFinite(num)` but just `return`ed with no feedback - value looked like it vanished. Now shows a toast ("Acoustic rating must be a number between 0 and 5"). Found and fixed opportunistically while already touching this handler for the Facilities work below - same file, same function. |
| **No session timeout - session usable after 5+ hours idle** | ✅ **Fixed (session 36, 26 Jul, PR #203, merge `84a3c33`; extended PR #204, merge `c2de757`).** Feedback `a3630648`. Originally scoped Admin-only in PR #203; extended same session to a tiered policy once picked back up: `AdminIdleGuard` generalized into `IdleTimeoutGuard`, keyed off a per-role limit table instead of a single Admin-only check. **ADMIN: 30 min** (account suspension, platform-wide financial visibility justify the strictest policy). **ORGANISER / VENUE_OWNER: 24 hours** (handle real bookings/money, but not full-platform admin power - a middle tier rather than none at all). **ARTIST / AUDIENCE: unchanged, no timeout** (pure browse/attend use - matches this doc's own "gate only at real commitment" principle; forcing idle logout during casual browsing or mid-event ticket-checking would be a real UX cost for no real security benefit at that privilege level). Mechanism unchanged from #203 - mousedown/keydown/touchstart/scroll activity resets a `setTimeout`; expiry calls `signOut({ callbackUrl: '/login?idle=1' })`. Login page's neutral "signed out after a period of inactivity" message (`?idle=1`) is unaffected, still matches the existing `?suspended=1` pattern. |
| **Facilities field free text producing garbage public-facing data** | ✅ **Fixed (session 36, 26 Jul, PR #203, merge `84a3c33`).** See the "Free-text fields" row above (updated in place) for full detail - new shared `FacilitiesPicker` component, preset chips + "Other" fallback, wired into both Create and Edit Venue. |
| **Indigo theme bleeding into intro splash logo/wordmark** | ✅ **Fixed (session 36, 26 Jul, PR #205, merge `9436eb4`).** Feedback `cmrzsjqd4`. A dedicated `--afa-brand-mark` CSS variable already existed for exactly this (never overridden by any theme) - `SiteNav.tsx`'s own logo already used it correctly. The intro splash script (raw `<script>` in `layout.tsx`, not a React component - see the hydration-bug comment nearby) just never got updated to use it: an SVG bar and the first letter of "AforAudience" both still referenced the theme-dependent `--afa-terracotta`, which the indigo theme overrides to a purple-indigo hex. Both switched to `--afa-brand-mark`. `themeColor` (PWA browser-chrome tint) deliberately left theme-dependent - that's a UI-preference tint, not the brand mark itself. **Not yet click-tested live** - needs a real check with indigo theme active, confirming the splash logo stays terracotta. |
| **`PATCH /api/events/[id]` missing `requireVerifiedPhone` on publish** | ✅ **Fixed (session 36, 26 Jul, PR #205, merge `9436eb4`).** Self-flagged while fixing the venue-required-at-publish gap in PR #201 (same route). Added the identical gate `POST /api/events` already had, right before the publish-transition status logic. |
| **Chatbot icon mismatch + no hover animation/tooltip** | ✅ **Partially already fixed, remainder fixed (session 36, 26 Jul, PR #205, merge `9436eb4`).** Feedback `8a1982fb`. Code-checked before assuming: the icon itself was already a deliberate on-brand redesign (`MicIcon` - a stage mic with warm spotlight glow, explicitly documented in `SupportWidget.tsx` as replacing a generic speech-bubble emoji) - that part of the report doesn't reproduce in current code, likely predates the redesign. The genuinely-missing part - no hover animation, no visible tooltip (only an `aria-label` for screen readers) - is now fixed: hover scale(1.08) + shadow transition, plus a native `title` attribute. |
| **Mobile nav tray doesn't close on swipe-up** | ✅ **Fixed (session 36, 26 Jul, PR #205, merge `9436eb4`).** Feedback `f1c26af4`. Simple vertical-delta touch tracking (`touchStartY` ref, 40px threshold) added to the mobile dropdown panel - swiping up now closes it same as the X button. No gesture library needed for a single-direction dismiss. |
| **Back-navigation links inconsistently labeled** | ✅ **Partially fixed (session 36, 26 Jul, PR #205, merge `9436eb4`).** Feedback `07210823`. Full-repo grep found the concrete case: Edit Venue and Venue Sales both link to the identical destination (`/dashboard/venue/${id}`) but used different casing ("Back to Venue" vs "Back to venue") - fixed to match. **Same inconsistency pattern confirmed much broader** - Events pages alone have "Back to Event" / "Back to event" / "Back to Events" mixed across ~10 files, similar for "Back to Dashboard" / "Back to dashboard". Deliberately not swept in this PR (would've been a 20+ file diff for a cosmetic issue) - flagging as its own follow-up rather than silently expanding scope. |
| **Seat-mapping option shown when editing a GA (Section-based) venue** | ✅ **Fixed (session 36, 26 Jul, PR #205, merge `9436eb4`).** Feedback `71bfecbc`. Traced to the "Your Venues" list card (`dashboard/venue/page.tsx`), not the Edit page itself - the card's "Seat Map" button showed unconditionally for every venue, including GA ones, leading straight to a page that says "this doesn't apply to you, manage pricing from Edit instead" (that fallback messaging was already correct, just reachable from a button that shouldn't have been there). Now only rendered when `venue.seatingMode === 'NUMBERED'`. **Note:** the Edit Venue page's own "Have real numbered seats instead? → Open Seat Map Builder" upsell (shown on GA venues) is a separate, deliberate feature - lets an owner switch a venue into Numbered mode - and was left untouched. |
| **Seat numbers not visible on ticket** | ✅ **Fixed (session 36, 26 Jul, PR #205, merge `9436eb4`).** Feedback `cmrxnrzfr`. Root cause was deeper than a display bug: neither the ticket PDF nor the on-screen `/tickets/` list ever read `BookingSeat`/`Seat.row`/`Seat.number` at all - both only ever used `Booking.seats` (a `Record<string, number>` section-name→count JSON blob), which has no concept of an individual seat. New shared `src/lib/seat-labels.ts` (`formatSeatLabels`, sorts by level/row/number, numeric-aware so A2 sorts before A10) wired into all three real touch-points: `GET /api/bookings/[id]/ticket` (on-demand PDF), `src/lib/ticket-delivery.ts` (email delivery), and `GET /api/bookings/my` + `/tickets/page.tsx` (on-screen list). GA bookings unaffected - no `BookingSeat` rows exist for them, so `formatSeatLabels` returns empty and all four call sites fall back to the pre-existing section+qty summary. **Not yet click-tested live** - needs a real Numbered-venue booking on QA, confirming the seat numbers appear correctly on both the downloaded PDF and the `/tickets/` page. |
| **Bare "Loading..." text, no branded UI, on ~27 lower-traffic pages** | ✅ **Fixed (session 36, 26 Jul, PR #206, merge `977e13e`).** Feedback `da4379a0`. `BrandLoader`'s own code comment already flagged this exact follow-on when it was first built for the 4 highest-traffic dashboards + profile ("~27 lower-traffic Loading... spots elsewhere are a natural follow-on, not done blind in this pass"). Swept 26 files from bare `<div>Loading...</div>` (or the `<Suspense>` fallback equivalent) to `<BrandLoader />`. Two occurrences deliberately left untouched - a different shape entirely, not full-page loaders: an inline attendee-list loading text on the Check-in page, and a button's temporary label inside a ternary on the Event Detail page. |
| **Google Maps Link validation too permissive** | ✅ **Fixed (session 36, 26 Jul, PR #206, merge `977e13e`).** Feedback `session36-mapsurl-f9ffa4a4`. Previous check only verified "is this a URL" (`new URL()` + protocol check), not "is this plausibly a Google Maps link" - `new URL("https:\\GARBAGEVALUE")` (backslashes) parsed cleanly per the WHATWG URL spec (browsers/JS treat `\\` like `//` for `https:`) and slipped straight through. New `src/lib/maps-url.ts` adds a real domain allowlist (`google.com`/`goo.gl`/`g.page`, subdomain-boundary-safe so `evilgoogle.com` and `google.com.evil.com` are both correctly rejected) on top of the existing syntax check. Wired into both venue create AND edit routes - edit had no `mapsUrl` validation at all before this. Verified with 11 automated test cases (`npx tsx`) before shipping, not just reasoned about. |

| **Venue booking calendar overlap enforcement unconfirmed** | ✅ **Confirmed real gap and fixed (session 36, 26 Jul, PR #207, merge `f2e6dce`).** Feedback `0ed1ec74`. Code inspection confirmed this wasn't just "unverified" - there was genuinely no overlap enforcement anywhere. Two different Organisers could both land a `CONFIRMED` `VenueBooking` for overlapping dates at the same venue. Fixed at both real confirm paths: `PATCH /api/venue-bookings/[id]` (standard interval-overlap test against other `CONFIRMED` bookings) and `POST /api/venue-booking-requests/[id]` `action=accept` (Flexible-venue negotiation acceptance, which creates a `CONFIRMED` booking directly in its own transaction and was bypassing the other route's guard entirely). Only checks against already-`CONFIRMED` bookings - a pending negotiation isn't a real commitment yet. **Not yet click-tested live.** |
| **Canvas header fields read confusingly to non-technical venue owners** | ✅ **Fixed (session 36, 26 Jul, PR #207, merge `f2e6dce`).** Feedback `47c26606`. "Section/Tier" → "Zone name", "Row:" → "Row letter:", "Next #:" → "Next seat #:", plus a one-line hint explaining zone name is what gets priced later. Applied consistently to the header inputs and the "Selected seat" edit panel. |
| **Seat-map view sometimes fails to render fully** | ✅ **Real root cause found and fixed (session 36, 26 Jul, PR #207, merge `f2e6dce`).** Feedback `78cb970a`. Not flakiness - the draft-restore `window.confirm()` fired synchronously right after several `setState` calls in the same data-fetch callback. A blocking `confirm()` can freeze the DOM in its pre-update state before React commits/paints the just-loaded seat data, matching exactly what was reported ("resolves after... dismissing the confirmation dialog"). Deferred the whole draft-check block via `setTimeout(0)` so the browser paints loaded seats first. **Not yet click-tested live** - needs a real load of a venue with both server-saved seats AND a pending local draft, on a slower connection/device where the timing gap would actually show. |
| **Artist profile pages show no photo/background styling** | ✅ **Investigated, found stale (session 36, 26 Jul).** Feedback `8560fb61`. Code-checked `ArtistProfileClientPage.tsx` before assuming - does not reproduce. Already has a deliberate `var(--afa-plum-black)` header background, a circular avatar with a styled initial-letter fallback (not a broken/empty box), genre badges, hype score, style tags. Marked resolved as stale rather than guess-fixing something already correct in code. |

Live count as of this reconciliation: **12 NEW + 4 REVIEWED** bug-category items remain open in the `Feedback` table (see table for full text of each) - mostly Manual Canvas mobile-responsiveness/UX polish, plus a handful of bigger-scope items needing a design decision before building (venue-identity snapshot/lock, Artist genre redesign, Venues/Artists listing visual redesign).

**Session-start reconciliation (26 Jul, session 35/internal) - full Feedback sweep against this doc before any new code.** Found and re-confirmed 3 items already fixed this session that Feedback hadn't been updated to reflect (publish-gating bypass → PR #193, Price Range dash → PR #194, header nav wrap → PR #195) - all corrected to `RESOLVED` with a changelog entry. Everything else cross-checked clean, including two 15 Jul `REVIEWED` items (username auto-suggestion, artist-profile-clickable-from-lineup) which remain accurately tracked as-is.

**Manual Canvas: no autosave/draft persistence** ✅ **Fixed (session 35, 26 Jul, PR #196, merge `8114446`).** Confirmed: an accidental scroll-triggered refresh wiped the entire in-progress layout with no way back - a genuine data-loss bug, distinct in severity from the other 29 Manual Canvas UX items in the batch above. Fix: the in-progress layout (seatingMode, levels, seatsByLevel, gridConfigByLevel, zonePricesByLevel, builderPathByLevel) now mirrors into `localStorage` per venue id on every change. On next load, if a non-empty local draft exists, the owner is asked to restore it or discard it - never silently applied over the server-loaded layout, never silently thrown away, since the two could legitimately differ (stale draft vs. real accidental-refresh loss). Draft clears automatically on a successful Save. Gated behind a `hydratedRef` so the initial load + restore-decision always settles first, before the autosave effect could otherwise fire against default/empty state and clobber a real unrestored draft. Convenience layer only - Save remains the sole explicit, server-side source of truth. **Not yet click-tested live** - no browser access this session; next step is Hitesh placing seats, refreshing mid-edit, and confirming the restore prompt appears and works.

**Edit Event: diverged pricing form for Numbered venues** ✅ **Fixed (session 35, 26 Jul, PR #197, merge `2a05283`).** Confirmed: Edit Event was on an older pricing form that never derived per-zone pricing for Numbered venues at all - only a flat `ticketPrice` field, which doesn't apply to tiered venues (Feedback, 25 Jul). Deeper than a frontend gap: `PATCH /api/events/[id]` never accepted `ticketTiers` in the body at all, so even a fixed form would have had nowhere to persist it - only `POST /api/events` (creation) ever wrote them. **Backend:** PATCH now accepts/validates `ticketTiers` (same bounds as POST) and full-replaces them (`deleteMany` + `create`) whenever the field is present in the body - only touched when the client actually sends it, so an unrelated PATCH (e.g. toggling `surpriseAct`) can't wipe existing tiers by omission. Confirmed safe to delete+recreate: bookings resolve a tier by `sectionName` string match at read time (`POST /api/bookings`), never by `TicketTier.id` - existing bookings are unaffected by fresh ids. Nulls the flat `ticketPrice` once tiers are in play, same convention as POST. **Frontend:** `venueSections`/`numberedZoneSections`/`usingTierPricing`/`suggestedAmount` derivation now mirrors the Create page exactly. `tierPrices` prefill prioritizes the event's own already-saved `ticketTiers` over the venue's mere suggested default - re-opening Edit doesn't silently reset prices an organiser already set. Offer Amount's suggested-rate auto-fill deliberately only fires when the venue is actually being *changed* away from what's currently attached - unlike Create (nothing to preserve yet), Edit must not silently overwrite an already-negotiated Offer Amount just because the suggestion recalculates (e.g. the organiser tweaks the event's time). Total Seats input replaced with the same per-section pricing table as Create when the attached venue is Numbered; unchanged flat input for GA venues. **Not yet click-tested live** - needs a real edit of a Numbered-venue event on QA: confirm the price table renders with existing prices prefilled, saves, and reloads correctly.

**Manual Canvas: three correctness bugs** ✅ **Fixed (session 35, 26 Jul, PR #198, merge `41d2d89`).** (1) **Click-to-select created a duplicate seat instead** - `startDrag`'s `stopPropagation()` on mousedown doesn't stop the separate `click` event fired afterward (mousedown+mouseup, no movement), which bubbles to the canvas's `onClick={placeSeat}` after `dragId` has already cleared via the drag effect's `onUp` - fixed by stopping the click on the seat element itself, independent of drag-state timing. (2) **Duplicate seat labels on manual Row/Next# reset** - resetting the counter (e.g. back to B/1 to patch in a missed seat) had no collision check; normal auto-incrementing during placement never hit this since it always counts up past what exists. `placeSeat` now checks the active level's seats for a Row+Number collision before placing, same scope as the existing Save-time duplicate check, and blocks with a toast instead of silently duplicating. (3) **Numeric fields couldn't be cleared directly** (side margin, seat spacing, rows, columns, gangway position, vertical-aisle gap/position%, Next#) - required overtype-then-delete, since each field's value was bound directly to a number and clearing it immediately re-rendered the old value (React has no "temporarily empty" state for a controlled number). New shared `ClampedNumberInput` buffers the field's own display text locally so it can genuinely go blank while typing, commits valid keystrokes live, and enforces min/max only on blur - not on every keystroke, which would otherwise reintroduce the same snapping the moment a value dips below the minimum mid-type. All 15 affected inputs across the wizard and advanced/fine-tune panel switched over. **Not yet click-tested live** - needs a real test on QA: select an existing seat without dragging it, reset Row/Next# and try placing over an existing label, and clear/retype each numeric field above.

**e2e automation attempt for session-35 fixes (26 Jul, same session, unresolved - flagged for manual verification, not further automated debugging).** Built real Playwright coverage for all 4 fixes (PR #196-#199): `e2e/login-code-case.spec.ts` and `e2e/manual-canvas.spec.ts`, a new fixture (approved Venue Owner + Numbered venue, `e2e.fixture.venueowner@example.com` / `e2efixturevo0001ven`, 2 seeded seats), and two `data-testid` hooks on the seat-map canvas (PR #200, merged). Along the way, fixed a real CI blind spot: job logs and artifact downloads both redirect to Azure blob storage, unreachable from Claude's sandbox - `e2e.yml` now posts a results summary as a commit comment via the GitHub API instead (needed an explicit `permissions: contents: write`, since the default `GITHUB_TOKEN` is read-only unless a workflow requests otherwise). **Results: `login-code-case.spec.ts` (4/4) and the pre-existing `smoke.spec.ts` (6/6) are reliably green across every run - real, repeated confirmation PR #199 works.** `manual-canvas.spec.ts` failed consistently across ~6 dispatched runs despite several genuine fixes along the way (missing `data-testid`s merged via PR #200; a real cold-start-timeout gap fixed by matching this file's own 25s convention). Root cause partially isolated but not fully closed: email-based login for the new fixture Venue Owner account fails consistently in CI even though the DB row was verified byte-exact correct (email, bcrypt hash, no lockout) - logging in with the *same account's* AFA code instead succeeds. Switched the fixture helper to code-based login as a pragmatic workaround, but the full suite still failed after that change too. **Per new standing rule (any scenario failing 2+ times in automated runs gets flagged for manual verification instead of further auto-debugging), this is now that flag** - the actual Manual Canvas product fixes are not in question (PR #198 code reviewed carefully, matches the exact bug mechanics described in the original Feedback reports); what's unresolved is only the automated test harness's ability to exercise them in CI. `numbered-seat-booking.spec.ts` and `waitlist-wallet-credit.spec.ts` continue to fail/timeout in the same run, consistent with their existing carried-forward status elsewhere in this doc - not a new regression. **Next step: a real manual click-through on QA** (place/select/reset/clear-field scenarios listed in the PR #198 entry above), not more CI iteration. The e2e spec files, fixture, and CI summary-comment infrastructure all still live on the unmerged branch `test/e2e-session35-fixes` - worth merging once someone (Hitesh or a future session with browser access) has verified the underlying behavior manually, at which point the manual-canvas spec's login flakiness becomes a lower-stakes problem to chase separately.

**Login code (AFA/ART/ORG/VEN) case-sensitive** ✅ **Fixed (session 35, 26 Jul, PR #199, merge `1347dcb`).** Confirmed: a correct code typed in lowercase or mixed case failed with the same generic invalid-account error as a genuinely wrong one. Codes are always generated uppercase by the DB's `generate_role_code()` function, but `resolveIdentifierToUser` did an exact-case `CODE_PATTERN` regex test and exact-case lookup against `User.code`/`Artist.code`/`Organiser.code`/`VenueOwner.code`. Fix uppercases the input before both the pattern test and each of the four lookups - mirrors what the other two identifier types on this same field already do for their own casing quirks (email lowercased, username matched case-insensitively). **Not yet click-tested live** - needs a real login attempt with a known code typed in lowercase/mixed case on QA.

**Data-hygiene notes (not code bugs, flagged for cleanup):** multiple test/placeholder venues and artists with garbage data (e.g. venue address/city literally reading "FREE TEXT SO I WILL PUT GARBAGE VALUE"; a test artist account "audi5001" currently featured as "Top Artist Right Now") are visible on public-facing pages today. An E2E test fixture event ("E2E Fixture: Waitlist/Wallet Flow") is also visible in the real public Events listing. Worth a cleanup pass before any screenshots/demos go outside the team, and a good concrete case for the QA seed-dataset idea filed to Feedback this session.

**Correction to this session's own framing of "3 open seat-map design decisions":** earlier session context (Claude's own working memory) carried these three items as still pending Hitesh's input: (1) zones/aisles as pure walkways vs. labeled, (2) publish-gating strictness for Numbered venues, (3) levels in wizard vs. advanced-panel-only. Cross-checking this document's own history before filing revealed that **(1) and (3) are already resolved and shipped** — see the Zone-model rewrite (PR #147/#148, aisles confirmed as pure walkways with no pricing identity) and Venue Levels (PR #149, lives in the main wizard, not advanced-panel-only) entries above. **Only (2), publish-gating strictness, remains genuinely open** — and today's live finding (Numbered venue published with 4 seats/no zones/no pricing, no warning) is fresh, real evidence it needs to be stricter than whatever check currently exists. Recommend Hitesh confirm this reading is correct so future session context reflects it accurately rather than re-treating (1) and (3) as open.

### 9.3 Schema exists, no UI yet (backend ready, waiting on frontend)

- **H3** — flag/suspend accounts. ✅ **Shipped (PR #119, 19 Jul).** `/dashboard/admin/users` + `GET /api/admin/users` + `PATCH /api/admin/users/[id]/suspend`. Blocks login immediately (both auth providers), invalidates a live session on its next check (same mechanism as the B5 password-reset check) rather than waiting out the JWT's life, and hides the account's future events/venues from public listings + detail pages. Deliberately does not cancel existing confirmed bookings or already-published events/venues - an audience member who already paid isn't part of the reason for the suspension. Can't suspend yourself or another Admin. Fully reversible, no separate "banned" state. Scope note: only Organiser/Venue Owner suspension has a public-facing effect; suspending an Artist/Audience account blocks login but doesn't touch Artist public profiles - not discussed as in-scope, flag if that's wanted too. **Live-verified same session** against real QA data (test Organiser `organ012`, event "Mic Fire 010" + "Mic Bon-fire 007"): suspended via direct DB write → confirmed both events dropped out of `GET /api/events` and the direct-link detail route returned 404 → unsuspended → confirmed both reappeared and the detail route returned 200 with `organiser.user.isSuspended: false`. Full round-trip, no lingering state. Login-block path (auth.ts) and live-session invalidation (SessionGuard) are code-reviewed but not click-through tested - no browser access in this environment; worth a real login attempt next session for full confidence, though the mechanism mirrors the already-proven B5 password-reset invalidation exactly.
- ~~Admin bookings list surfacing `Booking.deliveryError`~~ ✅ **Already shipped 15 Jul** (`b0b053c`, `1eaed64`) — full admin page at `/dashboard/admin/bookings` with Failed/Pending/Delivered/All tabs, error display, and retry button wired to the redeliver endpoint. This row was never updated when it landed; caught during the 19 Jul session-2 reconciliation when it was about to be picked up a second time. Lesson: §9.3 needs the same reconciliation discipline as the Feedback table, not just a one-time check at session start — a shipped item sitting in "not yet built" is exactly the kind of stale entry the standing process exists to catch, and it slipped through twice.
- **K4** — Admin revenue dashboard. ✅ **Shipped (PR #117, 19 Jul).** `/dashboard/admin/revenue` + `GET /api/admin/revenue-overview`. Platform's own actual revenue only (audience booking fee on CONFIRMED bookings) — per-venue Venue Owner view (`/dashboard/venue/[id]/sales`) and per-organiser views already existed and show *their* gross revenue, which is a different number. Range filter, timeline, top organisers/events by platform fee generated, current fee setting surfaced inline.
- ~~"Wall of Fame" — Artist of the Month / Event of the Month~~ ✅ **Shipped (PR #114, 19 Jul).** `/wall-of-fame` + `GET /api/wall-of-fame`, exactly per the decision recorded here: min 3 reviews, calendar month scope, aggregated in JS (artistId is two joins from Review, not groupBy-able directly). Longer-term idea, not yet built: the hero rotator (currently curated stock photos) could eventually pull from the winning artist's real event photos — ties the existing "For artists, featured here" hero caption to something real.
- **Top Venues / Top Organisers leaderboard.** ✅ **Shipped (PR #115, 19 Jul).** All-time ranking (not calendar-month scoped — that qualifier only ever applied to the two "of the Month" awards), same 3-review floor, top 5 each by avg rating desc then review count. Rendered as two new sections on the same `/wall-of-fame` page rather than a separate nav entry.

### 9.4 Not started at all
- **Address autocomplete + real venue lat/lng.** ✅ **Shipped (session 36, 26 Jul, PR #211, merge `7b7a6f3`).** Raised by Hitesh (26 Jul, session 36) asking what happens to the Address field, still plain free text after PR #210 only touched City. Found while investigating: **`Venue.lat`/`Venue.lng` already existed in the schema but nothing had ever populated them** - the only place they're read is the "Get Directions" link on the public venue page (`src/app/venues/[id]/page.tsx`), which had therefore never actually worked for any venue until now. Built as ONE merged Address-level lookup that derives City/State/Country/lat-lng from the same Place Details response, instead of running Address and City as two independent searches - keeps API call volume/cost the same as the City-only feature from PR #210, per the cost note below, not doubled. `includedPrimaryTypes` for the address search deliberately includes `establishment` so typing a venue's actual name can surface its real indexed address directly. Manually typing an address with no clean match still works (doesn't block registration) - City stays independently editable via its own existing autocomplete for a manual override. `POST`/`PATCH /api/venues` validate lat/lng server-side (finite, in-range) before storing, same discipline as every other numeric input in this app. **Not yet click-tested live** - needs a real address search on QA (both a street address and a venue/business name) confirming City/State/Country/lat-lng populate correctly and "Get Directions" now points somewhere real.

  **Cost note (26 Jul):** Claude flagged uncertainty here rather than guessing confidently - the `addressComponents` field (needed for the structured City/State/Country breakdown, already used by the existing City feature since PR #210) appears to fall into Google's pricier "Pro" billing tier rather than the cheapest "Essentials" tier, per several third-party sources, though those sources disagree with each other by a wide margin (roughly $5-$35 per 1,000 calls depending on source/fields) and none is Google's own current official pricing page. This isn't new cost introduced by Address specifically - the existing City feature already incurs whatever this tier costs, and building Address as a merged lookup (rather than a second search) keeps it that way rather than doubling it. Recommend checking the real Cloud Console billing/usage dashboard after a handful of live test registrations for the actual number, rather than trusting any published estimate - and keep the ~$10/month billing alert in place regardless.

- **Field-level help (i) icon for non-obvious fields, starting with venue creation/edit.** Raised by Hitesh (26 Jul, session 36) while discussing Acoustic Rating - some form fields use jargon or an assumed meaning that isn't obvious to a first-time Venue Owner. **Claude's agreement + audit:** genuinely only a handful of fields need this, not a form-wide sweep - checked venue creation directly, most fields (Name, Address, City, Facilities, Capacity) are self-explanatory. Real candidates found: **Acoustic Rating** (once redesigned per the entry above, still worth explaining what it means and who rates it), **"Flexible" rate type** (currently just a button label alongside Hourly/Daily with nothing explaining it means *negotiated per booking request*, not "flexible hours" or anything vaguer), **Google Maps Link** (has a small optional-hint already, but not why a real Maps link matters). **Settled (26 Jul):** an (i) icon attached to the field's **label**, not a persistent textbox/inline-copy under the field - opt-in on hover/tap, so it doesn't add permanent vertical clutter to every field for users who already understand it. Build as one reusable component (e.g. `FieldHelp`, icon + tooltip/popover) usable on any label anywhere in the app going forward, not a one-off patch scoped to venue creation alone. **Deliberately not built now** - backlogged per Hitesh's explicit call, same session.

- **Acoustic Rating: replace venue-owner self-report with real Artist/Organiser ratings.** Raised by Hitesh (26 Jul, session 36) reviewing the "Acoustic Rating (0-5)" field - currently a bare number the Venue Owner types about their own venue at registration/edit, no verification, no other input. **Hitesh's read, which Claude agrees with:** self-rating has no signal - a venue owner has no reason to ever enter anything below a 4, so the number can't mean anything once every venue converges toward the top. **Settled (26 Jul):**
  - Raters: **Artist and Organiser only** - both have direct, qualified experience of the room's actual acoustic properties (monitor mix, reverb, PA), unlike Audience, who mostly convey general show experience rather than acoustics specifically. Audience explicitly excluded from this rating.
  - **Conflict-of-interest exclusion, non-negotiable:** an Organiser's rating must not count when that Organiser is also the venue's owner. This needs a real code check (`Organiser.userId === Venue.ownerId` for that booking), not just a policy note - this app already has multi-role support (a person can hold Organiser + Venue Owner profiles simultaneously), so the conflict is a real, reachable case, not hypothetical.
  - **New venue default: "Not Rated Yet"** (not a blank dash, not a 0) shown until the first real rating exists - explicit, friendly empty state rather than looking broken.
  - **Venue Owner's create/edit forms lose the editable input entirely (26 Jul, Hitesh's follow-up + Claude's agreement).** Once ratings come from Artist/Organiser, an editable textbox on the owner's own form would be actively misleading - it would still look like the owner can set their own number, undermining the whole point of the fix. Replace with a **read-only label** wherever the owner sees it too (not just the public venue page) - e.g. "Acoustic Rating: 4.2/5 (8 ratings)" or "Not Rated Yet" - same display, just never an input the owner can type into.
  - Not yet scoped: whether this becomes a new field on the existing `Review` model (currently tied to Event/Performance, no venue-level rating exists yet) or a small dedicated rating model; how ratings aggregate (simple average vs. weighted); whether Artist and Organiser ratings display as one blended number or two separate ones.
  - **Deliberately not built now** - backlogged per Hitesh's explicit call, same session.

- **Artist Profile Picture: real file upload (not URL-paste).** Feedback `8560fb61` (originally "no photo/no styling on public profile", twice mis-resolved this session before the actual gap surfaced - see full history on that Feedback row). Confirmed via code: the Edit Profile field has existed since PR #179 (23 Jul) but is a bare URL-paste input only, with its own placeholder text admitting "direct file upload isn't available yet." **Hitesh's decision (26 Jul, session 36): build it properly via real file upload (Vercel Blob or equivalent), not the cheap base64-on-row workaround - but explicitly not now, backlog it.** Scope when picked up: an actual `<input type="file">` + upload endpoint, storing a URL back on `User.avatar` (field already exists, no schema change needed) instead of the pasted-URL flow; decide at that time whether to keep the URL-paste field as a fallback for users who already have a hosted photo, or replace it outright.

- **In-app messaging between confirmed parties (Hitesh request, 26 Jul, session 36).** Four relationship pairs, all needing the same underlying capability: (1) Artist <-> Organiser after a spot is booked, until the event is over; (2) Organiser <-> Venue Owner after the venue is booked, until the event is over; (3) Audience <-> Organiser after a ticket is booked, until the event is over; (4) Organiser -> all Artists in a lineup (broadcast).

  **Architecture recommendation (Claude, not yet built):** one unified messaging system reused across all four pairs - a generic `Conversation` + `Message` model pair, not four separate builds. Each `Conversation` is tied to the confirming record (`Performance`, `VenueBooking`, `Booking`) so context (which event, which booking) travels with the thread automatically rather than needing to be re-selected.

  **Lifecycle (proposed, not yet confirmed):** thread opens the moment the underlying commitment is confirmed (spot approved / venue booking confirmed / ticket booking confirmed); becomes **read-only** ~48 hours after the event's actual end time - archived, not deleted, matching this platform's existing "never delete evidence" pattern elsewhere. Exact grace period still Claude's suggestion, not Hitesh-confirmed.

  **✅ Settled (26 Jul, same session):**
  - **Broadcast (case 4):** **fan-out**, not a group chat - the organiser writes one message, it lands as an individual message in each artist's own private 1:1 thread with the organiser; replies stay private, not visible to other artists in the lineup.
  - **Admin visibility:** threads stay **private between the parties** - Admin cannot view any thread for dispute resolution. If a real dispute needs resolving later, it'll go through some other explicit mechanism (a report/flag action, not ambient read access), not scoped yet.

  **Still open, explicitly deferred (Hitesh: "not sure yet, revisit later"):**
  - **Audience <-> Organiser thread granularity** - one thread per individual `Booking` (private, 1:1, but a popular event could generate dozens of separate threads with the same organiser) vs. one shared thread per event that all attendees see into (more like a group FAQ - cheaper for an organiser to answer once, but a very different privacy/UX shape, and inconsistent with the fan-out-not-group-chat call just made for case 4). Claude's lean, offered for whenever this gets revisited: per-booking/private, for consistency with the broadcast decision above and because a shared thread has the same noise/privacy tradeoffs just decided against - but genuinely open, not pre-decided.

  **v1 scope recommendation (unchanged):** text-only (no attachments), polling-based refresh (~15-20s while a thread is open, matching the existing pattern already used for Venue Booking Requests/Revenue), push notification on new message via the existing `sendPushToUser` infra. Deliberately deferred: read receipts, typing indicators, real-time websockets, media attachments, moderation/report-abuse action (v1 has no admin visibility per the decision above, so a report mechanism has nowhere to route to yet - worth flagging as a gap once real users are messaging each other, not before).

  **Not yet built** - the Artist<->Organiser and Organiser<->VenueOwner pairs are now fully decided (both plain 1:1, no open questions) and could be built next; Audience<->Organiser should wait for the granularity call above rather than guessing and rebuilding.
  - Moderation/report-abuse handling - even a v1 needs at least a bare-minimum "report" action given this is now a vector for harassment between real people, unlike the platform's existing structured forms.

- **Artist waitlist for full lineups (Phase 1 of 2)** — Hitesh's own admin note (22 Jul, explicitly "don't treat as from user"): "Artist applying for Spot at event, but all spot are full... Artist can apply in waiting queue. If any artist cancel... The Waiting artist get a chance... As FCFS basis." **Phase 1 shipped (session 20, 23 Jul):** `ApplicationStatus` gets a new `WAITLISTED` value (additive, existing PENDING/APPROVED/REJECTED untouched). `POST /api/applications` no longer hard-rejects with "This event's lineup is already full" when `Event.maxPerformers` is reached - it creates a `WAITLISTED` application instead, FCFS by `createdAt`. Artist gets pushed their queue position; Organiser is NOT separately notified for a waitlisted application (nothing to act on immediately) but sees it in their normal Applications list with a distinct badge, and can manually Approve/Reject a waitlisted applicant the same way as any pending one - the existing capacity re-check at approval time (`filledSlots >= maxPerformers`) already handles "is there actually room now" correctly, no change needed there. Artist's own "Browse Events" page shows "Join Waitlist" instead of a disabled/blocked state when full, and "⏳ Waitlisted" for an existing waitlisted application.
  - ~~Phase 2, deliberately not built - a real gap found while scoping this~~ ✅ **Shipped (session 21, 23 Jul).** Consequence rules per Hitesh: applies only to Buy-in slots (Paid/Free have nothing to refund); a cancelling artist "will not be punished" - the recorded Buy-in amount defaults to REFUNDED the moment they cancel (bookkeeping only - no real Buy-in payment collection exists yet, so nothing actually moves, this just clears what was owed); cancelling inside 24 hours of the event is blocked outright, no exception; an Organiser can *afterward* choose to keep a refunded amount as wallet credit instead - explicitly the Organiser's own call over their own bucket, never the artist's choice and never the default. New `Performance.cancelledAt`/`buyInRefundStatus` (enum `REFUNDED`/`WALLET_CREDITED`) and `Organiser.walletBalance` (simple running ledger - no spend/reuse mechanism built yet since none was specified, revisit when a concrete "spend it on X" need shows up). `POST /api/performances/[id]/cancel` computes the 24h cutoff from the event's real date+startTime (same combine-into-an-instant pattern already used for backdated-event validation), and - the actual point of Phase 1 - auto-promotes the oldest WAITLISTED application for that event on a successful cancellation, reusing the event's own declared default compensation terms for the promoted artist (same fallback already built for manual approval in the payment-transparency PR). `PATCH /api/performances/[id]/refund-status` is the one-directional Organiser-only override (REFUNDED → WALLET_CREDITED only, never the reverse via this endpoint). `GET /api/events/[id]/lineup` (all three query sites) now excludes cancelled performances from drag-and-drop ordering - a cancelled slot isn't part of the active lineup to reorder. Artist dashboard shows a Cancel button on upcoming performances, disabled with an explanation inside the 24h window (client-side mirror of the same rule enforced server-side). Organiser event page gets a new "Cancelled Performances" section with refund/wallet status and the override button; main Organiser dashboard shows a small wallet balance line, only when non-zero. **Wallet reuse mechanism decided and shipped same day (session 24):** applies toward future venue booking platform fees - see the dedicated entry below.
- **Testing pass on waitlist/cancellation/wallet-credit logic (session 27, 23 Jul)** — code + live-data review (no click-through browser access this session), since none of the above had been exercised by a real user yet. Found and fixed two real issues:
  - **Waitlist over-count bug, latent, not yet triggered.** `POST /api/performances/[id]/cancel` correctly excludes `cancelledAt` rows when counting the lineup, but 4 other call sites (`POST /api/applications`'s waitlist-check and auto-approve-slot-number, `PATCH /api/applications/[id]`'s approval capacity-recheck and slot-number) didn't - meaning after the first real artist cancellation, the event would read as permanently full even with a genuinely open slot. Confirmed via direct Supabase query that zero cancellations exist in QA yet, so this hadn't manifested - would have as soon as one did. Fixed all 4 to match the one correct instance (PR #183).
  - **Wallet race conditions.** `refund-status` (organiser wallet-credit override) and `apply-wallet` (spend credit on venue fees) both read the wallet balance then wrote based on that stale read - a concurrent duplicate request (double-click, two tabs) could double-credit or overdraw the ledger. Hardened both with a guarded `updateMany` (only succeeds if the balance still matches what was read) so a losing concurrent request fails cleanly with 409/400 instead of corrupting the balance (PR #183).
  - **Still needed (update, 23 Jul, later same day):** the real click-through no longer needs to be manual - see "E2E automation: Playwright + GitHub Actions" below, which wires up the existing `e2e/*.spec.ts` suite to run unattended (push/nightly/manual), and adds a dedicated spec for exactly this flow. Multi-role mid-session switching is still genuinely un-click-tested.
- **E2E automation: Playwright + GitHub Actions (session 27, 23 Jul, later)** — the repo already had `playwright.config.ts` + `e2e/smoke.spec.ts` + `e2e/numbered-seat-booking.spec.ts` written (against the live QA deployment, no local server needed) but nothing ever ran them automatically - this closes that gap.
  - **PR #186** — `.github/workflows/e2e.yml`. Triggers: push to `qa`, nightly (02:00 UTC), manual dispatch. `concurrency: group: e2e-qa, cancel-in-progress: false` queues runs instead of racing them against the shared QA DB (same reasoning as `playwright.config.ts`'s `workers: 1`). Runs on GitHub's own infra, so it's unaffected by the Vercel Hobby-plan deploy rate limit blocking #185.
  - **PR #187** — the actual click-through for the waitlist/wallet-credit chain from the entry above: Artist A applies (Buy-in) → Organiser approves → Artist B applies (now waitlisted) → Artist A cancels (≥24h out) → B auto-promotes → Organiser converts A's refund to wallet credit → Organiser applies that credit to a venue booking's platform fee. New `e2e/helpers/roles.ts` (`registerTestArtist` - fresh Audience→Artist upgrade via the real `/profile` UI; `loginFixtureOrganiser`).
    - **Fixture, seeded once directly via SQL (not through any UI/browser - see below for why):** one approved Organiser (`e2e.fixture.organiser@example.com` / org id `e2efixtureorg0001org`), one `BUY_IN` + `maxPerformers:1` Event (`e2efixtureevt00001`, "E2E Fixture: Waitlist/Wallet Flow", 5 days out at seed time, attached to the existing approved Hourly venue "The Vintage Club"), one VenueBooking (`e2efixturevb0001`) with a real ₹199 platform fee. **Do not delete these rows** - reused by every future run of this spec. No existing QA event had `defaultCompensationType: BUY_IN` at the time (confirmed via direct query), so unlike `numbered-seat-booking.spec.ts` (which reuses "Jaipur Mic Gala 100"), there was no existing fixture this could point at instead.
    - **Why SQL instead of a real click-through UI build:** genuinely considered and started via Claude in Chrome first, per Hitesh's own preference for exercising real code paths - but building the fixture requires creating/logging into an Organiser account, which runs into a hard rule Claude operates under (never enters passwords into a live browser, even for a throwaway account it creates itself, even when explicitly asked). Writing a Playwright spec that does this programmatically is a different, already-established, and permitted pattern (identical to how `registerTestAudience` already worked) - so the fixture's *state* was seeded via direct SQL (matching real schema constraints/enums exactly), while the actual flow under test (cancel, promote, wallet-convert, apply-credit) still runs for real through the browser via Playwright, not faked.
    - **`.github/workflows/e2e-waitlist-wallet.yml` — manual (`workflow_dispatch`) only, deliberately not on the push/nightly cadence yet.** This spec is new and unproven; promote it into `e2e.yml`'s regular triggers once a first real run has been watched end to end.
    - **Known non-idempotency, worth remembering before re-running:** `VenueBooking.platformFeeAmount` only ever decreases (no code path replenishes it) - so the fee-application assertion is only meaningful on the *first* successful run; every run after finds the fee already at ₹0. Cancel/waitlist-promote/wallet-credit-conversion all still genuinely re-verify every run regardless - only that one assertion needs a fixture reset (re-run the `platformFeeAmount: 199` SQL) between full passes. A cleanup step (Artist B also cancels at the end) does keep the lineup slot itself reusable indefinitely, independent of this fee caveat.
  - **Multi-role switching** and the **+1 mandatory audience support** flow were also reviewed - no bugs found (switch-role's authorization/session-refresh handling and the plus-one comp-booking capping all read correctly against the code).

- **Wallet credit reuse - applies toward venue booking platform fees** — Hitesh's answer (23 Jul) to "how should this be reused": venue booking fees. New `PATCH /api/venue-bookings/[id]/apply-wallet` - Organiser-only, applies as much of their `walletBalance` as possible toward a specific `VenueBooking.platformFeeAmount`, capped at whichever is smaller (never over-applies past either the fee owed or the balance available). No new schema - both `platformFeeAmount` and `walletBalance` already existed. UI on the Organiser event page shows the remaining platform fee and an "Apply wallet credit" button whenever both the fee and the balance are non-zero. Scope note: `platformFeeAmount` is only ever set when a venue booking is created alongside event creation (Hourly/Daily rate path in `POST /api/events`) - the separate direct-booking route (`POST /api/venue-bookings`, used when adding/changing a venue after creation) doesn't currently set it at all, a pre-existing inconsistency not touched in this pass.

- **Multi-role support (Organiser + Venue Owner, or any combination, same person)** — Hitesh raised via Feedback (23 Jul, "organiser and venue owner is same person or company") then clarified the concrete scenario (session 19, 23 Jul): "It is possible that venue owner become organiser sometime." Confirmed real via code: `User.role` is a single enum, and it's load-bearing at actual authorization checkpoints (e.g. `POST /api/events` checks `user.role !== 'ORGANISER'` as its first gate, before even looking at the Organiser profile) - not just cosmetic routing. A true simultaneous-multi-role model would mean touching every one of those hard role checks across the money/publish-critical paths (events, venues, bookings, applications), which is a large, risky refactor to do casually. **Shipped instead (session 19, 23 Jul) - a "switch active role" model:** a person can hold multiple profile rows (Artist/Organiser/VenueOwner) simultaneously - the data model already allowed this structurally, it just wasn't reachable - but only one role is *active* at a time via `user.role`, exactly as before. Every existing role-gated authorization check elsewhere in the app needed **zero changes** - they keep checking the single active role exactly as they always did; this is additive on top, not a rewrite. Concretely:
  - `POST /api/organisers/apply`, `/api/venue-owners/apply`, `/api/artists/apply` - previously hard-blocked with "You're already registered as X, and can't apply for a second role right now" for anyone not on `AUDIENCE`. Now only `ADMIN` is excluded from self-serve applications; the "already applied" check is profile-existence-based (not role-based), so a Venue Owner can apply to also become an Organiser. `user.role` is only auto-flipped immediately for genuine first-timers (starting role `AUDIENCE`) - anyone with an existing active role keeps it untouched on application, so applying for a second role can never silently kick someone out of their current dashboard mid-session.
  - `GET /api/organisers/status`, `/api/venue-owners/status`, `/api/artists/status` - previously gated on `user.role === 'X'` first, so a Venue Owner with an approved-but-inactive Organiser profile would have seen `isOrganiser: false`, hiding their own status from themselves. Now check profile existence directly, and added an `isActive` field so the Profile page can tell "this is your current active role" apart from "you have this role too, just not active right now."
  - New `POST /api/users/me/switch-role` - the only thing that changes an already-active role, and only to a role the user holds a genuinely approved profile for (Artist needs no approval; Organiser/Venue Owner do). This is the sole "write" side of multi-role - everything else above is read/apply-only.
  - `/profile` page - each of the three role cards (Artist/Organiser/Venue) now has a fourth state alongside the existing three (no profile / pending approval / active-visit-dashboard): approved-but-not-active shows a "Switch to your X dashboard" button instead of a dead-end "visit" link that would 403 against the now-inactive role.
  
  **Deliberate trade-off, stated plainly:** this does not allow acting as two roles in the exact same moment (can't be mid-way through publishing an event and confirming your own venue booking in one click) - switching is an explicit action first. Chosen over a true simultaneous-role refactor because the risk of touching every authorization checkpoint in the app outweighs the rarity of needing genuinely simultaneous access, and the trade-off can be revisited later if it turns out to actually matter in practice.

- **Artist payment-terms transparency** — Hitesh request (18 Jul, two separate feedback entries making the same point, surfaced again from a fresh Feedback-table sweep 23 Jul session 18). Real, confirmed gap: `Performance.compensationType`/`feeAmount`/`buyInAmount` were only ever set by the Organiser *after* approving an application - an Artist applied completely blind to whether a spot was Free/Paid/Buy-in, and nothing stopped an Organiser from quietly offering different terms to different artists for the same event. **Shipped (session 18, 23 Jul):** `Event.defaultCompensationType`/`defaultFeeAmount`/`defaultBuyInAmount` - Organiser declares default payment terms at event creation (Lineup & Approvals area, alongside +1), shown publicly to Artists on the "Browse Events" apply page before they apply (a clearly separate, prominently-colored badge from the audience ticket price, which a previous version of that page displayed in a way that could be mistaken for artist pay). Organiser can still override per-artist at approval time for real exceptions (e.g. negotiating a different fee with a headliner) - the event-level field is the published default, not a hard lock. **Also fixed a related silent bug while building this:** `POST /api/applications/[id]` (the approve action) previously defaulted a Performance to `FREE` whenever the Organiser's approval request didn't include an explicit override - meaning an Organiser who declared "Paid ₹500" at event creation but forgot to re-enter it at approval time would have silently downgraded the artist to FREE with no error. Now falls back to the event's own declared default instead of a hardcoded FREE. Edit page also updated so this can be changed after creation, not just set once.

- **G3 — Tip.** Razorpay is now live in QA, so the blocker is gone; hasn't been scheduled.
- **B4 — Google/Apple sign-in.** Needs OAuth apps registered in Google Cloud Console and Apple Developer first — external prerequisite.
- ~~A5 — Search~~ ✅ **Shipped (session 23, 23 Jul).** Combined search (Events/Artists/Venues together, not separate per-section) with an inline instant-results dropdown in the header - both choices confirmed with Hitesh before building. New `GET /api/search?q=` - simple `contains`/insensitive matching (Postgres ILIKE via Prisma), no full-text search infra, capped at 5 results per group. New `SearchBox` component, debounced 250ms, click-outside-to-close, wired into both the desktop and mobile nav in `SiteNav.tsx` (hidden on detail pages using the `backHref` back-link variant, to keep those focused). Closes the "Search Engine Suggestion" item below too.
- **D2, D4** (Artist profile builder beyond what exists, Hype Score/growth report display).
- **K3** (checkout tip widget), **K5–K8** (Pro subscriptions, promoted placements) — all deliberately Phase B/C, not MVP.
- **Artist financial summary (income + spend)** — Hitesh request (19 Jul), supersedes/broadens the earlier "who tipped me" note. **Compensation/spend half shipped (session 16, 23 Jul):** "Recorded Earnings" section on `/dashboard/artist` - "Recorded Compensation" (sum of PAID `feeAmount` across the artist's Performances) shown separately from "Recorded Spend" (sum of BUY_IN `buyInAmount`) with a net figure, plus an explicit note that this is off-platform/not processed by the platform. Zero backend changes needed - `Performance.compensationType`/`feeAmount`/`buyInAmount` already flowed through `GET /api/artists/me` (Prisma includes all scalars by default via `include`), just weren't typed or displayed; this was purely a frontend addition. Section only renders once there's something to show, so a brand-new artist doesn't see an empty ₹0/₹0/₹0 block. **Tips received still hard-blocked on G3 (tipping)** - deliberately not combined into the same total, per the "do not blend recorded compensation with real tips" framing below.
- **Audience financial summary (spend + tips given + freebies)** — Hitesh request (19 Jul). **Spend/freebies half shipped (session 16, 23 Jul):** new `/dashboard/audience` ("My Activity") page - Total Spend (sum of `Booking.totalAmount` across CONFIRMED bookings), Events Attended, and Free Events Attended (count, not a fabricated rupee value, since free events have no notional price to subtract from). Zero backend changes - `event.isFree`/`totalAmount` already flowed through `GET /api/bookings/my` via `include`, purely a new frontend page. Also added the missing `AUDIENCE` case to `SiteNav`'s `getDashboardLink()` - Audience users previously saw no "Dashboard" nav link at all (fell through to the `null` default alongside genuinely-role-less users), now correctly routes here. "Whom I tipped" still **hard-blocked on G3** same as the Artist side - noted inline on the page as coming once tipping ships.
- ~~Profile completion nudge/percentage~~ ✅ **Shipped (session 22, 23 Jul).** From live feedback (18 Jul): "Notify user profile completion percentage... recommend for early completion for better result." Exactly the four fields the feedback named - bio, genre, styleTag, socialLinks (at least one non-empty link) - each weighted equally (25%). Zero backend changes needed - all four already flowed through `GET /api/artists/me`; purely a frontend addition. Progress bar + percentage shown on the Artist dashboard, only while incomplete (disappears entirely at 100%, no need to keep nagging once done), linking straight to Edit Profile. Framed the "why" honestly rather than just gamifying for its own sake: Organisers reviewing applications see bio/genre/style directly, so a complete profile has a real, material effect on approval odds.
- ~~Netflix-style intro/splash screen~~ ✅ **Shipped (PR #122, 19 Jul), refined several times same day (PR #124, #126, #129, #130) - stable as of #130.** Current implementation (a real architectural rewrite, not just a style tweak - see below): bottom-up bar reveal from `src/app/icon.svg` sized ~2x a typical Android PWA splash glyph (`clamp(240px, 68vw, 440px)`), shrinks+fades into an "AforAudience" wordmark that types itself in letter-by-letter with a blinking cursor, then a tagline fades in below ("Where Art Finds Its Crowd", reused from the homepage hero verbatim). Plays once per browser session, skips entirely on prefers-reduced-motion. Total runtime ~3.4s. **Refinement history:** #124 led with the icon-bars instead of cutting straight to the wordmark (matching the OS's own PWA splash instead of feeling like two disconnected screens). #126 tried a letter-cascade wordmark and a bigger icon - Hitesh preferred the original scale-in block after seeing both live, so text was reverted and only the icon size stuck. Same session, Hitesh then asked for a typewriter-style reveal instead (distinct from both scale-in and cascade) plus the tagline - both shipped in #129. **#129 also had a real bug, not a style issue:** the overlay was JSX with explicit style props, mutated by a synchronous inline script before hydration - React hydration compared the live DOM against its own render output and reverted the script's changes within milliseconds, so the overlay flashed almost invisibly on desktop and didn't appear at all on mobile. **Fixed in #130:** the entire overlay (style, svg, wordmark, tagline, and the script that animates them) is now one single `dangerouslySetInnerHTML` block instead of individual JSX elements - React treats that as opaque and never diffs into it after the initial render, so hydration can't revert the script's DOM mutations. Verified against live QA data this time (not just a clean Vercel build) before calling it done. Also worth recording from this stretch: a burst of 6 back-to-back deployments in ~30 minutes while Hitesh had the site/PWA open on his own devices caused a transient "stuck loading" / blank-page symptom, unrelated to the hydration bug - resolved with a hard refresh / PWA reopen. Lesson: pace deploys with a beat in between when the person has the app open and is actively testing live.
- **Hero banner GIF/video clip (~5s)** — from live feedback (18 Jul). Would replace or supplement the current curated-photo rotator (`HeroRotator.tsx`); larger asset-pipeline question (video hosting/compression) than the existing static-photo approach, not just a swap.
- ~~Chatbot icon redesign~~ ✅ **Already shipped, status corrected (found session 16, 23 Jul).** From live feedback (18 Jul), suggested something more on-brand (e.g. a mic-in-hand) than the generic icon. Turns out this was already built - `MicIcon` in `src/components/SupportWidget.tsx`, a real SVG (spotlight glow behind a stage mic, `currentColor` so it inherits button/text color) with its own comment explicitly noting it "replaces a generic speech-bubble emoji." Backlog entry was just stale, not an actual gap - no code changed, this is a status-only correction.
- ~~Follow Artist/Venue/Organiser for update notifications~~ ✅ **Shipped for Artist + Venue (session 14, 23 Jul); Organiser also shipped (session 17, 23 Jul).** Hitesh request (22 Jul, session 13); refined further same session (23 Jul) after Hitesh asked for a YouTube-style separate notification toggle rather than notifications always firing on follow. **Follow table migrated live on QA** from Artist-only (`artistId` direct FK) to polymorphic (`targetType` enum `ARTIST|VENUE|ORGANISER` + `targetId`, no FK - one table instead of three near-identical ones), plus a `notifyEnabled` boolean (defaults **true** - unlike YouTube, the entire stated reason to follow here is hearing about new events, so defaulting off would bury the feature behind a step almost nobody takes). UI: heart toggle + a bell icon (visible only once following, matching YouTube's own pattern) on the Artist, Venue, and now Organiser detail pages. Push trigger: genuine `DRAFT`/`PENDING_APPROVAL` → `APPROVED` transition only (both at creation and on later publish via edit) - re-saving an already-live event does not re-notify. Shares delivery plumbing (`sendPushToUser`) with the pre-existing "new follower" push, which is untouched and coexists as a separate trigger. Also fixed in the same pass: `Artist._count.followers` (relied on the now-removed direct relation) recomputed as a manual `Follow` count on the Artist profile server page. **Organiser piece required a real new public profile page** (`/organisers/[id]`) - see the entry just below for what that unlocked.
- **New public Organiser profile page** (`/organisers/[id]`, session 17, 23 Jul) - previously didn't exist anywhere (`src/app/(public)` had no `organisers` route at all), which was the actual blocker on Organiser-follow above, not a design gap. Mirrors the Venue detail page pattern: server component + a small isolated `OrganiserFollowButton` client island. Shows org name, bio, and their APPROVED events split into Upcoming/Past. Gated the same way Venue is - `isApproved: false` organisers 404 rather than showing an unapproved profile publicly. Also wired an "Organised by [Name]" link into the public event page's Overview tab (previously the organiser wasn't shown/linked anywhere on the event page at all), so the new profile is actually reachable from somewhere a real visitor would be, not just a direct URL. No listing/browse-all-organisers page yet - deliberately out of scope for this pass, would be its own feature.
- **"+1" mandatory audience support** — Hitesh request (23 Jul, session 15). Real problem: some Organisers charge artists a spot fee but still can't fill the room, so they require each artist to guarantee at least one audience member - previously an off-platform WhatsApp/friend-network scramble, invisible to the platform. **Shipped end-to-end (session 15):** `Event.plusOnesRequired` (Int, organiser-set at creation, next to the spot-fee field) applies independently to every artist in the lineup - each artist fills their own N, tracked via a new `PlusOne` model (userId, performanceId, bookingId, unique on user+performance). Confirming is instant, no artist approval step (the whole point is removing the scramble/delay, not recreating it). Confirming issues a real ₹0 comp booking (same QR check-in flow as any ticket, decrements `Event.availableSeats`) if the user doesn't already have one for that event, or tags their existing CONFIRMED booking if they do - either way something real is scannable at the door, not just a pledge. Capped at `plusOnesRequired` per artist (best-effort under a concurrent-confirm race, not a strict transaction - a rare extra confirmation is harmless). Artist gets a push via existing `sendPushToUser` on each confirmation and again on hitting the full count. UI lives in the event detail page's Lineup tab, next to each performer. **Deliberately deferred, not built:** artist-side reject/cancel of a specific +1 (v1 is read-only count + who's confirmed, no management surface - revisit only if this turns out to be a real problem). ~~The event edit page doesn't expose plusOnesRequired yet~~ ✅ **Closed (session 15, same day):** edit page now loads/saves it too, same clamp-on-change pattern as the create page - `PATCH /api/events/[id]` already accepted the field, this was purely the missing UI.
- ~~Reply-to-comment~~ ✅ **Shipped (session 16, 23 Jul).** Hitesh request (22 Jul, session 13). New `ReviewReply` model (reviewId unique - one reply per review, no threaded nesting) rather than touching `Review` itself - deliberately avoids putting a reply in the same table as `rating`, which would have meant either faking a rating for every reply or auditing every place ratings get averaged to exclude them. Owner resolution mirrors the existing review-notification logic already in `POST /api/reviews` exactly: a performance-specific review's owner is that Artist, a general event review's owner is the event's Organiser (no direct Review->Venue link exists in the schema, so Venue Owners can't reply yet - same pre-existing limitation). Reply submission lives in the Artist dashboard (natural "manage my own content" surface); the public event page shows replies read-only under each review. Notifies the original commenter via the existing `sendPushToUser` on reply. Purely additive schema change (new table only, no relation removals) - unlike the Follow migration, nothing else needed fixing as a result.
- ~~Comment box in the venue negotiation flow~~ ✅ **Shipped (PR #134, 19 Jul).** `VenueBookingOffer.comment` (nullable, additive). Optional free-text alongside the counter-offer amount, trimmed/capped to 300 chars, shown in italics in the offer history, included in the push notification when present. Ignored on accept/decline - the note travels with a specific offer, not the request as a whole.
- **Smaller cosmetic/UX items from earlier feedback (15 Jul) — status corrected (25 Jul, backlog reconciliation).** This doc previously said all three were "status REVIEWED, none built yet" — that was stale; `Feedback` actually had all three as `RESOLVED`, and code on `qa` was checked directly rather than trusting either source blind: (1) **mobile List/Grid toggle** — genuinely fixed: `events/page.tsx`'s `.events-filters-view-toggle` is `display:none` under the existing 780px breakpoint, exactly the ask. `RESOLVED` is accurate, left as-is. (2) **Rasa emotion prominence** — only partially addressed: `MoodThemeSection.tsx` shows each Rasa's one-word English gloss (e.g. "Shringara" / "Love") next to the Sanskrit name, no expanded description beyond that single word. (3) **Footer heading font size** — unchanged as far as can be told from a single snapshot (still 14px uppercase), no git history check done to confirm whether it was ever adjusted. **Hitesh's ruling (25 Jul): both (2) and (3) confirmed unverified/not actually satisfied, `Feedback` status reverted `RESOLVED` → `REVIEWED` for both — but explicitly low priority. Defer picking these up until all current high- and medium-priority work is done; do not schedule ahead of that queue.**
- ~~"Search Engine Suggestion" (15 Jul feedback)~~ ✅ **Closed by A5 above (session 23, 23 Jul)** - ties directly to Search, now shipped.
- ~~"Add blue tick feature" (18 Jul feedback)~~ ✅ **Shipped (session 23, 23 Jul).** Automatic, not Admin-toggled (Hitesh's choice) - awarded once profile is complete (same 4-field check as the completion nudge above: bio, genre, styleTag, socialLinks) **and** the artist has 3+ performances, reusing the existing "3-review floor" threshold already established for Wall of Fame rather than inventing a new number. Computed on the fly server-side (not stored - always accurate, no sync risk), shown as a small blue checkmark next to the artist's name on their public profile page. Scope note: only wired into the Artist public profile page for this pass - the same check could extend to event lineup listings or elsewhere an artist name appears, left as a natural follow-on rather than wiring every location blind in one pass.
- **Artist profile clickable from an Event's lineup section (15 Jul feedback, category BUG, ambiguous)** — the feedback text just states the behavior ("Users can view an artist's profile by clicking the artist listed under the Event → Line-up section") without saying whether that's wanted or unwanted. Needs re-triage with whoever filed it before treating as a bug or a confirmation.
- **"You can have your own event sometimes" (14 Jul feedback, unclear intent)** — vague as filed; not actionable without clarification from the source.
- **Reconciliation sweep, 25 Jul (organiser-role testing session) - older Feedback entries (18-23 Jul) that were never reflected in this doc at all, found while filing everything currently pending:**
  - **Seat number not visible on generated ticket** (23 Jul, `/tickets/`) - for bookings against Numbered-venue events, the ticket should show assigned seat/section/row. Not yet confirmed whether the ticket template (pdf-lib) omits this entirely or only for certain venue types - needs a quick code check before scoping a fix.
  - **"Tickets are taken, but still available ticket are out of out.. It is not reduced" (23 Jul, ambiguous wording)** - reads like a possible `availableSeats` not-decrementing bug, but the phrasing could equally mean the opposite (seats shown unavailable when they should be available). Too consequential (ticket inventory integrity) to guess at from wording alone - needs re-triage with the original reporter or a live repro before treating as confirmed either way.
  - **Self-serve ticket cancellation - unclear if it exists (23 Jul, "How to cancel ticket")** - unclear whether audience ticket holders have any self-serve cancel flow today, or whether cancellation is organiser/admin-only. Ties to the already-settled "full refund" policy (22 Jul, §9.5) which implies cancellation should be possible somewhere - worth confirming against the codebase.
  - **Username suggestion surfaces real full name - privacy concern (23 Jul, `/profile/`: "My name is coming as suggestion...revealing my identity")** - distinct from the separate "auto-suggestion not working" bug (15 Jul, tracked in §9.2 above). Even where the suggestion mechanism works correctly, suggesting the user's real name as a public-facing username is itself a privacy exposure - e.g. suggest based on role/interest instead of full name, or filter real-name-like patterns out of suggestions.
  - **Mobile layout + chatbot icon overlap on Venue dashboard (23 Jul, `/dashboard/venue/`: "Not aligned as per mobile, user see scroll... Chatbot hide")** - likely a real mobile responsiveness gap, possibly the chatbot icon overlapping/blocking content on small screens. Wording alone isn't enough to act on - needs a live repro to scope precisely.
  - **Current active role not shown in header nav (23 Jul, direct question to Claude-as-collaborator)** - real usability gap given the multi-role/switch-role feature already shipped (session 19, §9.2 area): a user active as e.g. Venue Owner but also holding an Organiser profile has no header-level indicator of which role is currently active, which the multi-role feature itself introduced as a point of confusion. **Recommendation: yes, worth adding** - a small role badge/label next to "Hi, [name]" in the header, reusing `user.role` already in session - low-cost, closes a real gap.
  - **Venue publish without phone verification? (23 Jul question)** - likely already answered by the existing gate: venue publish is one of the four `requireVerifiedPhone()` checkpoints (§9.2 "verification gating" fix). Worth a quick live re-confirm this is still true rather than assuming - the question's phrasing implies the reporter may have gotten through without it, which would be a regression if so.
  - **Which activities trigger notifications? (18 Jul question)** - likely already answered by the full push-notification matrix shipped in EPIC J; worth pointing whoever asked to that matrix rather than treating as open.
  - **"Bring some unique small feature that doesn't exist in the universe" (18 Jul)** - vague as filed, same category as the "own event sometimes" entry above; not actionable without follow-up from the source.
- ~~Live seat availability status (Filling Fast / Spots Available / Sold Out)~~ ✅ **Shipped (PR #136, 19 Jul).** New `src/lib/availability.ts` - ratio-based status (≤15% remaining = Filling Fast, 0 = Sold Out) replacing the old fixed "<10 seats" threshold, which had no sold-out state and didn't scale for large venues. Pure display layer over existing `Event.totalSeats`/`availableSeats` - no schema changes. Listing cards show the badge only for Filling Fast/Sold Out (available stays unbadged, same low-noise approach as before); event detail page always shows the status pill.
- **Seat-map builder for Venue Owners (BookMyShow-style numbered seating)** — Hitesh request (19 Jul), design confirmed 20 Jul (this session). **Ground truth check before designing:** `Venue.seatMap` is an unused `Json?` column nobody reads structurally — real capacity today lives entirely on `TicketTier` (sectionName/price/totalSeats per event) and `Booking.seats` is a `{tierId: qty}` map; the "live lock, no double-booking" in C1 is an atomic count check against active PENDING+CONFIRMED bookings, not per-seat rows. So this is genuinely net-new, not an extension of hidden plumbing. **Confirmed shape (additive, GA path untouched):** `Venue.seatingMode` enum (`GENERAL_ADMISSION` default | `NUMBERED`), opt-in per venue. New `Seat` model (`venueId, tierLabel, row, number, x, y`) — real x/y canvas coordinates confirmed with Hitesh (drag-to-place, true venue shape), not a rows×columns grid. New `BookingSeat` join model (`bookingId + seatId`) — a Numbered-mode booking populates this instead of the `Booking.seats` qty map; GA-mode bookings are completely unaffected, zero risk to the existing money path. Seat hold reuses the existing 15-min PENDING-booking TTL as-is — occupancy check becomes a `BookingSeat` lookup instead of a tier-count subquery for Numbered events, no new expiry job needed. Two new surfaces: a Venue Owner builder (canvas UI, place/drag seats, assign price-tier per seat or per drawn block) and an audience seat-picker (renders the saved layout, greys out taken/held seats). Live-lock UI across concurrent shoppers should use Supabase Realtime — already named in §5 Architecture as intended-but-unbuilt, this is its first real use case. Still flagged as a multi-session build: models + migration, builder UI, picker UI, and booking-flow branching (GA vs Numbered) are each substantial and should ship as separate PRs, verified independently. **Progress (20 Jul, same session):** ✅ Models + migration shipped (PR #140) — `Venue.seatingMode`, `Seat`, `BookingSeat`, applied to QA via Supabase MCP. ✅ Venue Owner builder shipped (PR #141) — `/dashboard/venue/[id]/seat-map`, real x/y canvas (click to place, drag to reposition, per-tier colour coding), `GET/PUT /api/venues/[id]/seats` (full-layout replace, owner/admin-gated). ✅ Audience seat-picker + booking-flow branching shipped (PR #142) — `GET /api/events/[id]/seats` (per-event occupancy, price resolved by matching `Seat.tierLabel` to `TicketTier.sectionName`), `SeatPicker` component wired into the event detail page, `POST /api/bookings` now accepts `seatIds` as a fully separate branch alongside the existing `seats` qty map (GA path untouched, same code, same behavior). All three PRs Vercel-verified green before merge. **Feature is code-complete end-to-end** — a Venue Owner can build a NUMBERED layout and an audience member can book against it. **Not yet done:** no QA venue has a real NUMBERED layout built, so this hasn't been exercised with a live test booking. Next step before calling this fully done is Hitesh building a small test layout and running one real booking through it. Known accepted limitation carried over from the GA path: the per-event occupancy check has a theoretical TOCTOU race under Postgres Read Committed isolation (two concurrent requests could both pass capacity check before either commits) — pre-existing in the GA tier-count check, not new here, not fixed here. **Discoverability fix (PR #143):** the builder was only reachable from the venues list card, not from create/edit — added a callout link inside the venue edit page's Seating & Pricing section. **Grid Generator + stage bar + reset (PR #144, same session, from two real venue references Hitesh supplied):** manual click-to-place alone doesn't scale to a real hall (Scenario A: single 3×5 block, gangway as a side margin; Scenario B: two blocks/30 rows with tapering column counts per row-range and a horizontal gangway between rows 10/11 — both now directly expressible). Added: row-groups (rows × columns, any number of tapering ranges), horizontal aisles (gap after row N), Single or Two-Block layouts with a configurable center-aisle width, row lettering past Z (AA, AB... for 26+ rows), and a visual Stage bar (both builder and audience picker) with a locked-in **stage-facing convention** — left/right in this system means the performer's left, not the audience's, stated explicitly in the builder UI. **Bug caught before shipping, not after:** two-block layouts numbering both blocks 1..N per row would have collided on the `(venueId, row, number)` uniqueness constraint since both blocks share a row letter — fixed to continuous numbering across the aisle (row B: 1-20 left, 21-40 right), which also happens to match real theater signage. Reset button clears local edits only (confirm-gated), nothing server-side until Save. **Still open:** curved/angled rows and balcony/mezzanine tiers were flagged as future scenarios, not built. No QA venue has been built with the generator yet — still the same outstanding "run one real test booking" step from the original three PRs, now also covering the generator path.

**Progress continued (20-21 Jul, sessions 10-11) - architectural redesign, now code-complete:** Hitesh paused freeform coding after #144 to agree a cleaner architecture before continuing; the redesign below is now fully built and merged, all Vercel-green.

- ~~Guided Setup wizard~~ ✅ **Shipped (PR #145).** Plain-language front door onto the grid generator (Guided Setup vs Draw It Myself, both first-class) for venue owners uncomfortable with the freeform canvas.
- ~~Venue creation forced a mandatory GA section even for Numbered venues~~ ✅ **Fixed (PR #146).** Create form now offers Section-based (GA) vs "Numbered seats - I'll build this after" (single approximate capacity number, real layout built later in the builder).
- **Zone-model rewrite** ✅ **Shipped (PR #147, #148).** Confirmed architecture: a seat's pricing identity comes from its row-group ("zone" - e.g. Front/Mid/Recliner), never from which side of a vertical aisle it's on; vertical aisles are pure walkways with no pricing identity (reverses what #145 initially shipped); horizontal aisles are usually a zone boundary by convention, not enforced. Wizard step 2, preview `colorForTier`, and the dead advanced-panel "Section names" block (all flagged broken in the session-10 handoff) rewritten/removed accordingly. Publish-gating for NUMBERED venues (both create and edit paths) and `Venue.capacity` sync after a real seat map is saved, also landed here.
- **Venue Levels** ✅ **Shipped (PR #149).** Real per-level independent layouts (Ground Floor/Balcony/etc.) - starts invisible as one implicit level, a level switcher appears once a second is added. Lives in the main wizard/canvas entry point, not advanced-panel-only, per Hitesh: "We must be able to design seat mapping per level."
- **Per-zone vertical aisles** ✅ **Shipped (PR #149).** Moved from a single global `GridConfig` setting to living on each zone (`RowGroup`) independently - a 16-column zone can have 4 splits while a 20-column zone in the same layout has 2.
- **Per-zone suggested pricing** ✅ **Shipped (PR #149).** New `VenueZonePrice` table (migrated to QA). Deliberately prefill/default only (decision 10a) - organiser can always override at event creation, same as GA section pricing already worked.
- **Row alignment (left/center/right, stage-referenced)** and **multiple horizontal aisles ("+Add another gangway")** ✅ **Shipped (PR #149)**, moved into the main wizard from advanced-panel-only.
- **Vertical aisles at the true zone edge** ✅ **Fixed (PR #150).** Cut-point clamp widened from `[1, cols-1]` to `[0, cols]` inclusive so a walkway can sit against the wall (before seat 1 / after the last seat), not just strictly between two seats.
- **Accidental seat placement on canvas click** ✅ **Fixed (PR #150).** Added an explicit **Manual Placement** toggle - defaults OFF right after Guided Setup (reviewing/scrolling a generated layout is now inert), defaults ON for Draw It Myself.
- **Event-creation pricing gap for NUMBERED venues** ✅ **Fixed (PR #151).** `GET /api/venues` now returns `seats`/`zonePrices`; the event-creation page derives real per-zone pricing sections from the venue's actual `Seat.tierLabel` layout instead of the dead GA-only `Venue.seatMap.sections` field, which had left NUMBERED venues silently falling through to flat free-text Total Seats/Ticket Price (confirmed live against venue V09, a real 150-seat/3-zone venue showing "500 seats / ₹98"). `POST /api/events`, the booking flow, and the audience seat-picker were already fully seatingMode-agnostic - no changes needed there.
- **Misleading "Event published" toast while pending venue approval** ✅ **Fixed (PR #152).** Button relabels to "Check approval status" while `PENDING_APPROVAL`, with an honest toast reflecting the real status the server returns, rather than disabling the button - `PATCH /api/events/[id]` genuinely re-checks approval on every call, so re-clicking while pending is a legitimate recheck, not a no-op.

**Seat-map feature is now architecturally complete end-to-end**, across Guided Setup + Draw It Myself, zones, per-zone aisles/pricing, Levels, and publish-gating. **Real progress this session (21 Jul, session 12) - first live Numbered-venue test, in progress, not yet complete:**
- ✅ **First real Numbered venue published:** "Jaipur Ja Mic 100" (Vijay/venue0001) - built via Guided Setup, 341 seats across 4 zones (General/Back/Zone 3/Zone 4), saved, then successfully Published (was blocked by the Draft-gating bug above until PR #155 landed).
- ✅ **First real event created against it with correct per-tier pricing:** "Jaipur Mic Gala 100" (Mahesh) - Seats & Ticket Price screen correctly showed all 4 zones with independent price fields, pulled from the real seat map (the "critical unresolved gap: event creation doesn't expose per-tier pricing for Numbered venues" noted in earlier handoffs turns out to already be resolved, likely via PR #151 - downgrading that item to closed rather than carrying it forward).
- ✅ **First real Flexible-rate negotiation started:** Mahesh proposed ₹2,999 against Vijay's venue, request correctly routed to Vijay's Flexible Requests (not Booking Requests). Vijay's inability to respond was the stale-session-role bug above, now fixed.
- **Still not done, next step:** Vijay accepting/countering Mahesh's offer (should now work with the role fix), the event auto-promoting to `APPROVED`, and an audience member completing one real seat booking through the seat-picker. This is the actual "first end-to-end NUMBERED booking" milestone - close, but not yet crossed. **Update (22 Jul, session 13):** three bugs found live-testing this exact step (seat-picker overlap, stale booking-error message, checkout page missing seat detail) are now fixed - PR #157, see §9.2. Next attempt at the audience-booking step should be testing the real flow, not fighting these.
- **Organiser seat-map visibility at event creation.** Confirmed still missing, live-observed this session: the Seats & Ticket Price screen shows zone names/counts/price inputs (enough to price correctly) but no visual layout - no way to see zone shape or level. Still a must-build per Hitesh, sequenced right after the end-to-end test above completes.
- **Event-creation pricing aggregates zones by name across ALL levels of a venue** (PR #151 gap, flagged not fixed) - a same-named zone on two different levels (e.g. "General" on Ground and Balcony) merges into one priced tier today. Needs its own pass once Levels sees real multi-level usage.
- **"Split is missing...vertical"** - an earlier live-testing report, never fully reproduced/clarified with Hitesh, raised before the edge-aisle fix (#150) landed. Worth a recheck now that both the per-zone rewrite and the edge-case fix are in.
- Curved/angled rows and balcony/mezzanine-as-distinct-tiers (separate from Levels) still flagged as future scenarios, not built.
- **Multi-platform / external-channel seat accounting** — Hitesh request (19 Jul): how to handle an Organiser who also sells the same event's tickets elsewhere (other platforms, WhatsApp, door sales), so AforAudience's seat count doesn't oversell against real-world availability. Ruled out real-time API sync with competitor ticketing platforms (BookMyShow etc. don't hand out partner APIs to outside apps — enterprise-only territory, and most Organisers' "other channel" here is realistically WhatsApp/cash, not an integrable system anyway). Recommended direction instead: an **allocation model** — Organiser sets total venue capacity as today, plus an optional "AforAudience allocation" (a subset reserved for sale through us, so we structurally can't oversell against it), plus a manual "mark N more sold elsewhere" adjustment field on the event dashboard for keeping the shared pool honest. Not yet confirmed with Hitesh as the final direction or scoped/built.

### 9.5 Business decisions still open, not engineering ones

**Pattern flagged (session 36, 26 Jul):** four separate decisions in this section were marked "Settled" on the date they were decided, but turned out - on actual code inspection, not assumption - to have never been built: venue-required-at-publish (settled 25 Jul session 31, fixed PR #201, 26 Jul), Dress Code/Vibe preset dropdown (settled 25 Jul organiser-testing session, fixed PR #204, 26 Jul), zone naming defaults (settled 25 Jul session 30, fixed PR #206, 26 Jul), and Artist genre multi-select (settled 25 Jul session 30, **still plain comma-separated free text as of 26 Jul - the one remaining unbuilt item**). "Settled" in this doc means Hitesh made the call, not that code shipped - worth a session-start habit of spot-checking a couple of "Settled" entries against actual code, the same way Feedback status gets cross-checked, rather than trusting the label at face value.

- ~~**Zone naming defaults (seat-map builder).**~~ ✅ **Settled (25 Jul, session 30) and built (session 36, 26 Jul, PR #206, merge `977e13e`).** Placeholder/watermark text (not a hard-filled default), cycling **Front → Middle → Back → Recliner**, then falling back to "Zone 5+". Applies to both Guided Setup and Manual Canvas. **Correction on click-through:** implemented as a real pre-filled default value (same mechanism the code already used), not literal HTML placeholder/watermark text - a true placeholder would leave the zone name empty until typed, which breaks zone pricing if left untouched at save. Flagging this minor deviation from the "placeholder" wording rather than silently reinterpreting the decision. New `defaultZoneName()` helper (`Front`/`Middle`/`Back`/`Recliner` then `Zone N`) applied to both the 2-zone wizard toggle and the "+Add Zone" button - previously only the first 2 slots were ever wired in, any 3rd+ zone fell straight to generic "Zone N".
- ~~**Genre field redesign (Artist profile).**~~ ✅ **Settled (25 Jul, session 30).** Searchable multi-select dropdown, multi-genre allowed. Unlisted genres go through a user request → **Admin approval** flow (can request multiple at once), folded into the Admin Dashboard — explicitly not a separate page. While pending, rest of profile stays usable; genre shows friendly placeholder copy, leaning toward **"Still figuring out my genre..."** Directly motivated by a confirmed live bug this session: uncontrolled free text let one artist's genre become their own name, and casing variants created duplicate public filter chips.
- ~~**Dress Code / Vibe fields (Create/Edit Event) — free text → bounded dropdown.**~~ ✅ **Settled (25 Jul, organiser-role testing session) and built (session 36, 26 Jul, PR #204, merge `c2de757`).** Same bug-class as the genre fix above, live-repro'd on these fields this session (free text let literal placeholder/test copy straight into event details). Scoped lighter than genre though: bounded preset dropdown (`Casual`/`Smart Casual`/`Formal`/`Costume / Theme` for Dress Code, `High Energy`/`Intimate`/`Chill`/`Curated`/`Family-Friendly` for Vibe) + an "Other — specify" free-text fallback (capped 60 chars, client-side only so far — no server-side length cap added, low risk since this is just informational display text, not a filter/aggregate). Both fields stay optional. **Auto-fill per `EventType` also built**, Create Event only: `OPEN_MIC`/`STAND_UP`/`LINEUP` → Casual / High Energy; `POETRY` → Casual / Intimate; `THEATER` → Smart Casual / Curated — pre-selected but editable, stops overwriting the moment the organiser directly edits either field. Edit Event gets the picker UI only, deliberately not the auto-fill-on-type-change behavior (would risk silently overwriting an already-set value on an existing event). **Not yet click-tested live.**
- ~~**Venue capacity cap.**~~ ✅ **Settled (25 Jul, session 30).** Hard default max **50,000 seats**. Above that, an explicit "Request extension" action opens a text box, capped under 1,000,000. **Still open:** Claude's suggested refinement (tiered thresholds + mandatory justification field above ~100K) has not been adopted or rejected yet.
- ~~**Venue relocation workflow.**~~ ✅ **Settled (25 Jul, session 30).** Venue Owner proposes relocation (+ seat remap if applicable) → **all affected Organisers must review and consent to both the relocation and the specific seat remap together, before Admin ever sees it** → Admin approval with documentation (property proof/lease + utility bill, tiered by risk) → ticket regeneration (`pdf-lib`) → audience notified via **email + a persistent "Updated" dashboard badge** (clears once viewed) → **Google Wallet sync flagged as a valuable future build, not in this version.** Document collection deferred until after company registration per the existing hard freeze. **Still open:** what happens if an Organiser refuses consent — block the whole relocation, or force a cancellation/refund conversation for just that event?
- ~~**Google Maps Platform budget.**~~ ✅ **Settled (25 Jul, session 30).** Up to **$10/month** approved for Places Autocomplete + geocoding on venue registration (not consumer-facing search). Recommend a Cloud Console billing alert at this threshold once built.
- ~~**Brand elements theme-invariance.**~~ ✅ **Settled (25 Jul, session 31).** Logo, "AforAudience" wordmark, and "Where Art Finds Its Crowd" tagline are theme-invariant, no exceptions, across all themes - directly fixes the confirmed indigo-theme bleed bug (Feedback, 03:09 UTC 25 Jul).
- ~~**Single "+Create Venue/Event" button.**~~ ✅ **Settled (25 Jul, session 31).** Header-permanent button; the empty-state card loses its own duplicate button.
- ~~**Phone-verification block styling at Publish.**~~ ✅ **Settled (25 Jul, session 31).** Disabled Publish button + visible side text stating the reason (not a hover tooltip).
- ~~**Venue requirement at Publish vs. Draft.**~~ ✅ **Settled (25 Jul, session 31).** Venue is required at Publish, optional at Draft only - directly closes the "event published with zero venue attached" bug filed this session. **Built (session 36, 26 Jul, PR #201, merge `07d0433`)** - the decision itself was never actually wired into code until now: `POST /api/events` and `PATCH /api/events/[id]` both let `publish: true` through with no `venueId` straight to `APPROVED`. Both routes now reject that (400) before any status transition, plus a matching client-side check in Create Event. Confirmed via code inspection this closes Feedback `c9d322a1` ("DIMPAL 103 MIC Stand UP") going forward - that event itself is left untouched, kept live intentionally as evidence per the original Feedback note. **Not yet click-tested live** - needs a real Publish attempt on QA with no venue attached, confirming the block message and that Draft still works with no venue.
- ~~**"+1 per artist" field order.**~~ ✅ **Settled (25 Jul, session 31).** Toggle first; the textbox only appears if the toggle is enabled.
- ~~**Buy-in amount default value.**~~ ✅ **Settled (25 Jul, session 31).** Defaults to **0**, not blank, when "Buy-in (pay to play)" is selected.
- ~~**The exact audience booking fee amount** for prod launch~~ ✅ **Settled (22 Jul, session 13).** Hitesh: it's already admin-configurable (`PlatformSettings.audienceBookingFee` via `/dashboard/admin/settings`), ships at ₹0 default — no single hardcoded "the number" is needed for launch, admin sets/changes it whenever ready. Not a blocking decision, just a runtime setting.
- ~~**Refund policy for Checkpoint 5.**~~ ✅ **Settled (22 Jul, session 13).** Hitesh: **full refund.** Cutoff window (e.g. 24hr before event) and whether the booking fee itself refunds along with the ticket weren't specified separately — worth a quick follow-up confirm before C5 is actually built, but "full refund" is the adopted default in the meantime (not partial, not non-refundable).
- ~~**Whether to include the optional checkout tip (K3)** in Phase A or defer to Phase B~~ ✅ **Settled (22 Jul, session 13).** Hitesh: **Phase B**, alongside Venue Pro. Not started, not blocking anything.
- **When exactly to introduce Venue Pro (K5).** The About page commits to "earn the right to charge before we charge" — this is a signal-based trigger (real Venue Owners actively using the platform and specifically asking for Pro-tier features), not a calendar date. Not yet reached.
- ~~**Compensation transparency at apply-time.**~~ ✅ **Settled (22 Jul, session 13).** Hitesh: **before apply** — Organiser must declare/publish compensation type (and amount, where applicable) upfront, visible to an Artist before they apply, not decided only at approval time as today. This is a real schema/UI change (`Performance.compensationType`/`feeAmount`/`buyInAmount` move from approval-time to event/lineup-slot creation time) — not yet scoped or built, now unblocked to be scoped.
- **Email verification as a gate before account activation.** Live feedback (15 Jul) requests this — but it directly conflicts with an already-documented deliberate decision (`verify-email/route.ts`'s own comment, referenced in §9.2/EPIC B): email verification is intentionally non-blocking, only phone/OTP gates real actions like booking. Needs a founder re-confirmation of the existing decision, not a default implementation of the feedback as filed — a real user expected stricter gating than what was deliberately chosen.
- ~~**Numbered-venue publish-gating strictness**~~ ✅ **Settled (26 Jul, session 33) and built (26 Jul, session 34, PR #193).** Hitesh: **strict** — every zone must be priced and every level explicitly declared before a Numbered venue can publish; no gaps allowed. This is the last of the three original seat-map architectural decisions (the other two — zones/aisles as pure walkways, levels in the main wizard — were already resolved and shipped in PR #147/#148/#149). Directly closes the live finding from session 30 (a Numbered venue published with 4 seats, no zones, no pricing, 1 of 2 levels, zero warning). Gate applies at Publish only, not Draft, matching the existing phone-verification and venue-required-at-publish pattern (browse/build freely, gate only at the real commitment).
- ~~**Admin Dashboard severity scale.**~~ ✅ **Settled (26 Jul, session 33).** Hitesh: **Low/Medium/High/Critical**, not P0–P3.
- ~~**Admin Dashboard — do resolved Feedback items stay visible?**~~ ✅ **Settled (26 Jul, session 33).** Hitesh asked for whatever's fastest, deferred the specific mechanism to Claude. Decision: default board view queries `NEW`/`REVIEWED` only — resolved rows are never loaded on initial page load. A "Show Resolved" toggle lazy-fetches them on demand. This gets the performance win (nothing extra loads by default) without the cost of a separate archive page (extra nav click every time you want to check something resolved). Not a full archive view, not always-visible faded column — a lazy on-demand filter.

---

## 10. Session & Handoff Discipline (established seventh amendment)

Two rules that make future sessions efficient, given the collaborator-and-context-window realities of this working model:

1. **This design doc lives in the repo (`docs/design.md`), not in project files.** Loaded on demand, section-scoped, not on every message. Project files are reserved for the current rolling handoff only.
2. **Handoffs are ≤60 lines.** Format: `Shipped` / `Broke or found` / `Blocked on` / `Next best unblocked work`. No lessons-learned narrative (that goes here if it's genuinely reusable), no restating decisions this doc already contains. The 300+ line format was burning 30–40% of the following session's context budget with no useful gain.
3. **Trigger:** when a working session's context usage hits ~90%, Claude produces the handoff and notifies the user rather than pushing through and running out.

---

## 11. Mobile App Strategy (established seventh amendment; QA APK validated eighth amendment)

The original plan (Release 3, ~₹6.4L outsourced, ~5 months to build React Native parity) still stands as the eventual goal but is deliberately deferred until after MVP revenue is proven. In the meantime, the recommended path to an Android-installable app is:

**Stage 1 — PWA-ify the Next.js app.** `manifest.json`, service worker, app icons, install prompt. Shipped in PRs #41 and #42 (eighth amendment). Installable from Chrome on Android ("Add to Home Screen") — home-screen icon, fullscreen, offline shell for saved ticket QRs.

**Stage 2 — Wrap the PWA as a TWA (Trusted Web Activity).** Feed the public prod URL into PWABuilder.com. Produces a signed Android APK, Play Store-ready. **QA APK generated in the eighth amendment** with package ID `com.aforaudience.qa.twa` (throwaway signing key — QA never goes on Play Store). PWABuilder uses Bubblewrap under the hood; if PWABuilder ever becomes flaky, running Bubblewrap locally produces the same output. Time from starting Stage 2 to APK in hand: ~15 minutes.

**Prod path (when ready — post-KYC, post-live-keys):**
- URL: `https://www.aforaudience.com` (not qa)
- Package ID: `com.aforaudience.app` — **reserved permanently for prod, do not use anywhere else**
- Signing key: generate new one via PWABuilder, save the keystore + password to at least three locations (drive, laptop, one more). Loss of this key = new Play Store listing forever.
- Upload the resulting `.aab` to Play Store. Review takes 3-7 days first time.
- Digital Asset Links: `public/.well-known/assetlinks.json` with the app's SHA-256 fingerprint (from the ZIP's `assetlinks.json` file). Without this, the app shows a URL bar. Same setup will apply for QA — a follow-up story.

Advantages of the PWA+TWA path over React Native: one codebase, ships to every phone the same day, no Play Store review cycle for web updates, no React Native/Expo divergence, no separate ~5-month build. Push notifications via Web Push API cover most of what the notification table in EPIC J needs.

**Constraints:**
- TWA validates against a public URL; deployment protection must be off or bypassed. Turned off across the project in the eighth amendment.
- If the TWA hits a wall on a genuinely-native capability (e.g. a QR scanner for venue-door check-ins in Phase 4), then that specific feature justifies going React Native — not the whole app all at once.

Full React Native (Release 3) revisits after MVP traction has been observed on the PWA/TWA path.

---
*Document version: 3.4 — Twenty-third amendment (19 Jul 2026, session 3, 22 PRs total #116–#137, all Vercel-verified before merge).* Session opened with the standing reconciliation pass (4 Feedback-table bugs, all already resolved — one status lagged, fixed; §9.2 type-error sweep closed as a non-issue, no real error existed). Shipped K4 (admin revenue rollup) and H3 (account suspension) — both live-verified against real QA data, not just a clean build; H3's suspension listing-hide was round-tripped live (suspend → confirm hidden → unsuspend → confirm restored) using a real test Organiser account. Built the Netflix-style intro splash end to end through several rounds of live feedback (icon-bars-first sequencing, size doubling, three different wordmark styles tried before landing on typewriter+cursor, a tagline added) — this included catching and fixing a genuine React hydration bug (PR #130): a client-mutated JSX overlay was getting silently reverted by hydration within milliseconds, invisible on desktop, totally absent on mobile; fixed by making the whole overlay one opaque `dangerouslySetInnerHTML` block instead of individually-managed JSX. Also diagnosed and explained an unrelated transient "stuck loading" incident from deploying 6 times in ~30 minutes while devices had tabs open — not a code defect. Closed out the admin nav "arrow-connected sections" complaint (raised independently twice) by replacing arrow-suffixed text links with bordered pill buttons. Shipped the venue-negotiation comment box requested by an actual tester of the Flexible negotiation flow. Shipped live seat-availability status badges (Filling Fast/Spots Available/Sold Out), replacing an old fixed-threshold badge that had no sold-out state. Closed with three new backlog items from Hitesh: seat-map builder for numbered venue seating (scoped as an opt-in second mode alongside general admission, flagged as its own multi-session build — not started), multi-platform seat accounting for Organisers selling the same event elsewhere (recommended an allocation model over chasing competitor-platform API access — not yet confirmed or built), and a live q&a on why the intro-splash hydration fix doesn't require testers to repeat any manual cache-clearing steps on revisit.

**Twenty-fourth amendment — 20 Jul 2026 session.** Opened with the standing reconciliation pass against the Feedback table: 3 open BUG-category items, all already resolved or already correctly deferred — venue-owner OTP + sticky-banner report was stale (both fixed PR #97/#102), marked RESOLVED in the DB; artist-profile-from-lineup and username-autosuggestion reports both already carry accurate "needs re-triage" / "no new repro" notes in §9.4, left as-is. No code work followed from Feedback this session. Remainder of the session was a dedicated design pass on the seat-map builder (see §9.4 entry, rewritten in place) — ground-truthed against the actual schema first (confirmed `Venue.seatMap` is dead weight, capacity is really tier-count-based today) before proposing the `Seat`/`BookingSeat` model split, confirmed with Hitesh that seat placement needs real x/y canvas coordinates (not a rows×columns grid) to represent true venue shapes. Design confirmed, not yet built — next session starts on migrations + builder UI.

**Twenty-fifth amendment — 25 Jul 2026 session.** Entire session was live manual QA by Hitesh (web + mobile), Claude in strict note-only mode throughout at Hitesh's explicit instruction. ~125 numbered findings logged live, then reviewed and filed at session end: 37 confirmed bugs inserted into the `Feedback` table (category=BUG, status=NEW); the structurally significant ones (P0 seat-map race condition/data-loss, publish-gating gap, no venue-snapshot-on-booking gap, Price Range display bug, header-wrap bug, two e2e test failures) added to §9.2 above. Five business/design decisions settled and added to §9.5 (zone naming defaults, genre field redesign + admin-approval workflow, venue capacity cap, venue relocation workflow, Google Maps budget). The three original seat-map architectural decisions (zones/aisles as pure walkways vs. labeled; publish-gating strictness; levels in wizard vs. advanced-only) remain fully open — today's publish-gating finding gives real supporting evidence toward "should be stricter" but Hitesh has not yet ruled. No code was written or merged this session.

**Twenty-sixth amendment — 25 Jul 2026, session 31 (same day, follow-on session).** Continued the manual-QA + design-decision pattern from session 30, this time centered on Dimpal's Organiser-side event testing. Corrected two session-30 findings after proper re-testing: the "Numbered-venue pricing gap" (Feedback) was verified as a non-issue - per-tier pricing UI works correctly once a venue with a real seat map is attached, confirmed end-to-end on NiKi 2600; likely resolves the `numbered-seat-booking.spec.ts` e2e failure too, recommend rerunning it before further investigation. The "bare loading states" bug was refined from device-level to page-level (Dashboard has a branded icon, `/tickets/` doesn't, same device). Two new bugs filed: publishing an event with zero venue attached (kept live on `DIMPAL 103 MIC Stand UP` as evidence), and Edit Event using a stale pricing form that doesn't correctly prefill section/offer/buy-in amounts versus Create Event. Six new business decisions settled and added to §9.5 (brand-element theme-invariance, single create button, phone-verify block styling, venue-required-at-publish, +1-per-artist field order, buy-in default value) - the venue-required-at-publish decision directly closes the zero-venue-publish bug above. One open suggestion not yet decided: venue list cards should show rate model + rate together instead of "— per seat". P0 seat-map race condition (session 30) remains unfixed and untouched per the sequencing-risk note above - `NIKI 4700 Duplex Canvas` deliberately left live and untested pending that fix. The pre-existing 4 Feedback BUGs + 2 REVIEWED items from 15 Jul (re-confirmed present at both session 30 and 31 starts) are still not reconciled against this document - carrying forward as a standing task. No code was written or merged this session.

**Twenty-seventh amendment — 25 Jul 2026 session (fresh session per HANDOFF_20).** Closed out the "15 Jul backlog not reconciled" standing task carried across sessions 30/31 - it turned out to already be done, and the "still open" framing itself was the stale part. Checked all 6 BUG-category Feedback entries from 15 Jul directly against this document and, where ambiguous, against actual code on `qa`: **all 4 `RESOLVED` ones are genuinely fixed and already documented** (free-event "₹0" label → line ~595, PR #104; country-dial-code login → ninth amendment; homepage marquee loop → eleventh amendment #58; mobile sort-dropdown misalignment → eleventh amendment #59), and **both `REVIEWED` ones already carry accurate "needs re-triage, not yet closed" notes** (username auto-suggestion, artist-profile-clickable-from-lineup) - this part was in fact already reconciled in the Twenty-fourth amendment (20 Jul), which the Twenty-fifth/Twenty-sixth amendments didn't check before re-asserting it as outstanding. Recommend future sessions verify a "standing task" against this doc's own history before re-carrying it forward - exactly the "broader lesson" the Twenty-sixth amendment itself named, applied here to its own claim.

While in the area, also code-verified (not just DB-status-trusted) the three cosmetic items noted in §9.4 above ("Smaller cosmetic/UX items from earlier feedback, 15 Jul") - found and corrected a real doc/DB mismatch there (see that entry, updated in place): the mobile List/Grid toggle fix is confirmed genuine, but the Rasa-description and footer-heading items' `RESOLVED` status was unverified/premature - **Hitesh's ruling (same session): both confirmed unverified, reverted `RESOLVED` → `REVIEWED`, explicitly low priority, deferred until all current high/medium-priority work is done.**

**Rest of session: organiser-role testing (Dimpal persona), web + mobile in parallel, screenshot-driven.** Six new bugs filed to `Feedback` + this doc's §9.2, in order found:
1. Create Event validation banner doesn't re-validate once fields are corrected - stays showing the original stale missing-fields list.
2. Offer Amount (negotiated-pricing Manual Canvas venue) not validated at Publish - blank value doesn't block submission, inverting the intended negotiation flow (venue owner ends up naming a price first instead of responding to one).
3. Artist Payment Terms "Paid" allows blank "Fee per artist" at Publish - same missing-validation family as #2, defeats the payment-transparency feature shipped a few days ago.
4. **P0/P1 - Venue Booking negotiation has no upper-bound price validation at all**, unlike per-section seat pricing which correctly caps at ₹1,00,00,000 (confirmed working, same session). Overflow-scale garbage entered by both sides across several rounds on `DIM MIC - 200 LINEUP` (`NiKi 2600 MANUAL Canvas`) reached `CONFIRMED` status. **Real corrupted record now in QA:** `VenueBooking` id `cms0oc890000504jigj4zfkq0`, `amount=3.33333333333333e+90`. `platformFeeAmount` on it is 0 (no corruption risk there), but it will break any Sales/Revenue view that sums/scales booking amounts. **Flagged for cleanup, not touched - same treatment as `NIKI 4700`, needs Hitesh's go-ahead before deleting.**
5. Venue Booking Requests list (organiser side) doesn't auto-refresh to reflect a remote status change - showed `Pending` after the same booking was already confirmed `Accepted` on the venue owner's side (push notification + DB both confirm `Accepted`). **Flagged as a possible third instance of the P0 poll/refetch pattern** already recommended for a codebase-wide grep - folding this in rather than treating as separate.
6. One `FEATURE_IDEA` filed (not a bug): Dress Code/Vibe should become bounded dropdowns + "Other" fallback, with defaults auto-filled per `EventType` (Hitesh-aligned mapping in §9.5) - motivated by live garbage free-text in these fields, same bug-class as the genre-field fix.

**Four separate instances of the same missing-validation pattern found in one session** (#2, #3, #4 above, plus the seat-price cap that *is* working correctly as the counter-example) - worth building as one shared price-validation utility/schema when picked up, not four individual patches.

**Full technical scope written for the Advanced Admin Dashboard** (Hitesh's stated top priority) - evolves the existing `/dashboard/admin/feedback` page rather than a from-scratch build. Both trend charts and board/list/detail view are in v1 (Hitesh's call), self-serve status/severity editing is in too, scoped narrowly (message stays immutable, every change auto-logs to a new changelog). Schema needs: `severity` enum, `resolvedAt` timestamp, `status` as a real enum, short `title`, `FeedbackChangeLog`. Two things genuinely undecided, not guessed at: the severity scale/labels, and whether resolved items stay visible (faded column) or move to a separate archive view. Full detail at the top of §9.1.

**New standing rules added to Claude's memory this session:** (1) log observations to `Feedback`/this doc continuously as found, not batched at session end - already the practice by the second half of this session; (2) if a session runs longer than ~2 hours, proactively flag it and suggest a handoff + fresh session rather than letting it run indefinitely.

No code was written or merged this session; this was a documentation/reconciliation pass plus a screenshot-driven manual testing pass with Claude filing in real time, as requested.

**Twenty-eighth amendment — 26 Jul 2026, session 37 (fresh session per HANDOFF_36).** Opened with the standard session-start reconciliation: swept the Feedback table, cross-checked against this doc, found everything accounted for except one genuinely new signal - `8560fb61` (artist profile photo) had been wrongly marked `RESOLVED` twice in prior sessions, both times checking the *public* profile's fallback avatar styling rather than whether an Artist can actually set a real photo. Reopened it properly rather than leaving the stale status stand.

**Four PRs shipped, all CI-verified before merge:**
- **PR #208 — in-app messaging.** Hitesh's go-ahead given this session. Generic `Conversation`/`ConversationParticipant`/`Message` schema (one shape for all three confirmed-relationship pairs - PERFORMANCE/VENUE_BOOKING/BOOKING), not three separate builds. Audience↔Organiser settled as per-booking (Hitesh's call). Broadcast fan-out (Organiser→lineup) confirmed as designed - one message lands in each artist's own private thread, replies stay private. Inbox + thread UI, ~15s poll, push via existing `sendPushToUser`. Wired entry points into Organiser's lineup page (+broadcast composer), Venue Owner's confirmed bookings, Audience's tickets. Organiser-side VenueOwner entry point deliberately left unwired (no existing page surfaces a raw `VenueBooking` id on that side) - functional via the shared inbox regardless, just no dedicated button yet. No nav link added deliberately (risked reopening the SiteNav width-wrap bug from session 36) - placement is Hitesh's call, still open.
- **PR #209 — real root cause fix, not just a symptom patch.** Investigating why `8560fb61`'s screenshot showed a field that's been in the code since 23 Jul led to a bigger find: `public/sw.js`'s `CACHE_VERSION` hadn't been bumped since 14 Jul despite the file's own comment saying to bump it on every cached-route change - meaning any device that ever hit the navigation cache fallback even once could silently serve a week+-stale page indefinitely. Fixed by auto-deriving the version from `VERCEL_GIT_COMMIT_SHA` at build time via a new `prebuild` npm hook - removes the "someone has to remember" failure mode entirely. This was a genuinely valuable catch, not scope creep - it could explain any past "why isn't my fix showing" confusion during testing generally, not just this one field.
- **PR #210 — City/State/Country dropdown at venue registration.** Closes Feedback `f7bd07eb`, unblocked once Hitesh provisioned the Google Maps API key this session (`GOOGLE_PLACES_API_KEY`, Vercel Preview env var, server-only - walked through Google Cloud Console setup live with Hitesh, including a billing-account gate and an API-restriction step). New `Venue.state`/`Venue.country` columns (nullable/additive). Server-side proxy (`src/lib/places.ts` + 2 routes) so the key never reaches the browser. Session tokens used throughout so a full search-and-pick bills as one cheap Google session, not per-keystroke. Manual typing with no dropdown match still works.
- **PR #211 — Address autocomplete + real venue lat/lng, same session follow-up.** Hitesh asked what happens to Address (still free text after #210). Found while investigating: `Venue.lat`/`Venue.lng` existed in the schema since day one but nothing had ever populated them - "Get Directions" on the public venue page has therefore never worked for any venue until now. Built as ONE merged Address-level lookup deriving City/State/Country/lat-lng from the same Place Details response, specifically to avoid doubling the API call volume/cost of #210 - a naive "just add a second search" implementation would have doubled it. `includedPrimaryTypes` includes `establishment` so typing a venue's actual name can surface its real indexed address.

**Real-world confirmation on Google Maps cost, worth recording:** initial testing hit a 403 - `Places API (New) has not been used in project... or it is disabled` - a genuine Google Cloud project-level enablement gap (separate from the key-level API restriction Hitesh had already set correctly), found via Vercel runtime error logs, not guessed. Hitesh enabled it directly via the URL Google's own error message provided. Once working, Hitesh shared the actual Google Cloud pricing tiers for Autocomplete Requests: **free up to 10,000/month**, then ₹267→₹20 per 1,000 across five descending volume tiers. Given this platform's expected volume (venue registrations, not public-facing search), this should sit comfortably inside the free tier - worth confirming against real Cloud Console usage after a few weeks live rather than assuming, per the standing recommendation already in §9.4.

**Three backlog items scoped and logged (§9.4 + Feedback, category=FEATURE_IDEA), all explicitly "not now" per Hitesh:**
1. Artist Profile Picture - real file upload (Vercel Blob), replacing the URL-paste-only field.
2. Acoustic Rating redesign - rated by Artist/Organiser only (Audience excluded), conflict-of-interest exclusion when the Organiser is also the venue owner (real code check needed, not just policy - this app's multi-role support makes it a reachable case), "Not Rated Yet" default for new venues, Venue Owner's editable textbox removed entirely and replaced with a read-only label everywhere they see it too.
3. Field-level (i) help icon, label-attached not a textbox, for the handful of genuinely non-obvious fields (Acoustic Rating, "Flexible" rate type, Google Maps Link) - reusable component, not scoped to venue creation alone.

**A fourth item (Address autocomplete + venue lat/lng) was briefly backlogged the same way mid-session, then Hitesh said "proceed" on it directly - built and shipped same session as PR #211 above, rather than staying in the backlog.**

**Four new standing rules established this session, now with a canonical copy in §9.0 above** (collaborator pushback + 15-min goal-alignment check; 30-min Feedback-table registration cadence; 2-hour handoff checkpoint; start-of-session Razorpay/Google-billing reminder in bold/red).

**Not yet click-tested live** - PRs #210 and #211 both need a real venue registration/edit on QA (address search, city/state/country autofill, "Get Directions" now pointing somewhere real) now that the Places API is confirmed enabled. The e2e `workflow_dispatch` run kicked off early this session came back `failure` overall; per-spec breakdown couldn't be confirmed (Azure blob log storage, same known sandbox limitation as prior sessions) - likely the two pre-existing known failures (`numbered-seat-booking.spec.ts`, `waitlist-wallet-credit.spec.ts`), not a new regression, but not independently verified this session.
**Twenty-ninth amendment — 26/27 Jul 2026, session 38.** Opened with Rule 4 reminder (Razorpay + Google Maps billing check). Hitesh confirmed live click-test of #210/#211: address search works, city/state/country autofill correctly. Closes the "not yet click-tested" item from session 37's handoff for the address-autocomplete core function.

**PR #212 — auto-derive Get Directions from Address autocomplete, merged same session (squash f94d6f37).** Follow-up Hitesh raised immediately after confirming search worked: once Address is resolved via autocomplete, asking the owner to separately find-and-paste a Maps share link is redundant and a source of drift (pasted link could point somewhere slightly different from the resolved address). Built as:
- New `Venue.placeId` column (nullable, additive, mirrors lat/lng's lifecycle - all three set/cleared together). Migration applied to QA via `Supabase:apply_migration`.
- Google Maps Link field now renders two ways: **read-only "📍 Directions" link** (built from `placeId` for an exact named pin) when Address was autocompleted; **editable paste input** (unchanged) when typed manually. Manual case's Get Directions never dead-ends either way - new address-text-search fallback added as the last tier.
- New shared `buildDirectionsUrl()` in `src/lib/maps-url.ts` - single source of truth for the 4-tier fallback (pasted mapsUrl > placeId exact pin > raw lat/lng > address text search), used by both the public venue page and the owner's create/edit forms so they can't drift into showing different links for the same venue.
- `AddressAutocomplete.tsx` gained an `onManualEdit` callback, fired only on real user typing (not on the component's own programmatic selection) - lets the parent form clear lat/lng/placeId and correctly revert the field to editable if an autocompleted address is edited back to free text.
- Field also moved up to sit directly after Address/City/State/Country, grouping all location-identity fields into one visual block ahead of Facilities/Acoustic Rating (Hitesh's call).
- `tsc --noEmit` sweep: 59 pre-existing Prisma-client-not-locally-generated baseline errors, zero in any file this PR touched.

**PR #213 — same-session fix, two real bugs from Hitesh's live click-test of #212 (squash abef096).**
1. Directions link 404'd. `/maps/place/?q=place_id:ID` (used in #212) is an Embed API iframe src, not a real navigable deep link - confirmed on device via screenshot (Android Maps app treated the raw place_id as literal search text, "No results found"). Switched to the documented Google Maps URLs API format: `query=<lat,lng>&query_place_id=<placeId>`. Also collapsed both form pages' inline duplicate of this logic to import the shared `buildDirectionsUrl()` helper - both copies had carried the same bug, exactly the drift risk the helper was meant to prevent by existing in only one place.
2. Address/City grid was fixed `1fr 1fr` with no mobile breakpoint - on real phone width this made the Address column narrow enough that long place names wrapped 4-5 lines, growing the autocomplete dropdown tall enough to visibly bleed over the Google Maps Link field below (confirmed via screenshot). Switched to `repeat(auto-fit, minmax(240px, 1fr))` - stacks to one column below ~500px container width, no media query needed.

Related but out of scope this session: the `rateType === 'HOURLY'` rate-fields grid (create page) has the same fixed-`1fr 1fr` pattern but is plain number inputs, not autocomplete-dropdown-driven - lower urgency, folds into the existing backlog item "Manual Canvas mobile-responsiveness + UX polish" rather than a separate fix.


**Click-test confirmation, same session (TWA + PWA, side by side).** Hitesh tested venue registration on both install types. #213's fixes hold on both: Address/City stack to one column on mobile, dropdown no longer overlaps content. **Directions link confirmed end-to-end working** - tapping it opens a real Google Maps pin for the resolved address (rating, reviews, Directions/Start/Ask buttons), not the garbled place_id text from before.

Two new things surfaced, both logged to Feedback:
1. Two more garbage-value venue records found in "Your Venues": `NIKI 5200 MIC DROP` (PUBLISHED, 52 seats) and `NIKI 5500 FIX MIC 2607` (DRAFT, 104 seats), both with address/city literally `GARBAGE VALUE, GARBAGE VALUE` - same corrupted-data class as the already-flagged `NIKI 4700`. **Hitesh's call: no cleanup for now, leave as-is.**
2. ~~Splash screen truncation~~ - not a bug. Hitesh confirmed the `AforAudien` text was just the splash animation caught mid-reveal at the moment the screenshot was taken, not a real rendering issue. Closed.


**PR #214 — seat map wizard preview scroll trap, same session (squash 93a1368).** A wide venue (279 seats/22 rows) cut off its left edge in the Preview step with no way to scroll to it. Root cause: `display:flex; justify-content:center` with `overflow:auto` is a known browser trap - only one side of an overflowing centered child stays scrollable, the other becomes permanently unreachable. Fixed by swapping to `margin:'0 auto'` on the child, keeping the same centering when content fits but preserving full bidirectional scroll once it overflows.

**PR #215 — Acoustic Rating: owner self-report removed, read-only label everywhere (squash 270e5a3).** Ships the session-37 redesign, pulled into scope this session. Create/edit forms: editable input gone, static "Not Rated Yet" label + note that real ratings come from Artist/Organiser feedback. Public venue page + owner dashboard card: previously hid the block entirely when null - now always shown. **Deliberate display decision, flagged not silently decided:** any already-published venue with an old self-reported number in `acousticRating` shows "Not Rated Yet" too, not the old number - self-reported numbers were the whole problem, so displaying them would undercut the fix. DB values untouched (no cleanup). Still genuinely open: the real Artist/Organiser feedback collection + aggregation system - needs its own design pass (which model it rides on, aggregation method, conflict-of-interest exclusion when Organiser is also the venue owner) before that half gets built.


**PR #216 — venue edit page was missing its entire Rental Rate section (squash ae1ef75).** Found while checking a smaller field-help-icon backlog item, not from a direct report - grep confirmed zero references to rateType/hourlyRate/dailyRate/dayRates anywhere in the edit page, and no separate pricing route exists either. A venue owner genuinely had no way to update their rate (or day-wise overrides) after initial creation - the whole section only ever existed on the create form. Fixed by mirroring create page's section exactly (rate type selector, validation, day-wise override picker), extending PATCH /api/venues/[id] with the same rate handling POST already had, and adding `dayRates` to GET /api/venues/[id]/owner's include so the edit page can hydrate existing overrides on load.


**PR #217 — venue identity-lock guardrail, immediate mitigation not the full fix (squash 7297384).** The genuine gap flagged by Claude proactively this session: `Event.venueId`/`Venue.seatMap` are live mutable references with no snapshotting (Feedback 94071451) - an owner could silently change a venue's name/address/seat layout after audience tickets already sold against the current version, especially now that editing venues is smoother post-#216. The real fix (snapshot-at-publish vs full lock vs warn-and-allow) is a genuine architecture decision needing its own design pass, same treatment as the seat-map redesign - not guessed at. Shipped the safe narrow mitigation instead: PATCH /api/venues/[id] now blocks name/address/city/GA-seatMap.sections changes once a published (APPROVED) event references the venue; facilities/rate/acoustic-rating/publish are unaffected, and only fields the request actually tries to change are checked (no-op resubmission isn't blocked). Also verified NUMBERED venues' separate seat-save path (`/api/venues/[id]/seats`) doesn't have this gap at all - already protected by a real `onDelete: RESTRICT` FK against `BookingSeat`, confirmed by reading the route rather than assumed.


**PR #218 — venue publish via Edit was bypassing phone verification entirely (squash bda31bd).** Investigated the terse Feedback report "Venue publish without verify mobile number" (filed session 34, never actioned) - confirmed real via code, not assumed from the vague wording. POST /api/venues (create) has always correctly gated Publish behind `requireVerifiedPhone()`. PATCH /api/venues/[id] (edit) never called this guard at all - an unverified owner could save a venue as Draft first, then publish it later via Edit instead of at creation, fully bypassing the exact protection this gate exists for (Organisers plan real bookings around a published venue). Fixed by adding the identical guard to PATCH - same `publish === true` condition, same action wording - so Draft saves stay unaffected and only Publish is gated, matching create exactly.


**PR #219 — Artist Genre converted to preset+Other pattern, closing the last free-text outlier (squash dce1d1d).** Hitesh asked to sweep for similar "free-text producing garbage public data" issues across the app, citing Facilities as the example of the pattern already fixed. Confirmed genre was the one remaining outlier - checked real production DB values before building, not guessed: "Standup comedy"/"Stand-up"/"standup comic" as three separate filter chips (public artists listing filters by exact string match), "Aman" (an artist's own name), role-words ("Singer"/"Dancer"/"Poet") instead of an actual genre. New `GenrePicker` component mirrors `FacilitiesPicker` exactly - bounded preset list grounded in this platform's real acts (comedy/poetry/storytelling/spoken word/music/dance/etc, not a generic taxonomy) + capped "Other" fallback. Still `string[]`, no schema/API change. Checked Style Tags for the same risk - confirmed via the public artists page it's display-only, never used to build filter chips, so left as free text (lower priority, not the same fragmentation risk). Existing garbage genre values in DB not retroactively cleaned - same "no silent cleanup" precedent as the garbage venue records, self-correct next time each artist edits.


*Confidential — Do not share*
