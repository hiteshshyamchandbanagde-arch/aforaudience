import ComingSoon from "@/components/ComingSoon"

// GEN-2609-003 (Mobile Redesign Phase 1) - tab-root placeholder. Real
// Saved/bookmark schema + API is Phase 4 scope; this route exists now so
// the mobile tab shell has a real, deep-linkable 4th destination instead
// of a dead link, per that phase's own instructions.
export default function SavedPage() {
  return <ComingSoon title="Saved" description="Bookmark events to come back to them later — coming soon." />
}
