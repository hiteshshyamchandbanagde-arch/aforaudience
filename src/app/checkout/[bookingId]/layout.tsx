// GEN-2609-003 (Mobile Redesign Phase 1) - mount-in push transition for
// this full-screen route when reached from EventDetail's Book flow. A
// route-segment layout rather than editing page.tsx (902 lines, several
// booking-state early returns) - see globals.css's .afa-push-mount rule
// for the actual animation (mobile-only, reduced-motion aware).
export default function CheckoutLayout({ children }: { children: React.ReactNode }) {
  return <div className="afa-push-mount">{children}</div>
}
