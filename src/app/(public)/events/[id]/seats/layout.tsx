// GEN-2609-004 (Mobile Redesign Phase 2) - mount-in push transition for
// this full-screen route, same pattern as checkout/[bookingId]/layout.tsx
// and events/[id]/layout.tsx from Phase 1. See globals.css's
// .afa-push-mount rule (mobile-only, reduced-motion aware).
export default function SeatSelectionLayout({ children }: { children: React.ReactNode }) {
  return <div className="afa-push-mount">{children}</div>
}
